// ปลุกตัวกวาดดันสต็อก **shopee** ตามเวลา — เพิ่ม 17 ก.ย. 2569 (gucut2 · ท่านประธานสั่ง "ทำให้ครบ")
// แยกฟังก์ชันต่อเจ้า: คิดแผนหนึ่งเจ้าก็ ~10–14 วิ ⇒ รวมสามเจ้าในรอบเดียวเกินเพดาน ~30 วิของฟังก์ชันตามเวลา
// ⏰ นาที 5,20,35,50 — เหลื่อมกับ Lazada (*/15) ไม่ให้สามเจ้าวิ่งพร้อมกัน และไม่ชนกุญแจ push_sweep_log.at
// ⛔ **ค่าเริ่มต้นคือซ้อม** — ยิงจริงเฉพาะ env `STOCK_PUSH_AUTO_SHOPEE=1` (ไม่ใช่ STOCK_PUSH_AUTO ของ Lazada ที่เปิดอยู่แล้ว)
//    โหมดซ้อมยังเขียนสมุดสถานะครบ ⇒ จอเห็นช่องทางนี้ได้ และรอบกวาดยืนยันของที่สั่งยิงมือได้
// สั่งกวาดเดี๋ยวนั้น: POST /api/core?pushsweep=1 body {"platform":"shopee"}

export default async function handler() {
  try {
    const { กวาดดันสต็อก } = await import("../lib/stock-push-sweep.mjs");
    const r = await กวาดดันสต็อก({ platform: "shopee" });
    console.log("[กวาดดันสต็อก shopee]", JSON.stringify(r));
  } catch (e) {
    console.error("[กวาดดันสต็อก shopee] ล้มเหลว:", e?.message || e);
  }
}

/* ⏱️ **วันละครั้ง — ท่านประธานสั่งเอง 17 ก.ย. 2569: "อัปเดตสต็อกแค่วันละ 1 ครั้ง"**
   ของเดิม "5,20,35,50 * * * *" = 96 รอบ/วัน · สองตัวรวมกัน 192 รอบ/วัน
   ⇒ กินเครดิตคืนเกือบหมดที่เพิ่งลดไป 69% เพราะเครดิต Netlify คิดตาม**เวลารันฟังก์ชัน**
     (ไม่ใช่จำนวนครั้งที่เรียก) — รอบที่ไม่มีอะไรให้ทำก็ยังเสียเวลาเปิดฐาน D1 ทุกรอบ
   ⇒ ตั้งเป็นวันละครั้งให้ตรงกับ Lazada (`stock-push-sweep.mjs` = 0 22) แล้วเหลื่อมเวลา
     กันสามตัวแย่งเพดานเวลาเดียวกัน · 05:10 น. ไทย
   🚫 **ห้ามเพิ่มความถี่กลับเองโดยไม่ได้สั่ง** — สต็อกเก่าได้ถึง 24 ชม. เป็นผลที่รับรู้แล้ว
      ของด่วนให้สั่งเดี๋ยวนั้นด้วย POST /api/core?pushsweep=1 ไม่ใช่เพิ่มรอบ */
/* ⏰ **ท่านประธานสั่งปรับกลับให้ถี่ขึ้น 18 ก.ย. 2569** — เมื่อเช้าถูกลดเหลือวันละครั้ง (ตี 5)
   เพื่อประหยัดเครดิต แต่ผลที่ตามมาคือ **สต็อกบนมาร์เก็ตเพลสเก่าได้ถึง 24 ชม. ⇒ เสี่ยงขายเกินสต็อก**
   และหนักขึ้นเพราะทิศลง (ลดสต็อก) ก็เกิดวันละครั้งเหมือนกัน
   ตัวเลขที่ใช้ตัดสิน (วัดจริงจาก push_sweep_log 18 ก.ย. 2569 ไม่ใช่ประมาณ):
     lazada 12,158 ms · shopee 7,127 ms · tiktok 2,490 ms ⇒ รวม ~22 วิ/รอบทั้งสามเจ้า
     ทุก 15 นาที = ~35 นาที/วัน · เทียบกับเครดิตที่ compute เป็นแค่ 7.7% (deploy 89%)
   🔴 **และทางออกเร็ว (FASTPATH) ยังไม่ช่วยเลยในสภาพตอนนี้** — มันเข้าเงื่อนไขเมื่อรอบก่อน
      planned = 0 เท่านั้น แต่ของจริง planned = 57/59/46 เพราะมีรหัสค้าง (error 85 · ถูกข้าม 91)
      ⇒ ห้ามคิดว่า "มี fastpath แล้วถี่ขึ้นฟรี" ต้องเคลียร์ของค้างก่อนถึงจะได้ส่วนลดนั้น */
export const config = { schedule: "5,20,35,50 * * * *" };
