// ปลุกตัวกวาดดันสต็อกตามเวลา — ท่านประธานสั่ง 17 ก.ย. 2569 "อยากให้มันออโต้ อัปเดตเอง"
//
// ⚠️ ฟังก์ชันนี้ "ไม่มี URL" โดยตั้งใจ (Netlify ไม่ให้ฟังก์ชันมี schedule พร้อม path)
//    อยากสั่งกวาดเดี๋ยวนั้น: **POST** `/api/core?pushsweep=1` (ต้องมีรหัสหลังร้าน)
//    🔑 กฎ "ของที่ไม่มีอะไรจุดชนวน": งานตามเวลาต้องมีทางสั่งเดี๋ยวนั้นเสมอ
//       ไม่งั้นตอนมันพัง จะพิสูจน์ไม่ได้ว่าพังที่ตัวงานหรือพังที่ตัวปลุก
//
// ⛔ **ค่าเริ่มต้นคือซ้อม ไม่เขียนอะไรกลับแพลตฟอร์ม** — ต้องตั้ง env `STOCK_PUSH_AUTO=1` ถึงจะยิงจริง
//    โหมดซ้อมยังเขียนสมุดสถานะครบ ⇒ จอเห็นความจริงได้ทันทีโดยยังไม่มีอะไรวิ่งออกนอก
//
// ตัวทำงานจริงอยู่ที่ netlify/lib/stock-push-sweep.mjs

export default async function handler() {
  try {
    const { กวาดดันสต็อก } = await import("../lib/stock-push-sweep.mjs");
    /* ⚠️ **ต้อง await** — Netlify แช่แข็งฟังก์ชันทันทีที่ตอบเสร็จ
       งานที่ไม่ await ตายกลางทางแบบไม่มี error (กัดมาแล้ว 3 ครั้งในโครงการนี้) */
    const r = await กวาดดันสต็อก({ platform: "lazada" });
    console.log("[กวาดดันสต็อก]", JSON.stringify(r));
  } catch (e) {
    /* พังก็ต้องพังให้เห็น — เงียบไปเฉย ๆ คือสิ่งที่ระบบนี้สร้างมาเพื่อกำจัด */
    console.error("[กวาดดันสต็อก] ล้มเหลว:", e?.message || e);
  }
}

/* ทุก 15 นาที — เลือกจากความเสี่ยงที่แพงที่สุดของร้านคือ "ขายเกิน"
   ช่องที่รู้ช้าสุดจึงควรสั้นกว่าเวลาที่ของชิ้นสุดท้ายจะถูกสั่งซ้ำ
   ⚠️ ปรับถี่กว่านี้ต้องดูเพดานเรียก API ของแพลตฟอร์มก่อน (ยังไม่ได้วัด) */
export const config = { schedule: "*/15 * * * *" };
