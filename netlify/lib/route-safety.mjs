/* 🛑 เส้นที่ **เขียน/ลบของจริง ทั้งที่ไม่มีด่าน method** — ต้องบอกด้วยมือ เพราะเครื่องอ่านไม่ออก
 *
 * 🔴 ที่มา 19 ก.ย. 2569: ฝั่งจอกวาดเส้นในท่อเพื่อดูว่าจอใช้ครบไหม แล้วแยกกอง "ยิงสำรวจได้"
 *    ด้วย **คำนำหน้าชื่อ** ⇒ `saledel` (ลบใบขายจริง) หลุดเข้ากองนั้น เพราะคำบ่งอยู่ท้ายชื่อ
 *    เขาจับได้เองก่อนยิง แล้วเปลี่ยนชื่อกองเป็น "ยังไม่รู้ว่าอ่านหรือเขียน" ⇒ ถูกต้อง
 *
 * 🔑 แล้วตัวติดป้ายอัตโนมัติของฝั่งท่อ (`routeMethod` ใน endpoints.mjs) ก็มีขอบเขตของตัวเอง:
 *    มันตัดสินจาก **ด่าน `req.method` ในบล็อกของเส้นนั้น** ⇒ เส้นที่ไม่มีด่านเลยได้ป้าย `unknown`
 *    ⚠️ `unknown` **ไม่ใช่ `readonly`** — และในกอง unknown มีเส้นที่เขียนของจริงอยู่ด้วย
 *    ⇒ ไฟล์นี้คือรายชื่อที่ **ตรวจด้วยตาแล้ว** ว่าเขียนของจริง แม้ป้ายอัตโนมัติจะบอกว่าไม่รู้
 *
 * ⚠️ รายชื่อที่คนเขียนด้วยมือจะค้างเสมอ ⇒ มีด่าน `route-safety.test.mjs` บังคับสองทิศ:
 *    ① ชื่อที่ไม่มีในสารบัญเส้นแล้ว (เปลี่ยนชื่อ/ถอดออก) ⇒ ตก
 *    ② ชื่อที่ได้ป้าย `write` จากตัวอัตโนมัติแล้ว ⇒ **ต้องถอดออกจากไฟล์นี้** ⇒ ตก
 *       (ทิศที่สองทำให้รายชื่อ "หดเอง" เมื่อมีคนไปใส่ด่าน method ให้เส้นนั้น
 *        ถ้าไม่มีทิศนี้ ไฟล์จะโตทางเดียวแล้วกลายเป็นรายชื่อที่ไม่มีใครเชื่อ)
 *
 * 🚫 **ห้ามใช้ไฟล์นี้เป็นด่านกันเขียน** — มันเป็นป้ายเตือนคน/เครื่องมือสำรวจ
 *    การกันจริงต้องอยู่ที่ตัวเส้นเอง (ด่าน method + confirm) ไม่ใช่ที่รายชื่อ
 */

export const เขียนแม้ไม่มีด่านMethod = [
  {
    ชื่อ: "resettransfers",
    ทำอะไร: "ล้างกระจกใบโอนแล้วเริ่มใหม่ — เรียกด้วย GET เฉย ๆ ก็ทำงาน",
    ตรวจเมื่อ: "2026-09-19",
    ค้างอยู่: "ยังไม่ใส่ด่าน method เพราะไม่รู้ว่าจอเรียกด้วย GET อยู่หรือเปล่า — ถามฝั่งจอแล้ว",
  },
  {
    ชื่อ: "d1move",
    ทำอะไร: "ย้ายฐาน D1 — ขั้น create/copy เขียนของจริง (ขั้น plan/verify/ping อ่านอย่างเดียว)",
    ตรวจเมื่อ: "2026-09-19",
    ค้างอยู่: "ขั้นในเส้นเดียวกันมีทั้งอ่านและเขียน ⇒ ด่าน method เดียวครอบไม่ตรง ต้องแยกตามขั้น",
  },
];

/** สรุปให้สารบัญ/จอ อ่าน */
export function ความปลอดภัยของเส้น() {
  return {
    เขียนแม้ไม่มีด่านMethod,
    "🔴 อ่านยังไง":
      "`routeMethod` ที่ท่อสร้างอัตโนมัติบอกได้แค่ว่าเส้นนั้น **มีด่าน method หรือไม่** · " +
      "🚫 **`unknown` ไม่ใช่ `readonly`** — ห้ามยิงสำรวจเส้น `unknown` โดยไม่อ่านซอร์สก่อน · " +
      "รายชื่อในไฟล์นี้คือเส้น `unknown` ที่ **ตรวจด้วยตาแล้วว่าเขียนของจริง** " +
      "(ยังไม่ครบทั้งกอง unknown — ตรวจได้เท่าที่ตรวจ ที่ไม่อยู่ในรายชื่อนี้ยังแปลว่า 'ยังไม่ตรวจ' ไม่ใช่ 'ปลอดภัย')",
  };
}
