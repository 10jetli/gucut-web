// สต็อกของที่อยู่ในทะเบียนใบอนุญาตเลื่อยโซ่ยนต์ — ฝั่งเซิร์ฟเวอร์
//
// 🔑 เจ้าของร้านสั่ง 25 ก.ย. 2569: "เลื่อยที่เหลือต้องไปเปิดขายที่เว็บ (ไม่ได้ใช้สต๊อค zort)"
//    + "บาร์ด้วย (ไม่ได้ใช้สต๊อค zort)"
//
// ของกลุ่มนี้นับเป็น **เลขซีเรียล** ในบัญชีรับ-จำหน่ายตามกฎหมาย ไม่ใช่จำนวนชิ้นธรรมดา
// วัดจริง 25 ก.ย. 2569:
//   • รหัสเลื่อย (`F 660` ฯลฯ) → ZORT ตอบ `found:false` = **ไม่มีในระบบ ZORT เลย**
//   • รหัสบาร์  (`01387` `02984`) → ZORT รู้จัก แต่เลขไม่ตรงทะเบียน (`02984` เคย **ติดลบ -3**)
// ⇒ ทั้งสองแบบเชื่อ ZORT ไม่ได้ ต้องใช้ทะเบียนเป็นตัวตัดสิน
//
// ⚠️ ไฟล์นี้ให้ **จำนวน** อย่างเดียว ไม่แตะ **ราคา** — ราคายังมาจาก ZORT เหมือนเดิมทุกประการ
//    (ZORT ให้ราคา NW 8800 = 18,000 ส่วนไฟล์สินค้าเขียน 20,000 · เรื่องราคาเป็นการตัดสินใจของเจ้าของร้าน
//     ไม่ใช่ผลพลอยได้ของการแก้สต็อก)
//
// ⚠️ อ่านไฟล์ JSON ชุดเดียวกับฝั่งหน้าเว็บ (`src/data/licensed-stock.json`) เพื่อกันเลขสองฝั่งเพี้ยนกัน
//    ถ้าแยกเป็นสองไฟล์ วันหนึ่งจะมีคนแก้ฝั่งเดียวแล้วการ์ดกับ /api/stock ตอบไม่ตรงกันเงียบ ๆ

import DATA from "../../src/data/licensed-stock.json" with { type: "json" };

/** รหัสสินค้า → จำนวนที่เหลือในทะเบียน (เลื่อย + บาร์ รวมกัน) */
export const LICENSED = Object.freeze({
  ...(DATA["เลื่อย"] || {}),
  ...(DATA["บาร์"] || {}),
});

/** วันที่นับของจากทะเบียนครั้งล่าสุด */
export const LICENSED_ASOF = DATA["_วัดเมื่อ"] || "";

/**
 * จำนวนที่เหลือตามทะเบียน · คืน `null` ถ้า **ไม่ใช่** ของในทะเบียน
 *
 * ⚠️ ต้องแยกสามสถานะให้ขาด — `null` (ไม่ใช่ของในทะเบียน ⇒ ใช้ ZORT ต่อ) ·
 *    `0` (อยู่ในทะเบียนแต่ขายหมดแล้ว ⇒ ห้ามถอยไปใช้ ZORT) · `>0` (มีของ)
 *    ยุบ `0` กับ `null` เป็นค่าเดียวเมื่อไหร่ ของที่หมดจริงจะเด้งกลับไปโชว์เลข ZORT ที่เชื่อไม่ได้
 */
export function licensedStock(sku) {
  if (!sku) return null;
  const n = LICENSED[String(sku).trim()];
  return typeof n === "number" && Number.isFinite(n) ? n : null;
}
