/* 📜 สัญญาของเส้น `list=` ทุกเส้น — **ไล่หน้าด้วยอะไร · ต้องมีพารามิเตอร์อะไรก่อน**
 *
 * 🔴 ที่มา 19 ก.ย. 2569 · ฝั่งจอเป็นคนชี้ และเหตุผลของเขาหนักกว่าที่ผมคิด
 *    วันเดียวเขาเกือบรายงานว่า "ท่อพัง" **4 ครั้ง** และ **3 ใน 4 เป็นเพราะท่อไม่ได้ประกาศว่ารับอะไร**
 *      ① ไล่หน้าด้วย `page` กับเส้นที่ใช้ `offset` ⇒ ได้แถวชุดเดิม ⇒ "เส้นนี้ไล่หน้าไม่ได้"
 *      ④ เรื่องเดียวกันอีกเส้น ⇒ ดึงทะเบียนได้ 200 จาก 360 ⇒ **ตัวเลขที่รายงานต่ำกว่าจริง**
 *      ③ ตัวกรองมีผลกับ `shown` ไม่ใช่ `total` ⇒ "ตัวกรองเป็นปุ่มหลอก"
 *    🔑 คำพูดของเขาที่ผมเห็นด้วยเต็มที่:
 *       **"ของที่ต้องพึ่งความระวังของคน จะพังอีกแน่ ๆ วันที่คนเปลี่ยน"**
 *    ⇒ แก้ที่ต้นทาง (ท่อประกาศเอง) ถูกกว่าแก้ที่นิสัยคนอ่าน
 *
 * 🔑 **ค่าทุกตัวในไฟล์นี้มาจากการยิงวัดจริง ไม่ใช่การอ่านซอร์ส** (19 ก.ย. 2569)
 *    วิธีวัด: ยิง `list=<ชื่อ>&limit=1` แล้วดูว่าท่อเติม `nextOffset` หรือ `nextPage` ให้
 *    ⇒ `ไล่หน้า: null` = **ยังไม่ได้วัด/วัดไม่ได้เพราะต้องมีพารามิเตอร์ก่อน**
 *      🚫 **ห้ามแปลว่า "ไล่หน้าไม่ได้"** — สามสถานะ ห้ามยุบเป็นสอง
 *
 * ⚠️ มีด่าน `list-contracts.test.mjs` บังคับว่า **ทุกชื่อใน `lists` ต้องมีแถวที่นี่**
 *    ⇒ เพิ่มเส้นใหม่แล้วไม่ประกาศ = ทดสอบตก (ไม่งั้นไฟล์นี้จะค้างทันทีที่มีเส้นใหม่)
 * ⚠️ `วัดเมื่อ` ต้องมีทุกแถว — คำว่า "วัดแล้ว" ที่ไม่มีวันที่ ใช้ตัดสินอะไรไม่ได้
 */

/** `ไล่หน้า`: "offset" · "page" · "ทั้งชุด" (คืนครบไม่ต้องไล่) · null (ยังไม่ได้วัด) */
export const LIST_CONTRACTS = {
  branches:        { ไล่หน้า: null,      ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ยิงแล้วไม่มีแถว ⇒ วัดการไล่หน้าไม่ได้" },
  bundleitems:     { ไล่หน้า: null,      ต้องมี: ["sku หรือ member"], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ไม่ส่ง sku ⇒ ได้สรุปรวม ไม่มีแถว" },
  bundles:         { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "🔴 เส้นที่ฝั่งจอเดาว่าใช้ page แล้วได้ของเดิม 5 รอบ ⇒ ได้ 200 จาก 360" },
  categories:      { ไล่หน้า: "ทั้งชุด", ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ประกาศ returnsAll เองอยู่แล้ว" },
  "channel-gaps":  { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "หนึ่งแถว = (รหัส × ช่องทาง) ⇒ กุญแจแถวต้องใช้สองช่อง" },
  contacts:        { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "" },
  deadstock:       { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "" },
  logistics:       { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "รับ from/to/carrier/only — ⚠️ `only` มีผลกับ `shown` ไม่ใช่ `total`" },
  "missing-sku":   { ไล่หน้า: null,      ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "รับ limit/offset แบบ opt-in และประกาศ `sliced` เอง" },
  moves:           { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ตอนวัดมีแถวน้อยกว่า limit ⇒ จบชุดแล้ว (ว่างเพราะเรายังไม่ตัดสต็อกเอง)" },
  orderfacets:     { ไล่หน้า: "ทั้งชุด", ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "คืนตัวนับตามกลุ่ม ไม่ใช่รายการแถว" },
  orders:          { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "กรองด้วย q จริง — ประกาศใน `applied`" },
  poscats:         { ไล่หน้า: null,      ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ยิงแล้วไม่มีแถว ⇒ วัดไม่ได้" },
  purchaseitems:   { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "" },
  purchases:       { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "" },
  quotations:      { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "🔴 เพิ่งเริ่มบอก limit วันนี้ ก่อนหน้านี้ไม่มีคีย์ไล่หน้าเลย · ไม่รองรับ q/type (ประกาศใน ignored)" },
  returnorders:    { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "🔴 เพิ่งเริ่มบอก limit วันนี้ · รับ from/to/days/status/source" },
  "returns-inbox": { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ตอนวัดแถวน้อยกว่า limit ⇒ จบชุดแล้ว" },
  sales:           { ไล่หน้า: null,      ต้องมี: ["ช่วงวัน"], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ยิงเปล่าได้ 0 แถว ⇒ ต้องส่งช่วงวันก่อนจึงวัดได้" },
  stock:           { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "กรองด้วย q จริง — ประกาศใน `applied`" },
  stockcard:       { ไล่หน้า: null,      ต้องมี: ["sku"], วัดเมื่อ: "2026-09-19", หมายเหตุ: "ไม่ส่ง sku ⇒ ไม่มีแถว" },
  topproducts:     { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "🔴 เคยโฆษณาว่าไล่หน้าได้ทั้งที่แถวซ้ำ — แก้แล้ว 19 ก.ย." },
  transfers:       { ไล่หน้า: "offset",  ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "" },
  warehouses:      { ไล่หน้า: "ทั้งชุด", ต้องมี: [], วัดเมื่อ: "2026-09-19", หมายเหตุ: "รายการคลังทั้งหมด (ไม่กี่แถว)" },
};

/* 📦 ตัวกรองที่แต่ละเส้นอ่านจริง — **สร้างจากซอร์สตอน build** (`scripts/gen-list-filters.mjs`)
   🔴 ฝั่งจอขอ 19 ก.ย. 2569: เขามีคำกล่าวอ้างเรื่องท่อ 186 จุด แต่ตรวจได้แค่ 4 จุด (2%)
      เพราะคำพวกนั้นเล่าเป็นภาษาคน ไม่ได้อ้างคีย์ที่เครื่องเทียบได้
      ⇒ ท่อประกาศคีย์ให้ ⇒ ด่านฝั่งเขาเทียบเองได้ทั้ง 186 จุด **โดยไม่ต้องยิงเลย**
   ⚠️ อ่านไม่ได้ ⇒ `null` ทุกแถว **ห้ามให้ทั้งไฟล์ล้ม** (สัญญาอื่นยังต้องส่งได้)
   🚫 `ตัวกรองที่รับ: null` = **ยังไม่รู้** ไม่ใช่ "ไม่รับอะไรเลย" */
/* 🔴 **ต้องเป็น static import ของโมดูล `.mjs`** (แก้ 19 ก.ย. 2569)
   ของเดิมอ่าน `.json` ด้วย `createRequire` ⇒ **ตัวรวมไฟล์ของ Netlify ไม่เอาไฟล์นั้นติดไปด้วย**
   ⇒ บน production ได้ `null` ทั้ง 24 เส้น **แบบเงียบ ๆ** ทั้งที่ deploy ขึ้นแล้วจริง
   ⇒ ฝั่งจอยิงตรวจเจอก่อนผม (ผมตรวจแค่ "คีย์มีอยู่" ซึ่งมีอยู่จริง — แต่ค่าเป็น null)
   🔑 **"ไฟล์ถูก commit แล้ว" ไม่เท่ากับ "ไฟล์ไปถึงที่รันจริง"** */
import { ตารางตัวกรอง } from "./list-filters.mjs";

/** สัญญาของเส้นเดียว + คำอธิบายวิธีอ่าน — คืน null เมื่อไม่รู้จักชื่อเส้น (ห้ามเดา) */
export function สัญญาของเส้น(ชื่อ) {
  const c = LIST_CONTRACTS[String(ชื่อ || "")];
  if (!c) return null;
  const f = ตารางตัวกรอง?.เส้น?.[String(ชื่อ)] ?? null;
  return {
    ...c,
    /* 🔴 **เปลี่ยนชื่อจาก `ตัวกรองที่รับ` เป็น `ชื่อที่โผล่ในบล็อก` (19 ก.ย. 2569)**
       ฝั่งจอยิงวัดแล้วพบว่าช่องเดิม **ผิดทั้งสองทิศ** ไม่ใช่แค่ทิศลบอย่างที่ผมกำชับเขาไว้:
         `list=quotations` ⇒ ช่องเดิมบอก `["limit","page","q","type"]`
         แต่ `supportedFilters` (ของจริง) คือ `["limit","page","store"]`
         ⇒ **ใส่ `q`/`type` ที่ท่อเมินจริง** (โค้ดอ่านเข้ามาเพื่อจะรายงานว่าเมิน)
         ⇒ **และตกหล่น `store` ที่กรองจริง**
       🔑 สาเหตุ: ตัวสกัดตอบคำถาม **"มีชื่อไหนโผล่ในโค้ด"** ไม่ใช่ **"ชื่อไหนกรองจริง"**
          ⇒ ผมตั้งชื่อช่องตามคำถามที่สอง ทั้งที่มันตอบคำถามแรก ⇒ **ชื่อผิดคือต้นเหตุของการอ่านผิด**
       🚫 **ใช้ตัดสินอะไรไม่ได้เลย ทั้งทิศบวกและทิศลบ** — ของที่ใช้ได้คือ `supportedFilters` + `ignored` */
    ชื่อที่โผล่ในบล็อก: f ? f.ตัวกรองที่รับ : null,
    "ชื่อที่โผล่ในบล็อก—อ่านยังไง": f
      ? "สกัดจากซอร์ส = **ชื่อที่โค้ดอ่านจากคำขอ** (รวมตัวที่อ่านเข้ามาเพื่อรายงานว่าเมิน) · " +
        "🚫 **ห้ามใช้ตัดสินว่าเส้นนี้กรองหรือไม่กรองตัวไหน** — ผิดทั้งสองทิศ (พิสูจน์แล้ว 19 ก.ย. 2569) · " +
        "✅ ของที่ใช้ได้: `supportedFilters` (กรองจริง) และ `ignored` (เมินอยู่) ในคำตอบของเส้นนั้นเอง"
      : "ยังไม่มีตาราง (สร้างตอน build) ⇒ ยังไม่รู้ · แต่ถึงมีก็ใช้ตัดสินไม่ได้ ดูคำอธิบายข้างบน",
    "🔑 อ่านยังไง":
      "`ไล่หน้า` บอกว่าต้องเดินหน้าด้วยพารามิเตอร์ตัวไหน — **อ่านค่าจาก `nextOffset`/`nextPage` " +
      "ในคำตอบเสมอ ห้ามบวก limit ที่ขอไปเอง** (ท่อบีบเพดานได้) · " +
      "🚫 `ไล่หน้า: null` = **ยังไม่ได้วัด/วัดไม่ได้เพราะต้องส่งพารามิเตอร์ก่อน** " +
      "ห้ามแปลว่า \"ไล่หน้าไม่ได้\" · " +
      "`ต้องมี` = ไม่ส่งตัวนี้แล้วจะได้คำตอบที่ไม่มีแถว ซึ่ง **ไม่ใช่ \"ไม่มีข้อมูล\"**",
  };
}
