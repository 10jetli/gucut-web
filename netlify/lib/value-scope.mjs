/* 📏 ค่าไหน "เป็นของทั้งชุด" · ค่าไหน "เป็นของคำขอนี้เท่านั้น" — ที่เดียว
 *
 * 🔴 ที่มา 19 ก.ย. 2569 · ฝั่งจอเป็นคนชี้ และเขาเกือบรายงานว่าโค้ดตัวเองพัง
 *    ท่อส่ง `truncated: true` ⇒ เขาหาบรรทัด "รายการถูกตัด" บนจอไม่เจอ
 *    ของจริง: หัวการ์ดยิง `limit=1` ⇒ `truncated` **จริงเสมอ** (ขอ 1 แถวจาก 91)
 *    ตอนกางยิง `limit=200` ⇒ ได้ครบ ⇒ `truncated:false` ⇒ ไม่ขึ้นเตือน = **ถูกต้องแล้ว**
 *    ถ้าเขาอ่านค่านี้จากก้อนหัว จอจะขึ้น "รายการถูกตัด" **ตลอดกาล ทั้งที่ไม่เคยถูกตัด**
 *
 * 🔑 คลาส: **ค่าที่เป็นจริงเฉพาะกับคำขอนั้น ถูกอ่านเป็นคุณสมบัติของข้อมูล**
 *    ⇒ ได้คำเตือนถาวรที่ไม่มีวันหาย ⇒ **คนเลิกเชื่อคำเตือนอื่นในจอเดียวกัน**
 *    (คลาสเดียวกับ `byStatus` ทั้งร้าน vs `byStatusFiltered` ที่แยกกันไปแล้ว 18 ก.ย.)
 *    ⚠️ ชื่อคีย์ไม่ได้บอกขอบเขตของตัวเอง และ**ไม่มีอะไรฟ้อง** เพราะค่าถูกต้องทุกครั้ง
 *       ที่ผิดคือ *คำถามที่ปลายทางเอาไปตอบ* ⇒ ด่านที่เทียบค่าจะเขียวตลอด
 *
 * ⚠️ **นี่คือคำประกาศ ไม่ใช่สวิตช์** — ไม่มีโค้ดไหนเปลี่ยนพฤติกรรมตามไฟล์นี้
 *    หน้าที่เดียวของมันคือให้ปลายทางรู้ว่า "ค่านี้ตอบคำถามไหน" ก่อนเอาไปแสดงบนจอ
 *    🚫 ห้ามเอาไปใช้ตัดสินใจ (ฟิลด์ที่เป็นคำอธิบาย ห้ามมีอำนาจตัดสินใจ)
 *
 * ⚠️ มีด่าน `value-scope.test.mjs` กวาดซอร์สหาชื่อตระกูลนี้มาเทียบ
 *    เพิ่มชื่อใหม่ (เช่น `xxxTruncated`) แล้วไม่ประกาศ = ทดสอบตก
 *    (ถ้าไม่มีด่าน ไฟล์นี้จะค้างทันทีที่มีคนตั้งชื่อใหม่ ซึ่งเกิดขึ้นบ่อยมากในตระกูลนี้)
 */

/** "คำขอนี้" = จริงเฉพาะกับ limit/offset/ตัวกรองที่ส่งไปรอบนั้น · "ทั้งชุด" = คุณสมบัติของข้อมูล */
export const ขอบเขตค่า = {
  // ── ของคำขอนี้เท่านั้น — 🔴 ห้ามเอาไปเขียนบนจอเป็นคุณสมบัติของข้อมูล ──
  truncated: { ขอบเขต: "คำขอนี้", คือ: "ยังมีข้อมูลเหลืออีกนอกเหนือจากที่ส่งกลับรอบนี้ — ขึ้นกับ limit ที่ขอ" },
  truncatedByTime: { ขอบเขต: "คำขอนี้", คือ: "หยุดเพราะหมดเวลาในรอบนั้น ไม่ใช่เพราะข้อมูลหมด" },
  diffTruncated: { ขอบเขต: "คำขอนี้", คือ: "รายการส่วนต่างถูกตัดให้สั้นลงเพื่อส่งกลับ" },
  unknownOnChannelTruncated: { ขอบเขต: "คำขอนี้", คือ: "รายชื่อรหัสที่ช่องทางไม่รู้จัก ถูกตัดให้สั้น" },
  listedNoStockTruncated: { ขอบเขต: "คำขอนี้", คือ: "รายชื่อของที่ลงขายแต่สต็อกหมด ถูกตัดให้สั้น" },
  hiddenTruncated: { ขอบเขต: "คำขอนี้", คือ: "รายชื่อของที่ถูกซ่อน ถูกตัดให้สั้น" },
  sliced: { ขอบเขต: "คำขอนี้", คือ: "ตัดแถวตาม limit/offset ที่ผู้เรียกขอ" },
  slicedNote: { ขอบเขต: "คำขอนี้", คือ: "คำอธิบายของ sliced" },
  pagingDone: { ขอบเขต: "คำขอนี้", คือ: "ได้แถวน้อยกว่าที่ให้ต่อหน้า ⇒ จบชุดแล้ว (เทียบกับ limit รอบนี้)" },
  pagingNote: { ขอบเขต: "คำขอนี้", คือ: "คำอธิบายของ pagingDone" },
  nextOffset: { ขอบเขต: "คำขอนี้", คือ: "จุดเริ่มหน้าถัดไป คิดจาก offset+จำนวนแถวที่ได้จริงรอบนี้" },
  nextPage: { ขอบเขต: "คำขอนี้", คือ: "หน้าถัดไป คิดจาก page ที่ส่งมารอบนี้" },
  limitClamped: { ขอบเขต: "คำขอนี้", คือ: "ขอ limit เกินเพดาน แล้วท่อบีบลง — **ไม่ได้แปลว่าข้อมูลไม่ครบ**" },
  returnedCount: { ขอบเขต: "คำขอนี้", คือ: "จำนวนแถวที่ส่งกลับรอบนี้" },
  rowsFetched: { ขอบเขต: "คำขอนี้", คือ: "จำนวนแถวที่ดึงมาได้ในรอบนั้น" },
  byStatusFiltered: { ขอบเขต: "คำขอนี้", คือ: "นับตามสถานะ **ภายใต้ตัวกรองที่ส่งมา** ⇒ ใช้เป็นตัวเลขบนแท็บ" },
  byStatusRows: { ขอบเขต: "คำขอนี้", คือ: "แถวที่ใช้นับ byStatus ของรอบนี้" },

  // ── ของทั้งชุด — เป็นคุณสมบัติของข้อมูล ไม่ขึ้นกับคำขอ ──
  byStatus: { ขอบเขต: "ทั้งชุด", คือ: "นับตามสถานะของทั้งร้าน ⇒ ใช้ตั้ง **ชื่อแท็บ** ไม่ใช่ตัวเลขบนแท็บ" },
  byStatusScope: { ขอบเขต: "ทั้งชุด", คือ: "คำประกาศว่า byStatus นับจากขอบเขตไหน" },
  declaredTotal: { ขอบเขต: "ทั้งชุด", คือ: "ยอดรวมที่ต้นทางประกาศเอง (ZORT/แพลตฟอร์ม) ⇒ ใช้เทียบว่าเราดึงครบไหม" },
};

/** ชื่อคีย์ที่ถูกจัดว่าอยู่ในตระกูลนี้ (ด่านใช้กวาดเทียบ) */
export const ชื่อที่ประกาศแล้ว = Object.keys(ขอบเขตค่า);

/** เอาไปแนบกับคำตอบ — คืนเฉพาะคีย์ที่มีอยู่จริงในคำตอบนั้น (ไม่ยัดคีย์ที่ไม่เกี่ยว) */
export function ขอบเขตของค่าที่ส่งไป(obj) {
  if (!obj || typeof obj !== "object") return null;
  const พบ = {};
  for (const k of ชื่อที่ประกาศแล้ว) if (k in obj) พบ[k] = ขอบเขตค่า[k].ขอบเขต;
  if (!Object.keys(พบ).length) return null;
  return {
    ...พบ,
    "🔑 อ่านยังไง":
      "\"คำขอนี้\" = จริงเฉพาะกับ limit/offset/ตัวกรองที่ส่งไปรอบนั้น — " +
      "🔴 **ห้ามเอาไปเขียนบนจอเป็นคุณสมบัติของข้อมูล** เช่นยิง limit=1 แล้ว truncated จริงเสมอ " +
      "ถ้าเอาไปขึ้นคำเตือน จอจะเตือนตลอดกาลทั้งที่ไม่เคยถูกตัด · " +
      "\"ทั้งชุด\" = คุณสมบัติของข้อมูล เอาไปแสดงได้ตรง ๆ",
  };
}
