# GUCUT Storefront

> ## 📌 เปิดช่องคุยใหม่? อ่านตรงนี้พอ
> ไฟล์นี้โหลดเข้าหัว Claude อัตโนมัติทุกครั้งที่เริ่มงานในโปรเจกต์นี้ —
> **เจ้าของร้านไม่ต้องรวมไฟล์หรือเล่าอะไรใหม่** พิมพ์งานที่อยากได้ต่อได้เลย
> อยากรู้ว่าเมื่อวานทำอะไรไปบ้าง สั่งว่า "ดู git log" ได้ (ข้อความ commit เขียนไทยละเอียดทุกครั้ง)
>
> **สถานะล่าสุด: 16 ส.ค. 2569**
> - เว็บ **ไม่เหลืออะไรผูกกับ Shopify แล้ว** (สินค้า · รูป · รีวิว · คลิป · บทความ · รูปปก)
> - หน้าร้านใช้งานได้ครบ: สั่งซื้อ → ออเดอร์เข้า ZORT + Telegram → ลูกค้าดูสถานะเองได้
> - หลังร้าน 8 เมนู: ออเดอร์ · แชท · เลือกคลิป · คอมเมนต์ · ผูกสินค้ากับคลิป · โค้ดส่วนลด · แต้มสะสม · สถานะระบบ
>
> **รอเจ้าของร้านทำ (Claude ทำแทนไม่ได้)**
> 1. กรอกข้อมูลร้านใน `src/lib/shop.ts` (ชื่อผู้ประกอบการ · เลขผู้เสียภาษี · ที่อยู่ · อีเมล) — หน้านโยบายรออยู่
> 2. ใส่ `NEXT_PUBLIC_PROMPTPAY_ID` ที่ Netlify — ไม่ใส่ = ลูกค้าจ่ายได้แค่เก็บเงินปลายทาง
> 3. export แต้มลูกค้าเก่าจากแอป CWILL Loyalty บน Shopify **ก่อนปิดร้าน** ไม่งั้นแต้มหายหมด
> 4. **ย้ายทะเบียนโดเมน gucut.com ออกจาก Shopify ก่อนปิดร้าน** — ปลดล็อก → ขอรหัส EPP →
>    ย้ายไป Cloudflare Registrar (~$10/ปี ถูกกว่า $16 ที่จ่าย Shopify และ DNS อยู่ที่นั่นแล้ว)
>    ไม่ย้าย = เสี่ยงเสียโดเมนอายุ 14 ปีพร้อมอันดับ Google ทั้งหมด
>
> **สิ่งที่เจ้าของร้านสั่งไว้ — ห้ามทำตรงข้าม**
> - deploy ได้เลยไม่ต้องถาม (แต่ต้องบอกถ้ามีอะไรให้ตัดสินใจ)
> - หน้าสถานะระบบ **ห้ามเช็คอัตโนมัติ** ต้องกดเอง (ประหยัดทรัพยากร)
> - บทความ **ห้ามใส่ลิงก์ในเมนู**
> - ฟีดวิดีโอ **ห้ามโชว์ว่าร้านมีคลิปทั้งหมดกี่ใบ**
> - ห้ามโหลดคลิปคนอื่นมาลงเว็บ (ละเมิดลิขสิทธิ์) — ใช้วิธีฝัง หรือขออนุญาตก่อน

เว็บขายเลื่อยยนต์ NEWWAVE / KingKong ของร้าน GUCUT — สไตล์ Shopee + TikTok Shop, mobile-first, ภาษาไทย, ราคาบาท

## โดเมน — อ่านก่อนแตะ robots.txt / sitemap
| โดเมน | สถานะ |
|---|---|
| **gucut.com** | ✅ **โดเมนเดียวของหน้าร้าน** — SSL ออกแล้ว · `robots.txt` เปิดให้ Google เก็บ |
| **admin.gucut.com** | ✅ หลังร้านตัวเก่า (GUCUT Dashboard · ZORT · Lazada/Shopee/TikTok) — คนละ Netlify project (`gucut-admin`) |
| ~~new78.com~~ | 🗑️ **เลิกใช้แล้วทุกที่ 18 ส.ค. 2569** — ถอดออกจาก Netlify ทั้ง new78.com · www.new78.com · admin.new78.com |

⚠️ **ร้าน Shopify ไม่ได้อยู่ที่ gucut.com อีกแล้ว** — เข้าจัดการที่ `admin.shopify.com/store/gucut1`
(export แต้ม CWILL ยังทำได้ปกติ)

DNS อยู่ที่ **Cloudflare** (nameserver `iris` / `lars.ns.cloudflare.com`) — แก้ที่นั่นที่เดียว
หน้า DNS ใน Shopify ยังโชว์ค่าเดิมแต่**ไม่มีผลแล้ว**

| record | ค่า |
|---|---|
| `gucut.com` | CNAME → `gucut-storefront.netlify.app` · DNS only |
| `www` | CNAME → `gucut-storefront.netlify.app` · DNS only |
| `video` | R2 bucket `gucut-video` · **Proxied (เมฆส้ม)** — ต้องส้มถึงจะแคชได้ |
| MX ×3 · SPF · DKIM | Cloudflare Email Routing (`info@` → `gucut1@gmail.com`) |
| `send.send` MX/SPF · `resend._domainkey.send` | Resend + Amazon SES (อีเมลส่งออก) |
| `account` | ยังชี้ Shopify — ปล่อยไว้ ไม่มีผล |

ที่อยู่เว็บอยู่ที่ `src/lib/site.ts` + `netlify/lib/site.mjs` อ่านจาก env `NEXT_PUBLIC_SITE_URL`
(ตั้งเป็น `https://gucut.com` ที่ Netlify project `gucut-storefront` แล้ว)

✅ **redirect URI ที่ LINE / Facebook / Google เพิ่มแล้ว (16 ส.ค. 2569)**
`https://gucut.com/api/oauth/{line,facebook,google}/callback` — ของ new78.com ยังอยู่ครบ
ใช้ได้พร้อมกันสองโดเมนเพราะ `oauth.mjs:25` สร้าง callback จาก `url.origin` ของคำขอ
ทดสอบแล้วทั้งสามเจ้าตอบ 302 ไปหาผู้ให้บริการพร้อม redirect_uri ที่ถูกต้อง

⚠️ **ย้ายโดเมนอีกครั้งเมื่อไหร่ ต้องกลับมาเพิ่ม URI ที่สามเจ้านี้ด้วยทุกครั้ง** — แก้ env อย่างเดียวไม่พอ

⚠️ **`www.gucut.com` ต้อง 301 ไป `gucut.com` เสมอ** (กติกาอยู่ใน `netlify.toml`)
ห้ามปล่อยให้สองชื่อเสิร์ฟเนื้อหาเหมือนกัน — เคยพลาดมาแล้ว 16 ส.ค. 2569 แล้วเจอสองปัญหาพร้อมกัน:
ปุ่มเข้าสู่ระบบพัง (`oauth.mjs:25` สร้าง `redirect_uri` จากโดเมนที่ลูกค้าเข้ามา
เข้าทาง www = ส่งชื่อที่ไม่ได้ลงทะเบียนไป → LINE ตอบ 400 · Facebook ตอบ "บล็อก URL แล้ว")
และ Google เห็นเป็นเนื้อหาซ้ำสองชื่อ

**robots.txt เหลือไฟล์เดียว** (`public/robots.txt`) — เสิร์ฟตรง ๆ ไม่ต้องมีกติกาใน `netlify.toml`
เมื่อก่อนต้องแยกสองไฟล์ตามโดเมน เพราะ new78.com เสิร์ฟเนื้อหาชุดเดียวกัน
เปิดทั้งคู่ = Google เห็นเป็นเนื้อหาซ้ำ แล้วอาจเลือกจัดอันดับเว็บซ้อมแทน
ถอด new78.com ออกแล้วจึงรวมเหลือไฟล์เดียว (18 ส.ค. 2569)

บทเรียนตอนย้าย (16 ส.ค. 2569) — เผื่อต้องทำอีก
- CNAME อยู่ร่วมกับ A/AAAA ชื่อเดียวกันไม่ได้ **ต้องลบ AAAA ก่อน** ถึงจะแปลง A เป็น CNAME ได้
- เพิ่มโดเมนใน Netlify ตอน DNS ยังชี้ที่อื่น → Netlify ไม่ขอใบ SSL ให้ และ**ไม่ลองใหม่เอง**
  ต้องกด **Renew certificate** ในหน้า Domain management แล้วยืนยันในกล่องอีกที (ออกใบใน ~45 วินาที)

## 🏪 ร้านต้นแบบ — clone ไปเปิดร้านใหม่ยังไง

เจ้าของร้านตั้งใจให้ gucut.com เป็น **ร้านต้นแบบ** (18 ส.ค. 2569) เอาไป clone เปิดร้านอื่นได้

**แก้ไฟล์เดียวได้เกือบทั้งหมด** → `src/lib/shop.ts` ตัวแปร `BRAND`
ชื่อร้าน · ชื่อเรียกอื่น · คำโปรย · คำอธิบายร้าน — ทุกที่ในหน้าร้านอ่านจากตรงนี้

⚠️ **ห้ามพิมพ์ชื่อร้านลงไฟล์อื่นอีก** ให้ `import { BRAND } from "@/lib/shop"` เสมอ
พิมพ์ตรง ๆ = ตอน clone ต้องไล่หาทีละไฟล์ แล้วตกหล่นแน่นอน
(ชื่อหน้าเว็บใช้ `titleSuffix("ชื่อหน้า")` จะได้ "ชื่อหน้า | ชื่อร้าน" อัตโนมัติ)

**ที่ยังต้องแก้แยกตอน clone** (ยังไม่ได้รวมเข้า BRAND)

| อะไร | อยู่ที่ |
|---|---|
| สีแบรนด์ | `tailwind.config.ts` |
| โลโก้ / ไอคอน | `public/icon-192.png` · `icon-512.png` |
| ชื่อแอปตอนติดตั้ง | `public/manifest.webmanifest` (ไฟล์นิ่ง แก้มือ) |
| ข้อมูลสินค้า · รูป · รีวิว · คลิป | `src/data/` · `public/img/` · `public/rv/` |
| รหัส ZORT · โดเมน · พิกเซล | Environment Variables ที่ Netlify |
| ข้อมูลนิติบุคคล | `src/lib/shop.ts` ส่วน `SHOP` |

⚠️ **บทเรียนตอนรวมศูนย์ (18 ส.ค. 2569)** — ใส่ `${BRAND.name}` ในเครื่องหมายคำพูดธรรมดา
จะกลายเป็นตัวหนังสือดิบบนหน้าเว็บ และ **`tsc` ผ่านฉลุยเพราะมันเป็นข้อความที่ถูกต้อง**
ต้องเป็น backtick เท่านั้น · ใน JSX ใช้ `{BRAND.name}`
**ตรวจด้วยการ grep หา `BRAND.name` ในโฟลเดอร์ `out/` หลัง build เสมอ** — เจอ = หลุด
(รอบนั้นหลุด 6 จุด และมี 1 จุดที่ regex ทำ JSX พังจนคลาส CSS เพี้ยนด้วย)

## Tech Stack
- Next.js 15 (App Router) + TypeScript + Tailwind CSS 3
- จานสี 4 สี: ส้มแดง `#FF3C00` · เข้ม `#333333` · เทาอ่อน `#ECECEC` · ขาว — ดู `tailwind.config.ts`
- ฟอนต์: ใช้ฟอนต์ระบบของเครื่องลูกค้า ไม่โหลดจากข้างนอก (ตัวหนังสือขึ้นตั้งแต่เฟรมแรก)
- PWA: `public/manifest.webmanifest` + `public/sw.js` + `public/icon-192.png`, `icon-512.png`
- Deploy: Netlify (`netlify.toml` พร้อมแล้ว)

## วิธีรัน
```bash
npm install
npm run dev      # เปิด http://localhost:3000
npm run build    # ตรวจ build ก่อน deploy
```

## โครงสร้าง
```
src/
├── app/
│   ├── layout.tsx            # โครงหลัก + Shell (เมนูล่าง ท้ายเว็บ ลงทะเบียน sw)
│   ├── page.tsx              # หน้าแรก: ค้นหา / แบนเนอร์ / Flash Sale / grid สินค้า
│   ├── products/[handle]/    # หน้าสินค้า (เวอร์ชันย่อ — ทำเต็มขั้นถัดไป)
│   ├── categories|videos|cart|account/  # หมวดหมู่ / วิดีโอ / ตะกร้า / บัญชี
│   └── offline/              # หน้าที่ขึ้นตอนเน็ตหลุด (service worker หยิบมาใช้)
├── components/               # SectionHead (หัวข้อประจำแบรนด์), SiteFooter, PwaSetup,
│                             # SearchBar, BannerSlider, FlashSale, ProductCard, BottomNav
└── lib/
    ├── catalog.ts            # ข้อมูลสินค้า 2,482 รายการ (อยู่ในโค้ด ไม่เรียก API ใคร)
    ├── local-images.ts       # สลับ URL รูปมาเป็นไฟล์ใน public/img/
    └── useLiveStock.ts       # ดึงสต็อก/ราคาสดจาก ZORT ผ่าน /api/stock
```

## ข้อมูลสินค้า — ไม่พึ่ง Shopify แล้ว
สินค้า รูป และรีวิว **เก็บไว้ในโปรเจกต์นี้ทั้งหมด** ปิดร้าน Shopify ได้โดยเว็บไม่กระทบ:
- สินค้า 2,482 รายการ → `src/lib/catalog.ts` + `src/data/`
- รูปสินค้า 5,595 ไฟล์ → `public/img/` · รูปรีวิว 3,117 ไฟล์ → `public/rv/`
- สต็อก/ราคาสด → ZORT ผ่าน `netlify/functions/stock.mjs`
- คลิปวิดีโอ 459 คลิป → `src/data/videos.json` (**ตัวไฟล์ .mp4 ยังอยู่บน Shopify CDN** ดูด้านล่าง)

สคริปต์ใน `scripts/` ที่มีชื่อ Shopify เป็นเครื่องมือ **ย้ายข้อมูลออกมาครั้งเดียว** ไม่ได้รันตอนเว็บทำงาน

## แสกนภาพหาสินค้า (ปุ่มกล้องในช่องค้นหา)
ลูกค้าถ่ายรูปอะไหล่ → เว็บหาสินค้าที่หน้าตาใกล้เคียง **ทำในเครื่องลูกค้าทั้งหมด**
รูปไม่เคยออกจากเครื่อง ไม่ต่อบริการ AI ข้างนอก ไม่มีค่าใช้จ่ายรายเดือน

| ชิ้นส่วน | ที่อยู่ | ขนาด |
|---|---|---|
| ตัวคิด (MobileNet v1 0.50) | `public/model/mobilenet/` | 5.3 MB |
| ลายนิ้วมือสินค้า 2,262 ใบ | `public/img-vectors.bin` | 1.2 MB |
| ตัวเทียบ | `src/lib/imgsearch.ts` | tfjs โหลดตอนกดปุ่มเท่านั้น |
| หน้าจอกล้อง | `src/components/ScanSheet.tsx` | |

**เพิ่ม/เปลี่ยนรูปสินค้าแล้วต้องรันใหม่** แล้ว commit ไฟล์ `.bin` ตามไปด้วย:
```bash
node scripts/gen-img-vectors.mjs     # ใช้เวลา ~4 นาที
```
ไฟล์ `.bin` เรียงตรงกับ `items` ใน `public/search-index.json` — ถ้าแก้ `gen-search-index.mjs`
ให้ลำดับเปลี่ยน **ต้องสร้าง `.bin` ใหม่ด้วย** ไม่งั้นผลค้นหาจะสลับตัวกันมั่ว

แม่นกับของที่หน้าตาเด่น (เครื่อง โซ่ในกล่อง บาร์) · ของที่เหมือนกันไปหมด (สกรู น็อต สปริง)
เดาผิดได้ จึงมีปุ่ม "ทักร้านให้ช่วยหา" ท้ายผลลัพธ์เสมอ

> `public/model/` กับ `img-vectors.bin` ต้องอยู่ในรายการแคชของ `sw.js` (แบบใช้ของเก่าก่อน)
> ไม่งั้นลูกค้าโหลดใหม่ 6.5MB ทุกครั้งที่กดกล้อง

> ✅ **ไม่เหลืออะไรผูกกับ Shopify แล้ว** (15 ส.ค. 2569) — เช็คจาก build จริง ไม่มีลิงก์ `cdn.shopify.com` สักหน้า
> รูปปกหน้าแรกเก็บเองแล้วที่ `public/img/cover-all.jpg` · คลิปอยู่บน R2 · บทความดึงมาครบแล้ว
> (`src/lib/videos.ts` ยังมีค่า `CDN` ของ Shopify ค้างไว้เป็นทางถอยกลับตอน `HOST` ว่างเท่านั้น)

## คลิปวิดีโอ — อยู่บน Cloudflare R2 แล้ว
ย้ายครบ **459 คลิป · 12.8 GB · 22,545 ไฟล์** เมื่อ 15 ส.ค. 2569 (2 ชม. 18 นาที ไม่พลาดสักใบ)
เก็บเป็น HLS 3 ความคมชัด (480/720/1080) เบราว์เซอร์เลือกเองตามเน็ตลูกค้า

| | |
|---|---|
| bucket | `gucut-video` (Asia-Pacific) · เปิด Public Development URL แล้ว |
| ที่อยู่ปัจจุบัน | `HOST` ใน `src/lib/videos.ts` = `https://video.gucut.com` (ทางถอยกลับ: ลิงก์ `pub-xxx.r2.dev`) |
| CORS | ตั้งแล้ว (`new78.com`, `gucut.com`, `localhost:3000` · GET/HEAD · header `range`) |
| ตัวย้าย | `scripts/video-to-r2.mjs` (รันซ้ำได้ จดใน `.r2-done.json`) · ตัวช่วยตั้งค่า `public/r2-setup.sh` |

> ✅ **เสิร์ฟผ่าน `video.gucut.com` แล้ว (16 ส.ค. 2569)** — ผูกโดเมนเข้ากับ bucket โดยตรง
> พร้อม Cache Rule ชื่อ `cache-video-gucut` (Edge 30 วัน · Browser 7 วัน · ไม่สนใจ cache-control จากต้นทาง)
> **ลบกฎนี้เมื่อไหร่ คลิปกลับไปไม่แคชทันที** เพราะ R2 ไม่ส่ง `cache-control` มาเอง
>
> วัดจริงแล้ว: ความเร็วดิบจากเน็ตในไทย **ไม่ต่างจาก r2.dev** (R2 อยู่ APAC ใกล้อยู่แล้ว)
> ที่ได้จริงคือ **เบราว์เซอร์แคชได้** (`max-age=604800` ส่วน r2.dev ไม่ส่งอะไรเลย) ·
> แคชที่ขอบเครือข่าย (`cf-cache-status: HIT`) · และไม่โดน r2.dev หรี่ความเร็วตอนคนดูพร้อมกันเยอะ
> `public/video-fast.sh` (แผนเดิมที่จะใช้ Worker) จึง **ไม่ต้องรันแล้ว** — ใส่ตัวกันรันซ้ำไว้แล้ว

**บทเรียนเรื่องตัวเล่น HLS — อย่าแก้กลับ**
- ⚠️ **`ref` ของ `<video>` ต้องเป็นฟังก์ชันตัวเดิมทุกรอบ** (`useCallback`)
  เขียน `ref={(node)=>{...}}` อินไลน์ = สร้างฟังก์ชันใหม่ทุก render
  React ถอด ref (เรียกด้วย `null`) แล้วใส่ใหม่ทุกครั้ง → `setEl` → re-render → วนกันเอง
  คลิปถูก reset รัว ๆ (`emptied`) และ `play()` ถูกยกเลิกกลางทางตลอด (`AbortError`)
  **อาการ: คลิปค้างไม่เล่นบนมือถือ แต่หน้า HTML ธรรมดาเล่นได้ปกติ**
  ยิ่งวนหนักเพราะฟีดมี state เปลี่ยนบ่อย (`ready` จาก waiting/canplay · `muted` ตอนถอยไปเล่นเงียบ)
  เจอของจริง 17 ส.ค. 2569 — ไล่จับด้วยตัวฟ้องบนจอ (`?debug`) ถึงเห็น `emptied` รัวกับ AbortError
- ⚠️ **ห้ามใส่ `src` ของ `<video>` ใน JSX ถ้าค่าขึ้นกับการตรวจเบราว์เซอร์** (เช่น `isSafariHls()`)
  หน้าถูก build เป็น HTML ล่วงหน้า ตอน build ไม่มีเบราว์เซอร์ ค่าจึงเป็น false เสมอ
  แล้ว **React ไม่แก้ attribute ที่ไม่ตรงกันตอน hydrate** → iPhone ได้ `<video>` ไร้ src ตลอดกาล
  อาการ: โปสเตอร์ขึ้น วงหมุนค้าง แตะไม่ติด **เฉพาะบน iPhone** (คอมปกติเพราะ hls.js ป้อนผ่าน JS)
  ทางแก้: ป้อน src ด้วย JS ใน effect ทั้งสองทาง — เจอของจริง 16 ส.ค. 2569 หลังย้ายมา gucut.com
- `canPlayType("application/vnd.apple.mpegurl")` **โกหก** — Chrome บนคอมตอบ "maybe" ทั้งที่เล่นไม่ได้
  ต้องเช็ค user agent ประกอบใน `isSafariHls()` (ห้ามกัน CriOS/FxiOS/EdgiOS — เป็น WebKit บน iPhone)
- ฟีดต้องต่อ hls.js **เฉพาะใบที่ดูอยู่กับใบถัดไป** (`useHls(el, src, eager)`) ไม่งั้นหลายใบดูดเน็ตพร้อมกัน
  จนใบที่ลูกค้าดูอยู่ค้าง (`preload="metadata"` ใช้กับ HLS ไม่ได้ hls.js ไม่สนใจค่านั้น)
- ⚠️ **กติกา `eager` ต้องใช้กับ "ทั้งสองทาง" ไม่ใช่แค่ฝั่ง hls.js**
  ฝั่ง Safari ใส่ `src` ตรง ๆ ก็ต้องจำกัดด้วย `eager` เหมือนกัน — เคยพลาดมาแล้ว 16 ส.ค. 2569
  ใส่ `src` ให้ทุกใบในโหมดวิดีโอ (4 ใบ) = iPhone มีตัวเล่น HLS 4 ตัวแย่งเน็ตกันเอง
  อาการ: ขึ้นเฟรมแรกแล้ว**ค้างที่วงหมุน** บนคอมไม่เป็นเพราะทางนั้นกันไว้ตั้งแต่แรก
  และพอเลิกเป็นใบที่ดูอยู่ ต้อง `pause()` + `removeAttribute("src")` + `load()` ด้วย
  ถอด src เฉย ๆ ไม่พอ เบราว์เซอร์ยังดึงข้อมูลที่ค้างท่ออยู่ต่อ
- ถอด `<video>` ออกจากหน้าแล้ว **เบราว์เซอร์ไม่หยุดเล่นให้เอง** ต้อง `pause()` + ล้าง `src` เองตอน unmount
- เทสในแท็บที่ถูกบัง (`visibilityState=hidden`) จะดูเหมือนคลิปพัง เพราะ Chrome ไม่เปิด MediaSource ให้

**ตามหาคลิปเพิ่ม — ค้นมาหมดแล้ว อย่าเสียเวลาค้นซ้ำ**
เจ้าของร้านบอกว่าแอป Vizup มีคลิปแนวตั้งราว 500 ใบ แต่ในมือเรามี 347 ใบ ที่หายไปอยู่ในแอป Vizup เอง
ค้นมาแล้วทุกทาง ไม่เจอที่อื่นอีก:
- Shopify Files (bulk ทั้ง 7,260 ไฟล์) → วิดีโอ 562 ใบ · ยาว ≥5 วิ 459 ใบ · แนวตั้ง 347 ใบ
- media ที่ติดกับสินค้า 113 รายการ → เป็นชุดเดียวกับใน Files ทั้งหมด ไม่มีใบใหม่
- GenericFile 396 ไฟล์ → Vizup อัปแต่ **รูป** .avif 330 ใบ ไม่มีวิดีโอ
- shop metafield `vizup.videos` → เป็นแค่รายชื่อ hash ที่ mirror มาจาก Shopify Files (ชุดเดิม)
- Cloudflare Worker `gucut-pwa` → โค้ดที่ deploy อยู่ **ไม่มี route `/vizup-api/` แล้ว** (เหลือแต่ auth + pwa-api)
  ถึงแม้ `snippets/vizup-popup.liquid` ในธีมเก่าจะยังอ้างถึงอยู่
- metaobject / metafield ของสินค้า / app embed ในธีมหลัก → ไม่มีข้อมูลผูกคลิป-สินค้าของ Vizup

จะได้คลิปที่เหลือต้องให้ Vizup ดันเข้า Shopify Files ก่อน แล้วรัน `scripts/gen-videos.mjs` ใหม่

## สถานะ Phase 1 — ครบแล้ว ✅
- [x] หน้าแรก feed สไตล์ Shopee (แบนเนอร์ / Flash Sale countdown / grid สินค้า)
- [x] หน้าสินค้าเต็ม: รูปสไลด์ / variant / ปุ่มวิดีโอ / สเปกตาราง / ปุ่มซื้อติดล่างจอ
      คลิปลอยมุมจอแบบ Shopee (`ProductVideoFloat`) — ขึ้นเฉพาะ **90 สินค้า** ที่ผูกคลิปไว้ใน Shopify
      อยากให้ขึ้นมากกว่านี้ต้องผูกคลิปที่เหลือ (368 ใบ) กับสินค้าก่อน
- [x] ตะกร้า localStorage + badge ที่ bottom nav
- [x] เช็คเอาต์แบบ Shopee หน้าเดียวจบ → POST `/api/orders`
      ที่อยู่ (พับได้) / สินค้า **ปรับจำนวน+ลบได้ในหน้านี้** / หมายเหตุ / **ใบกำกับภาษีแบบเต็มรูป** /
      ตัวเลือกการจัดส่ง / ช่องทางชำระเงิน / สรุปยอด + แถบเช็คเอาต์ติดล่างจอ
      - **เก็บเงินปลายทาง (COD)** — 🚫 **ปิดอยู่ตอนนี้** (เจ้าของร้านสั่ง 16 ส.ค. 2569)
        ปุ่มยังโชว์แต่เป็นสีเทา กดไม่ได้ · เปิดใหม่ด้วย `NEXT_PUBLIC_COD=1` ที่ Netlify
        ไม่ตั้ง = ปิด (ตั้งใจ กันเปิดโดยไม่ตั้งใจ) · **เซิร์ฟเวอร์เช็ค env ตัวเดียวกันด้วย**
        ปิดแค่หน้าเว็บไม่พอ ยิง POST ตรงมาก็สั่งแบบ COD ได้
      - **QR พร้อมเพย์** (gen QR เองใน `lib/promptpay.ts`) → แนบสลิป → ยืนยัน
      **ค่าส่งเป็นขั้นบันไดตามยอดค่าสินค้า 12 ขั้น (70–400 บาท)** — ตารางอยู่ที่
      `src/lib/shipping.ts` (หน้าเว็บ) กับ `netlify/lib/shipping.mjs` (เซิร์ฟเวอร์) **ต้องตรงกันเสมอ**
      คัดมาจากค่าจริงใน Shopify (`deliveryProfile/120738840868`) เมื่อ 16 ส.ค. 2569 — ไม่ได้ดึงสด
      ร้านแก้ค่าส่งใน Shopify เมื่อไหร่ ต้องมาแก้สองไฟล์นี้ตาม
      คิดจากยอดหลังหักส่วนลดโค้ด (ไม่นับแต้ม) ให้ตรงเงื่อนไข TOTAL_PRICE ของ Shopify
      **เซิร์ฟเวอร์คิดค่าส่งใหม่เองเสมอ ไม่เชื่อ `body.shipping`** (เดิมเชื่อ — แก้แล้ว 16 ส.ค. 2569)
      ตัวเลขอื่นยังอยู่หัวไฟล์ `CheckoutView.tsx`: `COD_FEE` `SHIP_MIN_DAYS` `SHIP_MAX_DAYS` `SHIP_NAME` `CARRIER`
      ⚠️ **ร้านไม่ได้ส่งฟรี** — ห้ามเขียนคำว่า "ส่งฟรี" ที่ไหนอีก (เคยเขียนผิดไว้ 9 จุด แก้หมดแล้ว)
      **โค้ดส่วนลด** — ตรวจที่ `netlify/functions/coupon.mjs` เท่านั้น รายชื่อโค้ดอยู่ใน
      ตัวแปรลับ `COUPON_CODES` (JSON · ดูตัวอย่างใน `.env.example`) กันเดาโค้ด 20 ครั้ง/IP/10 นาที
      ⚠️ ห้ามย้ายไปตรวจในเบราว์เซอร์เด็ดขาด ลูกค้ากดดูซอร์สก็เห็นโค้ดทั้งหมด
- [x] หน้าวิดีโอ feed แนวตั้งแบบ TikTok — **368 คลิป** (คัดจาก 459)
      **ตัดคลิปภาพนิ่งออก 91 ใบ** (รูปอะไหล่พื้นขาว + การ์ตูนอธิบาย คนเลื่อนผ่านทันที)
      คัดด้วย `scripts/gen-motion.mjs` — วัดความสว่างเฉลี่ยด้วย ffmpeg เกิน 164 = ภาพสตูดิโอ
      (ลองวัด "ความเคลื่อนไหว" ก่อนแล้วใช้ไม่ได้ คลิปคนเลื่อยไม้ตั้งกล้องนิ่งได้คะแนนต่ำเหมือนกัน)
      รายชื่อ `src/data/still-clips.json` กรองที่ `src/lib/feed.ts` — คลิปพวกนี้ยังโชว์ในหน้าสินค้าปกติ
      ⚠️ 90 ใบที่ตัดออกคือคลิปที่ผูกสินค้าไว้เกือบทั้งหมด **ฟีดจึงเหลือคลิปกดซื้อได้ใบเดียว**
      อยากได้ปุ่มซื้อในฟีดกลับมาต้องผูกสินค้าเข้ากับคลิปหน้างานเอง (ยังไม่มีเครื่องมือ)
      ปุ่มขวาแบบ TikTok: ตะกร้า · หัวใจ · คอมเมนต์ · บันทึก · แชร์ (`VideoActions`)
      **สุ่มทุกครั้ง ไม่ใช่เรียงตามความนิยม** — เจ้าของร้านสั่งว่า "อย่าให้เรียงคลิป"
      ใช้การสุ่มแบบถ่วงน้ำหนัก (Efraimidis–Spirakis): `Math.random() ** (1 / น้ำหนัก)`
      ทุกคลิปมีโอกาสขึ้นใบแรก แต่ใบที่คนชอบมีโอกาสสูงกว่า · เปิดสิบครั้งได้สิบลำดับ
      น้ำหนัก = log2(ยอดวิว×0.5 + หัวใจ + คอมเมนต์×3 + 2) **มีเพดานที่ 3**
      ⚠️ เพดานสำคัญมาก — กัน "ยิ่งดังยิ่งได้ขึ้น" (คลิปที่ขึ้นก่อนได้วิวเพิ่ม แล้วยิ่งได้ขึ้นก่อนอีก
      วนจนคลิปใหม่ไม่มีวันได้โอกาส) จำลองแล้ว: คลิปที่ยังไม่มีใครดูเลยได้ขึ้นใบแรก ~21% ของรอบ
      ใบที่เคยดูแล้วไปท้ายเสมอ · ป้ายมุมซ้ายโชว์แค่ความยาว **ห้ามโชว์ว่ามีกี่คลิป**

      **ยอดวิวเก็บที่ `netlify/lib/views.mjs`** (store `gucut-social` คีย์ `w/<คลิป>/<ผู้ชม>`)
      ⚠️ ห้ามเก็บแบบอ่านก้อนเดียวมาบวกแล้วเขียนกลับเหมือน `counts` — คนดูพร้อมกันจะเขียนทับกันจนยอดหาย
      ใช้ "หนึ่งคน = หนึ่งคีย์" แล้วนับคีย์ · คนเดิมดูซ้ำนับครั้งเดียว (เป็น "คนดู" ไม่ใช่ "ครั้ง")
      กันคนกดรีเฟรชปั่นยอดตัวเองไปในตัว · นับเมื่อดูค้างนานพอเท่านั้น ไม่ใช่ตอนเลื่อนผ่าน

      **ชิงโหลดล่วงหน้าปรับตามเน็ตลูกค้า** (`prefetchDepth()` ใน `src/lib/videos.ts`)
      4G ขึ้นไป 5 ใบ · 3G 2 ใบ · 2G/โหมดประหยัดเน็ต 1 ใบ · ไล่ทีละใบห่างครึ่งวินาที
      ⚠️ เอาแค่ "ช่วงต้น" (~320KB/ใบ) ไม่ใช่โหลดเต็มทั้งคลิป — TikTok/Reels ก็ทำแบบนี้
      โหลดเต็มหลายสิบใบ = กินเน็ตลูกค้าเป็น GB และแย่งเน็ตจนใบที่ดูอยู่สะดุด
- [x] หมวดหมู่ + ตัวกรองราคา

## ระบบออเดอร์ — เก็บเอง ไม่พึ่งใคร
ลูกค้ากดสั่ง → `netlify/functions/orders.mjs` (`/api/orders`) เก็บที่ Netlify Blobs
(store `gucut-orders` · ตัวออเดอร์ `o/<id>` แยกจากสลิป `slip/<id>` ให้หน้ารายการโหลดเร็ว)
แล้วเด้งแจ้งเตือนเข้า **กลุ่ม Telegram เดิมของร้าน** (ใช้ `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`
ชุดเดียวกับแชท ไม่ต้องตั้งอะไรเพิ่ม) + push เข้ามือถือแอดมิน
- เลขเงินคิดใหม่ฝั่งเซิร์ฟเวอร์ ไม่เชื่อยอดจากเบราว์เซอร์ · จ่ายแบบ QR ต้องมีสลิปถึงรับ
- กันสแปม 10 ออเดอร์/IP/10 นาที · สถานะ: new → confirmed → shipped → done / cancelled
- หน้าจัดการอยู่ `/admin/orders/` (ดูเต็มใบ กดโทร เปิดสลิป เปลี่ยนสถานะ)
- **ส่งเข้า ZORT อัตโนมัติ** (แบบเดียวกับตอนขายผ่าน Shopify) — `zortAddOrder()` ยิง
  `POST /v4/Order/AddOrder` ด้วยรหัสชุดเดียวกับ `/api/stock` (`ZORT_STORENAME/APIKEY/APISECRET`)
  ZORT จับคู่สินค้าด้วย **SKU** แล้วตัดสต็อกเอง (ตะกร้าเก็บ SKU ของตัวเลือกที่หยิบตั้งแต่ `VariantSheet`)
  พังไม่ล้มออเดอร์ — ติดธง `order.zort` โชว์ในหน้าออเดอร์ + Telegram พร้อมปุ่ม "ส่งซ้ำ"
  จ่าย QR = ส่งยอดชำระเต็ม + `paymentmethod`/`paymentdate` · COD = ยอดชำระ 0 + `isCOD:true`
  (ป้าย COD ในหน้ารายการขาย) · ยกเลิกบนเว็บ**ไม่**ยกเลิกใน ZORT ต้องไปยกเลิกเอง
  ชื่อที่ไปโชว์ในคอลัมน์ "ช่องทาง" กับ "บริการขนส่ง" อยู่หัวฟังก์ชัน `zortAddOrder`
  (`SALES_CHANNEL` / `SHIPPING_CHANNEL` / `PAYMENT_METHOD` · ทับด้วย env ได้)
  ⚠️ ช่องทางการขายต้อง**มีชื่อนั้นอยู่ใน ZORT อยู่แล้ว** (ตั้งค่า → ช่องทางการขาย)
  สะกดไม่ตรง = ZORT ไม่รู้จัก แล้วปล่อยคอลัมน์ว่างเหมือนเดิม โลโก้ประจำช่องทาง
  มีเฉพาะช่องทางที่ ZORT ต่อตรงเอง (Shopee/Lazada/TikTok/Shopify) ช่องทางที่สร้างเองไม่มีโลโก้
- `ORDER_FORWARD_URL` (ไม่บังคับ) ส่งต่อออเดอร์ไป Make.com/ที่อื่นอีกทาง — ยิงจากเซิร์ฟเวอร์
- หน้า "การซื้อของฉัน" (`OrdersView` → `/api/orders?mine=1`) ต่อแล้ว — จับคู่เบอร์โทรของ
  บัญชีที่ล็อกอินกับเบอร์ผู้รับในออเดอร์ ลูกค้าเห็นสถานะเองได้ ไม่ส่งสลิปกลับไป

## หัวใจ / คอมเมนต์ ใต้คลิป
`netlify/functions/social.mjs` (`/api/social`) เก็บที่ Netlify Blobs (store `gucut-social`)
- ยอดทุกคลิปเก็บเป็น**ก้อนเดียว** (`counts`) ฟีดขอทีเดียวได้ครบ ไม่ต้องยิงทีละใบ · แคชที่ edge 60 วิ
- "ฉันกดไปแล้ว" กับ "บันทึกไว้" เก็บในเครื่องลูกค้า ไม่ต้องล็อกอินก็กดได้ (เป็นความนิยมโดยประมาณ)
- กันสแปม: หัวใจ 120/IP/10 นาที · คอมเมนต์ 5/IP/10 นาที · ห้ามใส่ลิงก์ · คอมเมนต์ใหม่เด้ง Telegram
- ร้านลบคอมเมนต์ได้ที่ `/admin/comments/`

## บทความ — ดึงจากบล็อก Shopify มาเก็บเองแล้ว
**73 บทความ** → `src/data/articles.json` (174KB) · รูป 17 ไฟล์ → `public/art/`
ดึงด้วย `scripts/gen-articles.mjs` (อ่านจากหน้าร้านสาธารณะ ไม่ต้องใช้รหัส API · รันซ้ำได้ ของเดิมไม่หาย)
หน้าเว็บ `/articles/` กับ `/articles/<handle>/` มี JSON-LD + og:image + อยู่ใน sitemap

> ⚠️ **ไม่มีลิงก์ในเมนูใด ๆ ตามที่เจ้าของร้านสั่ง** — เข้าถึงผ่าน sitemap กับลิงก์ระหว่างบทความเท่านั้น
> ⚠️ **ชื่อไฟล์ภาษาไทยต้องนับเป็นไบต์ ไม่ใช่ตัวอักษร** — Linux ที่ Netlify ใช้ build จำกัด 255 ไบต์
> ไทยตัวละ 3 ไบต์ · macOS นับเป็นตัวอักษรจึง build ผ่านในเครื่องแต่พังบนเซิร์ฟเวอร์ (เคยล่มมาแล้ว)
> ⚠️ URL เปลี่ยนจากของเดิม (`/blogs/news/xxx` → `/articles/xxx`) ยังไม่ได้ทำ redirect

## สิ่งที่เจ้าของร้านต้องใส่ก่อนเปิดใช้จริง
1. `NEXT_PUBLIC_PROMPTPAY_ID` — เบอร์ PromptPay รับเงิน
2. ตัวเลข "ขายได้ X ชิ้น" → `src/data/sold.json` รูปแบบ `{ "<handle สินค้า>": 22300 }`
   ไฟล์ว่าง `{}` = การ์ดไม่โชว์บรรทัดนี้ (ห้ามใส่ตัวเลขมั่ว) · ใส่บางตัวก็ได้ ตัวที่ไม่ใส่ก็ไม่โชว์
   ตัวเลขต้องเป็นยอดรวมทุกช่องทาง (Shopee/Lazada/TikTok/หน้าร้าน) — ตัวเลขจาก Shopify อย่างเดียวต่ำเกินจริงมาก
3. ราคาก่อนลดจริงใน `src/lib/catalog.ts`

## Phase 2
> **ตัดสินใจแล้ว: เลิกใช้ Shopify** — ห้ามเสนอให้ต่อ Storefront API หรือย้ายกลับไป Shopify
> เว็บนี้ยืนด้วยตัวเองครบแล้ว (สินค้า/รูป/รีวิวอยู่ในโปรเจกต์ · สต็อกจาก ZORT · สมาชิกเก็บที่ Netlify Blobs · เก็บเงินด้วย QR PromptPay)

1. **เข้าสู่ระบบด้วย LINE / Facebook / Google — เขียนเสร็จแล้ว**
   ทางเดินกลางอยู่ที่ `netlify/lib/oauth.mjs` · แต่ละเจ้าเป็นไฟล์บาง ๆ ใน `netlify/functions/oauth-*.mjs`
   รอแค่ใส่คีย์ที่ Netlify แล้วเปิดทีละเจ้าด้วย env (ดู `.env.example`)
   ลูกค้าใหม่กดปุ่ม → กรอกเบอร์ครั้งเดียวที่ `/account/link/` → ครั้งต่อไปกดปุ่มเดียวเข้าเลย
   บัญชียังผูกกับ **เบอร์โทร** เป็นหลักเหมือนเดิม ออร์เดอร์เก่าจึงตามมาครบ
   หนึ่งเบอร์ผูกได้หลายเจ้าพร้อมกัน (เก็บใน `u.social`)

   **เพิ่มเจ้าใหม่**: เขียน object ใน `lib/oauth.mjs` แบบเดียวกับ `LINE`/`FACEBOOK`/`GOOGLE`
   (ต้องมี `authorizeUrl` กับ `getProfile`) แล้วสร้างไฟล์ `oauth-<เจ้า>.mjs` 3 บรรทัด
   เรื่อง state / cookie / กัน open redirect ตัวกลางจัดการให้หมดแล้ว
2. รีเซ็ตรหัสผ่านเองได้ (ตอนนี้ต้องทักแชทให้ร้านตั้งให้)
3. ~~หน้า นโยบายความเป็นส่วนตัว / เงื่อนไขการใช้บริการ~~ **เสร็จแล้ว**
   `/policy/privacy/` · `/policy/terms/` เขียนจากสิ่งที่โค้ดทำจริง ลิงก์อยู่ท้ายเว็บทุกหน้า
   ⚠️ ยังต้องกรอก `src/lib/shop.ts` (ชื่อผู้ประกอบการ เลขผู้เสียภาษี ที่อยู่ อีเมล เบอร์)
   ช่องไหนว่างหน้าเว็บไม่โชว์บรรทัดนั้น ห้ามใส่ข้อมูลมั่ว · และต้องยืนยันเงื่อนไขคืนสินค้า
   (`RETURN_DAYS = 7` = ขั้นต่ำตามกฎหมายขายตรงฯ ไม่ใช่เลขที่ตั้งเอง)

## ระบบหลังร้าน
อยู่ที่ **gucut.com/admin/**

> 🚫 **`admin.gucut.com` ไม่ใช่ของโปรเจกต์นี้** — เป็น Netlify project คนละตัว (`gucut-admin`)
> build จาก repo **`github.com/10jetli/gucut-next`** = หลังร้าน "ตัวเก่า" (GUCUT Dashboard)
> ที่ต่อ ZORT · ดึงออเดอร์จาก Lazada/Shopee/TikTok · สั่งของกับโรงงาน · โอนสินค้า · ดึงบิล
> เจ้าของร้านใช้ทำงานจริงทุกวัน **ห้ามย้ายโดเมนนั้นมาที่โปรเจกต์นี้**
>
> **สองระบบรวมกันแล้ว (18 ส.ค. 2569)** — หลังร้านตัวเก่ามีเมนู
> **เว็บไซต์ → ระบบหลังร้านเว็บ** (`/site`) ที่ฝังหน้านี้ไว้เป็นกรอบเต็มจอ
> คนใช้จึงเห็นเป็นระบบเดียวโดยไม่ต้องย้ายโดเมน
>
> ⚠️ ฝังเป็นกรอบเพราะทั้งสองระบบเป็น Next.js และใช้เส้นทาง `/_next/` ชนกัน
> ดึงหน้ามาเสิร์ฟข้ามโดเมนแบบ 200 ไม่ได้ จะพังทั้งสองฝั่ง
> ⚠️ ห้ามใส่ `X-Frame-Options` หรือ CSP `frame-ancestors` ให้ `/admin/` ไม่งั้นกรอบจะว่าง

เมนูที่มีตอนนี้: **ออเดอร์** · **แชทลูกค้า** · **เลือกคลิป** · **คอมเมนต์ใต้คลิป** ·
**ผูกสินค้ากับคลิป** · **โค้ดส่วนลด** · **แต้มสะสม** · **ตรวจสุขภาพ SEO** ·
**พิกเซลการตลาด** · **คนเข้าเว็บ** · **สถานะระบบ**

### คนเข้าเว็บ — `/admin/live/`
นับที่เซิร์ฟเวอร์เอง **ตัวบล็อกโฆษณาบล็อกไม่ได้** จึงได้เลขจริงกว่า GA4
(GA4 มองไม่เห็นคนที่ใช้ตัวบล็อก หรือ Safari/iOS ที่ตัดคุกกี้ — ซึ่งคือลูกค้าส่วนใหญ่ของร้าน)
โชว์: ออนไลน์ตอนนี้ · กำลังดูหน้าไหน · **มาจากประเทศไหน** · ผู้เข้าชม 7 วันล่าสุด
**ไม่นับตัวเองตอนเข้าหลังร้าน**

ประเทศได้มาจาก `context.geo` ของ Netlify — ไม่ต้องพึ่งบริการภายนอกและ**ไม่ต้องเก็บ IP ลูกค้า**
ชื่อประเทศภาษาไทยใช้ `Intl.DisplayNames` เบราว์เซอร์แปลให้เอง ธงใช้ regional indicator
ไม่ต้องมีตารางชื่อประเทศหรือรูปธงในโค้ดเลยสักไบต์

เก็บที่ store `gucut-live` — **ห้ามเปลี่ยนไปใช้วิธีอ่านมาแก้แล้วเขียนกลับ**
คนเข้าพร้อมกันหลายคนจะเขียนทับกันจนนับหาย จึงใช้ "หนึ่งคน = หนึ่งคีย์" แล้วนับจำนวนคีย์เอา
  `l/<นาที>/<รหัสผู้ชม>` ใครออนไลน์  ·  `v/<วันที่>/<รหัสผู้ชม>` ใครเข้าวันไหน
ของเก่าถูกเก็บกวาดตอนเปิดหน้าหลังร้าน ไม่ต้องตั้งงานตามเวลาเพิ่ม

ไม่ใช้คุกกี้ — รหัสผู้ชมเป็นเลขสุ่มใน `sessionStorage` ปิดแท็บก็หาย ผูกกับตัวตนจริงไม่ได้
หน้านี้มีสวิตช์ให้เปิดรีเฟรชอัตโนมัติทุก 15 วิ แต่**ค่าเริ่มต้นคือกดเอง** ตามกฎที่เจ้าของร้านสั่งไว้

### ตรวจสุขภาพ SEO GEO AEO — `/admin/seo/`
**เลิกจ่ายแอปรายเดือนบน Shopify สองตัวแล้ว** เอาความสามารถมาทำเองที่นี่ (18 ส.ค. 2569)
| แอปเดิม | ค่าใช้จ่าย | ย้ายมาเป็น |
|---|---|---|
| SearchPie SEO & Speed | $39/เดือน | แท็บ **ค้นหา** + **ความเร็ว** |
| Vizby AI | $29/เดือน | แท็บ **ผู้ช่วย AI** + `agents.md` + ตัวจับบอต AI |

รายการงานคิดที่ `src/lib/audit.ts` **ตอน build จากข้อมูลจริงในโปรเจกต์**
ไม่ยิงเครื่องมือข้างนอก จึงไม่มีค่าใช้จ่ายรายเดือนและ**ไม่มีปุ่มสแกน** — อัปเดตทุกครั้งที่ deploy

⚠️ **เพดานคะแนน** — ด้านที่ยังมีงานค้างขึ้นไม่ถึง 100 · มีงาน "ควรแก้ก่อน" ขึ้นไม่ถึง 90
ตัวหารเป็นจำนวนของทั้งหมด (พันกว่าชิ้น) ปัญหาไม่กี่ชิ้นจึงถูกกลบจนคะแนนขึ้น 100 ทั้งที่มีปัญหา
**ห้ามถอดเพดานนี้ออก** ไม่งั้นจะได้หน้าที่เขียนว่า 100 คะแนน แต่ข้างล่างลิสต์ปัญหาไว้สองข้อ

### ฟีดสินค้า `/products.json` — สต็อกสดจาก ZORT (ไม่ใช่ไฟล์นิ่งแล้ว)
เดิมเป็น route แบบ `force-static` สร้างจากสต็อกที่แช่อยู่ใน `src/data/products.json`
(ไฟล์นั้นแก้ล่าสุด 15 ส.ค. 2569 และไม่มีอะไรอัปเดตให้เอง)
แปลว่าเราบอก ChatGPT/Gemini ว่าของเหลือเท่าไหร่ด้วยตัวเลขเก่าหลายวัน — **ของเข้าใหม่ AI ไม่มีวันรู้**

19 ส.ค. 2569 เปลี่ยนเป็น **Netlify function** `netlify/functions/products-feed.mjs` ประกบสองส่วน
| ส่วน | มาจาก | อัปเดตเมื่อ |
|---|---|---|
| ชื่อ · ลิงก์ · รูป · ยี่ห้อ · รีวิว | `public/feed-base.json` (`gen-feed-base.mjs`) | ตอน deploy |
| **สต็อก · ราคา** | ZORT ผ่าน `netlify/lib/zort-stock.mjs` | **ทุก 30 นาที อัตโนมัติ** |

วัดตอนเปิดใช้: ต่างจากตัวเลขที่แช่ไว้ **151 รายการ** · ของที่เคยหมดแล้วกลับมามีของ **26 รายการ**

⚠️ **ต้องไม่มี `src/app/products.json/route.ts` อยู่** — ไฟล์นิ่งชนะฟังก์ชันเสมอ ลบทิ้งแล้ว
⚠️ **กวาดคลังต้องดึงหลายหน้าพร้อมกัน** (6 หน้า × 200) Netlify ให้ฟังก์ชันรอผลได้แค่ 26 วินาที
⚠️ **ตัวสำรองสามชั้น** ZORT สด → ของเก่าที่เคยกวาด (บอกผ่าน `stockLive:false`) → สต็อกที่แช่ไว้ตอน build
⚠️ **`llms-full.txt` ห้ามเขียนจำนวนคงเหลือ** — ไฟล์นั้นสร้างตอน build ตัวเลขจะแช่ทันที
   ให้บอก AI ว่า "ก่อนบอกลูกค้าว่ามีของ ให้เช็ค /products.json ก่อนเสมอ"

**ไฟล์สำหรับผู้ช่วย AI — สร้างใหม่ทุกครั้งที่ build ทั้งหมด**
| ไฟล์ | ตัวสร้าง | คืออะไร |
|---|---|---|
| `llms.txt` | เขียนมือ | สารบัญย่อ — **ต้องมีลิงก์แบบมาร์กดาวน์** ไม่งั้น PageSpeed หมวด Agent ตก |
| `llms-full.txt` | `gen-llms-full.mjs` | เนื้อหาทั้งร้านในไฟล์เดียว (1.3MB ดิบ · ส่งจริง 149KB) |
| `agents.md` | `gen-agents-md.mjs` | กติกาสำหรับตัวแทนซื้อของอัตโนมัติ |

⚠️ **คำถามที่พบบ่อยอยู่ที่ `src/data/faq.json` ที่เดียว** — หน้า `/faq/` กับ `llms-full.txt` ใช้ชุดเดียวกัน
ข้อ "จ่ายเงินยังไง" มีสองคำตอบ สลับตาม `NEXT_PUBLIC_COD` จริง (`src/lib/payment.ts`)
เคยเขียนตายตัวว่า "เก็บเงินปลายทางได้" ทั้งที่ปิดอยู่ — ผิดทั้งกับลูกค้าและกับ FAQPage schema ที่ส่งให้ Google

### ตัวคุมบอต AI — หลังร้าน → ตรวจสุขภาพ SEO → แท็บผู้ช่วย AI
เลือกได้ว่าจะให้ AI เจ้าไหนเก็บข้อมูลเว็บเราบ้าง (19 ตัว) — แทนแอป Avada AEO Optimizer
กติกาเก็บที่ `netlify/lib/botrules.mjs` (store `gucut-coupon` คีย์ `botrules`)
**บังคับจริงที่ edge function** (`ai-bots.js` คืน 403) ไม่ใช่แค่เขียนใน robots.txt ซึ่งเป็นแค่คำขอ

⚠️ **สามข้อห้ามของระบบนี้**
1. **ปิดได้เฉพาะบอต `kind === "ai"`** — Googlebot / Bingbot / บอตโซเชียล ไม่มีให้เลือกในหน้าจอเลย
   ปิด Googlebot = เว็บหายจาก Google · ปิดบอตโซเชียล = แชร์ลิงก์ไม่ขึ้นรูป
2. **อ่านกฎไม่ได้ = ไม่ปิดใคร** ระบบต้องพังไปทางปล่อยผ่านเสมอ
3. **จำกฎไว้ 5 นาที** (`RULES_TTL`) ไม่งั้นบอตทุกคำขอต้องรออ่าน Blobs ที่ us-east-1 (~0.5 วิ)
   ผลคือ**เปลี่ยนค่าแล้วมีผลภายใน 5 นาที** ไม่ใช่ทันที — ทดสอบจริงแล้วใช้เวลาราว 4 นาที

**`public/agents.md`** — สร้างใหม่ทุกครั้งที่ build ด้วย `scripts/gen-agents-md.mjs`
เป็นไฟล์บอกทางให้ "ตัวแทนซื้อของอัตโนมัติ" ของ ChatGPT/Gemini (robots.txt ของยุค AI)
อ่านชื่อร้านจาก `BRAND` ใน `shop.ts` **ห้ามพิมพ์ชื่อร้านลงในสคริปต์นั้น** (กติการ้านต้นแบบ)

**ตัวจับบอต AI** — `netlify/edge-functions/ai-bots.js` → `/api/ai-bots` → แท็บผู้ช่วย AI
บอกได้ว่า GPTBot / ClaudeBot / Google-Extended / PerplexityBot มาอ่านเว็บเราหรือยัง กี่หน้า
และ **Googlebot กลับมาเก็บอีกครั้งไหมหลังปลด noindex** · รู้จักบอต 28 ตัว
ต้องเป็น edge function เพราะเว็บเป็นไฟล์นิ่งและบอตไม่รัน JS ตัวนับคนเข้าเว็บจึงมองไม่เห็นเลย

⚠️ **สามข้อห้ามของไฟล์ edge function นี้ — ผิดข้อไหนก็เจ็บ**
1. **ห้าม `await` ตัวเขียนข้อมูล** วัดจริงแล้วบอตช้าจาก 223ms เป็น **1,400ms** (Blobs อยู่ us-east-1)
   Google ลดจำนวนหน้าที่ไล่เก็บเมื่อเซิร์ฟเวอร์ตอบช้า = ทำร้าย SEO ด้วยเครื่องมือที่สร้างมาช่วย SEO
   ใช้ `context.waitUntil()` ถ้ามี ไม่มีก็ปล่อยลอย — ยอมข้อมูลหายบ้างดีกว่าถ่วงบอตทุกตัว
2. **ห้าม `import` โมดูลไว้หัวไฟล์** ต้อง `await import()` ข้างใน `try` เสมอ
   ถ้าโหลดโมดูลไม่ได้ ความผิดพลาดจะเกิด**ก่อน**ถึง try แล้ว**ทั้งเว็บขึ้น 500** ทุกหน้า
3. **ตะแกรงหยาบ `MAYBE_BOT` ต้องกว้างกว่ารายชื่อจริงใน `lib/aibots.mjs` เสมอ**
   ชื่อที่ไม่มีคำว่า bot ต้องเขียนเพิ่มเอง — เคยลืม `Google-Extended` แล้วมันหลุดไปทั้งตัว

เก็บที่ store `gucut-live` คีย์ `p/<วันที่>/<บอต>/<หน้า>` — **หนึ่งหน้า = หนึ่งคีย์ แล้วนับคีย์**
ห้ามเปลี่ยนไปอ่านมาบวกแล้วเขียนกลับ (กติกาเดียวกับตัวนับคนเข้าเว็บ)
ตัวเลขจึงเป็น "จำนวนหน้าที่บอตเก็บไปในวันนั้น" ไม่ใช่ "จำนวนครั้ง" — บอตวนอ่านหน้าเดิมไม่ทำให้เลขบวม

⚠️ **ลิงก์ในบทความถูกล้างตอนโหลด** (`tidyLinks` ใน `src/lib/articles.ts`)
ของเดิม 89 ลิงก์เขียนเต็มเป็น `https://www.gucut.com/...` ซึ่งโดน 301 ทุกครั้ง (redirect chain)
ล้างที่ตอนโหลดแทนการแก้ `articles.json` เพราะถ้ารัน `gen-articles.mjs` ใหม่ไฟล์จะถูกเขียนทับ

### พิกเซลการตลาด — `/admin/marketing/`
กรอกรหัสพิกเซลในหลังร้านได้เลย **ไม่ต้อง deploy ใหม่** เก็บที่ store `gucut-coupon` คีย์ `marketing`
รองรับ **Meta · TikTok · GA4 · Google Ads · LINE Tag** — ปิดสวิตช์ไว้ = ไม่โหลดสคริปต์ของเจ้านั้นเลย
ยิง 4 เหตุการณ์ให้อัตโนมัติ: `ViewContent` · `AddToCart` · `InitiateCheckout` · `Purchase`

**ยิงสองทางเหมือนที่แอป Omega ทำตอนอยู่ Shopify** — จากเบราว์เซอร์ + จากเซิร์ฟเวอร์ (CAPI)
เพราะพิกเซลฝั่งเบราว์เซอร์โดนสกัดเยอะ (ตัวบล็อกโฆษณา · iOS ตัดคุกกี้ · ปิดหน้าก่อนสคริปต์จบ)
⚠️ **ทั้งสองทางต้องใช้ `event id` เดียวกัน = เลขออเดอร์** ไม่งั้นยอดขายถูกนับสองเท่า
⚠️ **token ของ CAPI ห้ามหลุดออกหน้าเว็บ** — `publicView()` ใน `netlify/lib/marketing.mjs` เป็นด่านกัน
   `/api/marketing` ที่หน้าร้านเรียกส่งกลับแค่รหัสพิกเซล (ซึ่งฝังในหน้าเว็บอยู่แล้ว) ไม่มี token
⚠️ **เบอร์/อีเมลต้องแฮช SHA-256 ก่อนส่งเสมอ** และทำเป็นรูปแบบสากลก่อน (`081...` → `6681...`)
   ไม่งั้นแฮชไม่ตรงกับที่ปลายทางมี = จับคู่ลูกค้าไม่ได้ ยอดโฆษณาก็ไม่แม่น

**หน้านโยบายความเป็นส่วนตัวเปลี่ยนข้อความตามอัตโนมัติ** (`TrackingNotice.tsx`)
เปิดพิกเซลแล้วข้อความจะเปลี่ยนจาก "ไม่ได้ติดตั้ง" เป็นบอกชื่อเครื่องมือที่ใช้จริง
**ห้ามเขียนข้อความนี้ตายตัว** — ประกาศว่าไม่เก็บทั้งที่เก็บ = ผิด PDPA และเป็นข้อที่
Meta/Google ตรวจก่อนอนุมัติบัญชีโฆษณา

`/admin/status/` เช็คสุขภาพระบบ **20 เรื่อง** แบบ**ยิงของจริงทุกตัว** ไม่ใช่ข้อความตายตัว — เขียนไฟล์จริงแล้วอ่านกลับ ·
ยิง ZORT · ยิง Telegram getMe · ขอไฟล์คลิปจาก R2 · เช็คไฟล์ตัวแสกนภาพ · นับเครื่องที่เปิดรับ push
**5 สถานะ**: ปกติ / ช้าเกิน 2.5 วิ / **ควรมาดู** / ใช้ไม่ได้ / ยังไม่เปิดใช้ (= ยังไม่ได้ตั้งค่า ไม่ใช่ของเสีย)

⚠️ **"ควรมาดู" (`warn`) มีไว้สำหรับของที่ทำงานอยู่แต่ไม่ปกติ** — ฟีดใช้สต็อกเก่า · มีบอตถูกสั่งปิด ·
ลูกค้าจ่ายเงินไม่ได้ ถ้าไม่มีสถานะนี้ ของพวกนั้นจะขึ้นเขียวเหมือนทุกอย่างเรียบร้อย ซึ่งหลอกตา
**เพิ่มตัวตรวจใหม่แล้วอย่าลืมว่า `warn` ต้องมีทั้งฝั่งเซิร์ฟเวอร์และในหน้าจอ** ไม่งั้นมันจะกลายเป็นเขียวเงียบ ๆ

⚠️ **บทเรียน 19 ส.ค. 2569 — ตัวตรวจที่ "เขียวได้ทั้งที่ของจริงพัง" อันตรายกว่าไม่มีตัวตรวจ**
ตัวตรวจคลิปเช็ค `pub-xxx.r2.dev/v/...` มาตลอด ซึ่งเป็นที่อยู่เก่าสองชั้น
(ย้ายมา `video.gucut.com` แล้ว และเปลี่ยน `/v/` เป็น `/v2/`) ไฟล์เก่ายังอยู่เป็นทางถอยกลับ
มันจึงขึ้นเขียวเสมอ ต่อให้ของจริงล่ม · และช่องพร้อมเพย์เขียนตายตัวว่า "จ่ายได้แค่ปลายทาง"
ทั้งที่ COD ปิดไปแล้ว = หน้าที่มีไว้บอกความจริงกลับโกหกเสียเอง
**เขียนตัวตรวจต้องเช็คที่อยู่ที่ลูกค้าใช้จริง และข้อความต้องคิดจากค่าจริง ห้ามเขียนตายตัว**

### กันหน้าขาว — `src/app/error.tsx` · `src/app/admin/error.tsx`
Next.js เจอ error ระหว่างวาดหน้าจะโยนทั้งหน้าทิ้ง ขึ้นข้อความอังกฤษ
*"Application error: a client-side exception has occurred"* ลูกค้าอ่านไม่รู้เรื่อง
สองไฟล์นี้ดักไว้ให้เห็นหน้าที่กดต่อได้ · ฝั่งหลังร้านโชว์สาเหตุจริงด้วย (คนอ่านคือเจ้าของร้าน)

⚠️ **ห้ามสร้าง `Intl.DisplayNames` / `Intl.*` ไว้ระดับไฟล์** — เบราว์เซอร์บางรุ่นสร้างแล้ว throw
โมดูลจะพังตั้งแต่โหลด ลากทั้งหน้าตายไปด้วย ให้สร้างตอนเรียกใช้แล้วครอบ try/catch
(เคยพลาดมาแล้ว 17 ส.ค. 2569 — หน้าคนเข้าเว็บพังบ้างไม่พังบ้างบน Safari)

⚠️ **ข้อมูลจาก API ต้องกันรูปแบบไม่ครบเสมอ** — `Array.isArray()` ก่อน `.map`/`.reduce`
ตอน deploy ใหม่ หน้าเว็บเก่ากับ API ใหม่ (หรือกลับกัน) จะอยู่ด้วยกันชั่วครู่เสมอ
และ `decodeURIComponent` โยน error ได้ถ้าเจอ `%` ที่ไม่ใช่รหัส — ต้องครอบ try/catch

**ด่านตรวจรหัสอยู่ที่ `netlify/lib/admin-gate.mjs` ที่เดียว** — API หลังร้านทุกตัวต้องเรียกผ่านตัวนี้
ห้ามเทียบ `CHAT_ADMIN_KEY` เองในไฟล์ฟังก์ชัน ไม่งั้นจะไม่มีตัวกันเดารหัสรัว ๆ
- ใส่รหัสผิดเกิน 5 ครั้ง (นับแยกตาม IP) พักไว้ 15 นาที
- เทียบรหัสแบบใช้เวลาเท่ากันเสมอ (`timingSafeEqual`)
- เครื่องลูกค้าจำรหัสไว้ 30 วันแล้วต้องใส่ใหม่ (`src/lib/admin.ts`)
- ⚠️ ตั้ง `CHAT_ADMIN_KEY` เป็นตัวอักษรอังกฤษ/ตัวเลขเท่านั้น — header ส่งภาษาไทยไม่ได้

## ทำเป็นแอปในอนาคต — ออกแบบเผื่อไว้แล้ว
เว็บนี้เป็น **PWA** ตั้งแต่ต้น ลูกค้ากด "เพิ่มลงหน้าจอโฮม" ได้เลย ไม่ต้องผ่านสโตร์

ของที่พร้อมแล้ว
- `public/manifest.webmanifest` — ชื่อ ไอคอน สีแถบ ทางลัด (หมวดหมู่ / ตะกร้า / คำสั่งซื้อ)
- `public/sw.js` — แคชไฟล์ให้เปิดได้ตอนเน็ตหลุด + รับ push
- `src/components/PwaSetup.tsx` — ลงทะเบียน service worker ให้ลูกค้าทุกคน
- `src/app/offline/page.tsx` — หน้าที่ขึ้นแทนจอขาวตอนไม่มีเน็ต
- ระยะหลบขอบจอ (`env(safe-area-inset-bottom)`) ที่เมนูล่างและแถบซื้อ

**กฎการแคชใน sw.js — อย่าแก้มั่ว**
- `/api/*` ห้ามแคชเด็ดขาด (ล็อกอิน สต็อก ราคา ต้องสด)
- หน้าเว็บ = ขอเน็ตก่อน ไม่ได้ค่อยใช้ของเก่า → deploy แล้วลูกค้าเห็นทันที
- ไฟล์ใน `/_next/static/`, `/img/`, `/rv/` = ใช้ของเก่าก่อน (ชื่อมี hash อยู่แล้ว)
- ขึ้นเวอร์ชันที่ตัวแปร `VERSION` เมื่อแก้กติกาแคช ของเก่าจะถูกลบให้เอง

ถ้าวันหนึ่งอยากขึ้น App Store / Play Store
- **Play Store**: ห่อด้วย TWA (Bubblewrap) ใช้เว็บเดิมทั้งหมด ไม่ต้องเขียนใหม่
- **App Store**: ห่อด้วย Capacitor — ต้องแก้เรื่องเดียวคือ **session cookie**
  ตอนนี้ใช้ cookie แบบ HttpOnly (`gu_sess`) ซึ่งดีที่สุดสำหรับเว็บ
  ถ้าทำแอปเนทีฟจริง ให้เพิ่มทางเลือกรับ token ผ่าน header ที่ `netlify/lib/session.mjs`
  ไม่ต้องรื้อของเดิม เพิ่มเป็นทางที่สองได้เลย
- API แยกจากหน้าเว็บอยู่แล้ว (`/api/auth`, `/api/stock`, `/api/oauth/*`) แอปเรียกใช้ชุดเดียวกันได้

## Deploy ขึ้น Netlify (step-by-step)
1. push โค้ดขึ้น GitHub (`git init && git add -A && git commit -m "init"` → สร้าง repo แล้ว push)
2. เข้า app.netlify.com → **Add new site → Import an existing project** → เลือก repo
3. Netlify อ่าน `netlify.toml` ให้เอง (build command `npm run build`) → กด **Deploy**
4. ตั้ง env variables ที่ Site settings → Environment variables (ค่าจาก `.env.example`)
5. ผูกโดเมน gucut.com ที่ Domain management
