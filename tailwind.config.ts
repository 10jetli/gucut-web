import type { Config } from "tailwindcss";

// จานสีประจำแบรนด์ GUCUT — ใช้ 4 สีนี้เท่านั้น ห้ามเพิ่มสีนอกจาน
//   #C42D00 ส้มแดง  = สีนำ ใช้กับสิ่งที่ต้องการให้กด/ให้เห็น
//   #333333 ดำเทา   = พื้นเข้ม (หัวเว็บ ท้ายเว็บ)
//   #ECECEC เทาอ่อน = พื้นหน้าเว็บ
//   #FFFFFF ขาว     = พื้นการ์ด
const config: Config = {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        // ใช้ชื่อเดิม (steel/safety) เพื่อให้ทุก component เปลี่ยนสีตามอัตโนมัติ
        steel: {
          900: "#ececec", // พื้นหลังหน้า — สีแบรนด์
          800: "#ffffff", // พื้นการ์ด/แถบ = ขาว
          700: "#dcdcdc", // เส้นขอบอ่อน (เข้มขึ้นให้เห็นบนพื้น #efefef)
          600: "#c7c7c7", // เส้นขอบเข้มขึ้น
          300: "#6b6b6b", // ตัวหนังสือรอง — ผ่านเกณฑ์อ่านง่ายบนพื้นเทา
        },
        safety: {
          DEFAULT: "#c42d00", // ส้มแดงประจำแบรนด์
                              // ⚠️ ห้ามกลับไปใช้ #ff3c00 (ของเดิมถึง 19 ส.ค. 2569)
                              //    สีนั้นบนพื้นขาวได้คอนทราสต์แค่ 3.56 ซึ่งต่ำกว่าเกณฑ์ 4.5
                              //    ทำให้ราคาสินค้า ลิงก์ และปุ่ม รวม 29 จุดไม่ผ่านการตรวจ
                              //    และคนสายตาไม่ดี (ลูกค้าจำนวนมากของร้านเครื่องมือช่าง) อ่านราคาไม่ชัด
                              //    ต้องเทียบกับ "พื้นเทาอ่อน #ececec ของตัวหน้าเว็บ" ด้วย ไม่ใช่แค่พื้นขาวของการ์ด
                              //    #d63200 ผ่านบนขาว (4.87) แต่ตกบนเทา (4.12) — เคยพลาดตรงนี้มาแล้ว
                              //    #c42d00 ผ่านทั้งคู่: บนขาว 5.64 · บนเทา 4.78
                              // ⚠️ บนพื้นเข้ม (bg-carbon / bg-ink) ห้ามใช้สีนี้ ต้องใช้ safety-light
          dark: "#b32900",    // ตอนกด/ชี้ — เข้มกว่าสีนำหนึ่งขั้น
          light: "#ff7a52",   // ใช้บนพื้นเข้มเท่านั้น (โลโก้บนแถบดำ) — ได้ 4.91 บน #333333
                              // สีนำตัวเข้มอ่านไม่ออกบนพื้นดำ (2.60) จึงต้องมีตัวนี้คู่กัน
          tint: "#fff2ee",    // พื้นอ่อนมาก ใช้รองแถบรีวิว/ตัวกรอง
        },
        // พื้นเข้มประจำแบรนด์ — หัวเว็บ ท้ายเว็บ
        carbon: {
          DEFAULT: "#333333",
          dark: "#242424",   // เข้มกว่าไว้ไล่เฉด
        },
        ink: {
          DEFAULT: "#1a1a1a", // ดำประจำแบรนด์ — ตัวหนังสือหลัก
          700: "#333333",     // ตัวหนังสือรอง
          500: "#6b6b6b",     // ตัวหนังสือจาง
          300: "#6f6f6f",     // ตัวหนังสือรอง — เข้มพอให้ผ่านเกณฑ์คอนทราสต์ 4.5:1 บนพื้นขาว
                             // ⚠️ ห้ามทำให้จางกว่านี้ ของเดิม #8a8a8a ได้แค่ 3.45 (ตก)
                             // และเจ้าของร้านเคยบอกเองว่าอ่านไม่ชัด
        },
      },
      // ฟอนต์ระบบของเครื่องลูกค้า — แนวเดียวกับ shopee.co.th ที่ไม่โหลดฟอนต์ไทยเลย
      // iOS/Mac → Sukhumvit Set · Windows → Leelawadee UI · Android → Noto Sans Thai
      // ไม่ต้องดาวน์โหลดอะไร ตัวหนังสือขึ้นตั้งแต่เฟรมแรก
      fontFamily: {
        heading: [
          "-apple-system", "BlinkMacSystemFont", "Sukhumvit Set",
          "Helvetica Neue", "Leelawadee UI", "Noto Sans Thai",
          "Segoe UI", "Roboto", "Helvetica", "Arial", "sans-serif",
        ],
        body: [
          "-apple-system", "BlinkMacSystemFont", "Sukhumvit Set",
          "Helvetica Neue", "Leelawadee UI", "Noto Sans Thai",
          "Segoe UI", "Roboto", "Helvetica", "Arial", "sans-serif",
        ],
      },
    },
  },
  plugins: [],
};
export default config;
