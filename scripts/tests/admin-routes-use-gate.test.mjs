/* ด่าน: ฟังก์ชันฝั่งหลังร้านทุกตัว **ต้องผ่าน `adminGate`** — ห้ามเทียบรหัสเอง
 *
 * 🔴 ที่มา 19 ก.ย. 2569 · ฝั่งจอเสนอ "ด่านพฤติกรรม: ยิงด้วยรหัสผิดต้องได้ 401"
 *    ⇒ ผมพยายามทำแบบนั้นฝั่งท่อ แล้วเจอข้อจำกัดที่ต้องบันทึกไว้:
 *    🔴 **`adminGate` ล็อก IP 15 นาทีเมื่อใส่รหัสผิดครบ 5 ครั้ง**
 *       ⇒ ยิงกวาดทุกเส้นด้วยรหัสผิด = **ล็อกเครื่อง g1 ออกจาก API หลังร้านทั้งหมด**
 *         (ป้ายเครดิตบนจอ · กระดานงาน · ตัววัดทุกตัว ใช้เส้นเดียวกันหมด)
 *    🔑 **กลไกป้องกันทำให้การตรวจสอบตัวมันเองแบบอัตโนมัติเป็นไปไม่ได้จากไอพีเดียวกัน**
 *       ⇒ นี่ไม่ใช่ข้อบกพร่องของ `adminGate` — มันทำงานถูก · แต่เป็นข้อจำกัดที่ต้องรู้
 *
 * ⇒ จึงแบ่งเป็นสองชั้นตามที่ทำได้จริง:
 *    ① **ชั้นนี้ (อ่านซอร์ส · ครอบทุกเส้น)**: ทุกไฟล์ที่เป็นเส้นหลังร้านต้องเรียก `adminGate`
 *       ⚠️ ชั้นนี้คือ "ร่องรอย" ไม่ใช่ "พฤติกรรม" ⇒ ตามบันไดของฝั่งจอ มันคือชั้นกลาง ๆ
 *    ② **ชั้นพฤติกรรม (ยิงจริง · ครอบได้ทีละ ≤4 เส้น)**: ทำมือตอนต้องการ **ห้ามใส่ในรอบอัตโนมัติ**
 *       `curl -H "x-admin-key: ผิด" .../api/core?...` ⇒ ต้องได้ 401 · ทำได้ไม่เกิน 4 เส้นต่อ 15 นาที
 *
 * 🔑 และห้ามเทียบรหัสเองในไฟล์ฟังก์ชัน (กติกาเดิมของโปรเจกต์) เพราะจะไม่มีตัวกันเดารหัสรัว ๆ
 */
import { test } from "node:test";
import assert from "node:assert/strict";
import { readdirSync, readFileSync } from "node:fs";
import { ซอร์สไม่เอาคอมเมนต์ } from "./_src.mjs";

/* 🔴 **รายการนี้ต้องมีอำนาจจริง — รอบแรกผมเขียนแล้วมันไม่มีผลต่อการตัดสินเลย**
   รอบแรกด่านดูแค่ "ไฟล์ที่อ่าน `CHAT_ADMIN_KEY` เองโดยไม่ผ่าน adminGate"
   ⇒ **ลบไฟล์ออกจากรายการยกเว้นแล้วด่านยังเขียว** (พิสูจน์ด้วย plant-bug)
   ⇒ กลายเป็น **รายการที่ดูเหมือนมีอำนาจแต่ไม่มี** = คลาสเดียวกับที่เราไล่ปิดกันทั้งวัน
   ⇒ รอบนี้ด่านตัดสินจาก "ไม่เรียก adminGate และไม่อยู่ในข้อยกเว้น" ⇒ รายการมีผลจริง

   ⚙️ **งานตามเวลายกเว้นอัตโนมัติ** (มี `schedule` ใน config) — ไม่มีคนนอกเรียกได้
      ⇒ ไม่ต้องใส่รายการมือ ⇒ เพิ่มงานตามเวลาใหม่ไม่ต้องมาแก้ไฟล์นี้
      ⚠️ แลกด้วย: ถ้าใครเพิ่ม `schedule` ให้เส้นที่คนเรียกได้ ด่านจะเงียบ ⇒ ยอมรับ เพราะ
         การใส่ `schedule` เป็นการตัดสินใจที่เห็นชัดในโค้ด ไม่ใช่การลืม */
/* 🔑 **เหตุผลมีสองพันธุ์ ต้องดูแลคนละแบบ** (ข้อของฝั่งจอ 19 ก.ย. เย็น — ผมเอามาใช้กับรายการนี้)
   · `[ขอบเขต]` = เหตุผลเชิงการออกแบบ/คำสั่ง ("นี่เป็นประตูของลูกค้า") ⇒ **ไม่หมดอายุเอง**
     ไม่มีอะไรในโลกข้างนอกทำให้มันเป็นเท็จได้ ⇒ ไม่ต้องตรวจซ้ำ
   · `[โลกข้างนอก]` = เหตุผลที่อ้างว่า **ของข้างนอกทำงานแบบหนึ่ง** ("Beam ยืนยันด้วยลายเซ็น" ·
     "สคริปต์ Google Ads มีรหัสของตัวเอง") ⇒ **เป็นเท็จได้ทุกวันโดยที่ซอร์สเราไม่เปลี่ยนสักตัวอักษร**
     ⇒ ต้องมีวิธีตรวจซ้ำกำกับ และเป็นรายการที่ต้องรื้อดูเมื่อผู้ให้บริการเปลี่ยนวิธียืนยัน
   🔑 แยกสองพันธุ์นี้ไม่ออก = **ตรวจซ้ำทุกอันเท่ากัน (แพง) หรือไม่ตรวจเลยทั้งกอง (อันตราย)**
   ⚠️ ป้ายนี้ **ไม่มีอำนาจตัดสินใจในโค้ด** (ด่านยังยกเว้นทุกแถวเหมือนเดิม) — เป็นของสำหรับคนอ่าน
      ถ้าวันหน้าจะให้มันตัดสิน ต้องแยกเป็นฟิลด์ ไม่ใช่ตีความจากข้อความ [[explain-fields-cant-decide]] */
const เส้นสาธารณะของลูกค้า = {
  "auth.mjs": "[ขอบเขต] ลูกค้าเข้าสู่ระบบ — เป็นประตูของลูกค้า ไม่ใช่ประตูหลังร้าน",
  "oauth-line.mjs": "[ขอบเขต] ลูกค้ากดเข้าสู่ระบบด้วย LINE — ทางเดินกลางกัน open redirect ให้แล้ว",
  "oauth-facebook.mjs": "[ขอบเขต] ลูกค้ากดเข้าสู่ระบบด้วย Facebook",
  "oauth-google.mjs": "[ขอบเขต] ลูกค้ากดเข้าสู่ระบบด้วย Google",
  "cart-sync.mjs": "[ขอบเขต] ตะกร้าของลูกค้าที่ล็อกอินแล้ว — ใช้ session ลูกค้า ไม่ใช่รหัสหลังร้าน",
  "stock.mjs": "[ขอบเขต] หน้าร้านเรียกดูสต็อก/ราคาสด — เปิดให้ลูกค้าโดยตั้งใจ",
  "products-feed.mjs": "[ขอบเขต] ฟีดสินค้าให้ผู้ช่วย AI อ่าน — สาธารณะโดยตั้งใจ",
  "catalog-feed.mjs": "[ขอบเขต] ฟีดสินค้าให้ Google Merchant อ่าน — สาธารณะโดยตั้งใจ",
  "pay-options.mjs": "[ขอบเขต] หน้าเช็คเอาต์ถามว่าเปิดช่องทางชำระเงินอะไรไว้",
  "permit-link.mjs": "[ขอบเขต] ลิงก์สั้นของฟอร์ม ลซ.๑ ที่ลูกค้ากดแชร์เอง — เก็บ 7 วันลบอัตโนมัติ",
  "ads-push.mjs": "[โลกข้างนอก · ตรวจซ้ำ: ยิง /api/ads-push ไม่ใส่ google.pushKey ต้องได้ 401] สคริปต์ใน Google Ads ยิงเข้า — **มีรหัสของตัวเอง** (google.pushKey) เทียบแบบเวลาเท่ากัน",
  "beam-webhook.mjs": "[โลกข้างนอก · ตรวจซ้ำ: ยิง /api/beam/webhook ด้วยลายเซ็นผิด ต้องถูกปฏิเสธ] Beam ยิงเข้าเมื่อลูกค้าจ่ายเงิน — ยืนยันด้วยลายเซ็นของ Beam ไม่ใช่รหัสหลังร้าน",
  "telegram.mjs": "[โลกข้างนอก · ตรวจซ้ำ: ยิง /api/telegram ไม่ใส่ secret ต้องถูกปฏิเสธ] Telegram ยิง webhook เข้า — ยืนยันด้วย secret ของ Telegram",
  "token-refresh.mjs": "[ขอบเขต] ต่ออายุ token มาร์เก็ตเพลส — ไม่มีคนเรียก (งานเบื้องหลัง)",
};

/* 🔒 **ด่านของรายการยกเว้นเอง** — ทุกแถวต้องติดพันธุ์ของเหตุผล (เพิ่ม 19 ก.ย. เย็น)
   🔑 ถ้าไม่บังคับด้วยกลไก แถวใหม่จะถูกเพิ่มแบบไม่มีป้ายภายในสัปดาห์เดียว
      (กติกาที่อาศัยความจำ ถูกละเมิดครั้งแรกโดยคนที่ตั้งมัน — เราพิสูจน์กันมาทั้งวัน)
   และแถวพันธุ์ `[โลกข้างนอก]` ต้องมี **วิธีตรวจซ้ำ** กำกับ ไม่ใช่แค่ป้าย
   ⇒ ไม่งั้นเราจะรู้ว่ามันหมดอายุได้ แต่ไม่มีใครรู้ว่าจะตรวจยังไง = รู้แล้วทำอะไรไม่ได้ */
test("ทุกแถวในรายการยกเว้น ต้องติดพันธุ์ของเหตุผล · พันธุ์ [โลกข้างนอก] ต้องบอกวิธีตรวจซ้ำ", () => {
  const แถว = Object.entries(เส้นสาธารณะของลูกค้า);
  assert.ok(แถว.length > 5, `รายการยกเว้นมีแค่ ${แถว.length} แถว — น้อยเกินจริง ⇒ ตะแกรงพัง`);
  const ไม่มีป้าย = แถว.filter(([, r]) => !/^\[(ขอบเขต|โลกข้างนอก)/.test(r)).map(([f]) => f);
  assert.deepEqual(ไม่มีป้าย, [], "ต้องขึ้นต้นด้วย [ขอบเขต] (ไม่หมดอายุ) หรือ [โลกข้างนอก] (หมดอายุได้)");
  const ขาดวิธีตรวจ = แถว
    .filter(([, r]) => r.startsWith("[โลกข้างนอก") && !r.includes("ตรวจซ้ำ:"))
    .map(([f]) => f);
  assert.deepEqual(ขาดวิธีตรวจ, [], "แถวที่อ้างพฤติกรรมของข้างนอก ต้องเขียนวิธีตรวจซ้ำกำกับ");
});

const ไฟล์ = readdirSync("netlify/functions").filter((f) => f.endsWith(".mjs"));

const เป็นงานตามเวลา = (code) => /export\s+const\s+config\s*=\s*\{[^}]*schedule/.test(code);
/* 🔴 **ต้องตรวจว่า "เรียกใช้จริง" ไม่ใช่ "มีชื่ออยู่"** (แก้ 19 ก.ย. 2569)
   รอบแรกผมใช้ `/adminGate/.test(code)` ⇒ ปลูกบั๊กเปลี่ยนเป็น `adminGateX` **ด่านยังเขียว**
   เพราะ `adminGateX` มีคำ `adminGate` อยู่ข้างใน ⇒ **ตะแกรงจับสตริงย่อย** (โรคที่ทีมห้ามตัวเองไว้แล้ว)
   ⇒ เปลี่ยนเป็นจับ **รูปของการเรียกฟังก์ชัน** พร้อมขอบคำ ⇒ ใกล้ "พฤติกรรม" ขึ้นอีกชั้น
   ⚠️ ยังไม่ใช่พฤติกรรมจริง (ยังเป็นการอ่านซอร์ส) ⇒ ชั้นพฤติกรรมคือยิงด้วยรหัสผิดแล้วต้องได้ 401 */
const เรียกใช้จริง = (code) => /\badminGate\s*\(/.test(code);

test("ไฟล์ฟังก์ชันที่คนเรียกได้ ต้องเรียก adminGate — ห้ามเทียบรหัสเอง", () => {
  const ค้าง = [];
  for (const f of ไฟล์) {
    const code = ซอร์สไม่เอาคอมเมนต์(`netlify/functions/${f}`);
    /* ① เทียบรหัสเอง = ผิดแน่นอน ไม่ว่าเป็นเส้นแบบไหน (จะไม่มีตัวกันเดารหัสรัว ๆ) */
    if (/CHAT_ADMIN_KEY/.test(code) && !เรียกใช้จริง(code)) {
      ค้าง.push(`${f} — อ่าน CHAT_ADMIN_KEY เองโดยไม่ผ่าน adminGate`);
      continue;
    }
    /* ② ไม่เรียก adminGate เลย ⇒ ต้องเป็นงานตามเวลา หรืออยู่ในรายการเส้นสาธารณะพร้อมเหตุผล */
    if (!เรียกใช้จริง(code) && !เป็นงานตามเวลา(code) && !(f in เส้นสาธารณะของลูกค้า)) {
      ค้าง.push(`${f} — ไม่เรียก adminGate · ไม่ใช่งานตามเวลา · ไม่อยู่ในรายการเส้นสาธารณะ`);
    }
  }
  assert.deepEqual(ค้าง, [],
    "ไฟล์เหล่านี้เทียบรหัสหลังร้านเอง ⇒ **ไม่มีตัวกันเดารหัสรัว ๆ**:\n  " + ค้าง.join("\n  ") +
      "\n  ⇒ ต้องเรียกผ่าน `netlify/lib/admin-gate.mjs` เท่านั้น");
});

test("รายการยกเว้นต้องเป็นรายไฟล์พร้อมเหตุผล และต้องไม่มีไฟล์ที่ตายแล้วค้างอยู่", () => {
  /* 🔑 ยกเว้นเป็นกลุ่ม = เปิดประตูให้ไฟล์ใหม่ที่ยังไม่มีใครดู (บทเรียนของฝั่งจอ)
     และรายการที่ชี้ไปไฟล์ที่ไม่มีอยู่ ทำให้คนเชื่อว่ามีไฟล์นั้น */
  const ตายแล้ว = Object.keys(เส้นสาธารณะของลูกค้า).filter((f) => !ไฟล์.includes(f));
  assert.deepEqual(ตายแล้ว, [], "รายการยกเว้นชี้ไปไฟล์ที่ไม่มีอยู่แล้ว: " + ตายแล้ว.join(", "));
  for (const [f, เหตุ] of Object.entries(เส้นสาธารณะของลูกค้า)) {
    assert.ok(String(เหตุ).length > 10, `${f}: เหตุผลยกเว้นสั้นเกินไป`);
  }
});

test("⚠️ ด่านนี้ครอบ 'ร่องรอย' ไม่ใช่ 'พฤติกรรม' — ต้องเขียนบอกไว้ในไฟล์", () => {
  /* กันคนอ่านผลเขียวแล้วเชื่อว่าตรวจครบ — ชั้นพฤติกรรมยังทำได้ทีละ ≤4 เส้นเท่านั้น */
  const เอง = readFileSync("scripts/tests/admin-routes-use-gate.test.mjs", "utf8");
  assert.match(เอง, /ล็อก IP 15 นาที/, "ต้องบันทึกข้อจำกัดเรื่องตัวกันเดารหัสไว้");
  assert.match(เอง, /ร่องรอย/, "ต้องบอกว่าด่านนี้อยู่ชั้นร่องรอย ไม่ใช่ชั้นพฤติกรรม");
});
