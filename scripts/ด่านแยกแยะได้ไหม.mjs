#!/usr/bin/env node
/* 📖 **ทะเบียน: ด่านตัวไหน "พิสูจน์แล้วว่าแยกแยะได้"** — และตัวไหนยังไม่เคยถูกพิสูจน์
 *
 * 🔴 ที่มา 19 ก.ย. 2569 ค่ำ · ฝั่งจอทำทะเบียนแบบนี้ก่อน แล้ววัดได้ว่า **6 จาก 39 ด่าน**
 *    ของเขาถูกพิสูจน์แล้ว ⇒ ที่เหลือ **เขียวของมันแปลว่า "ยังไม่เจอ" ไม่ใช่ "ไม่มี"**
 *    ⇒ ฝั่งท่อ **ไม่มีทะเบียนแบบนี้เลย** ⇒ ผมตอบไม่ได้ว่าด่าน 16 ตัวของผมกี่ตัวเคยถูกพิสูจน์
 *    ⇒ ไฟล์นี้ทำให้ตอบได้ **ด้วยการรันของจริง ไม่ใช่ความจำ**
 *
 * 🔑 ทำไมต้องเป็นทะเบียนที่ **คนเขียนเอง** ไม่ใช่ตะแกรงที่สกัดจากซอร์ส:
 *    ฝั่งจอลองสกัด "ด่านที่พิมพ์รายการตอนแดง" จากรูปการเขียน ⇒ ได้ 17 จุด และ
 *    **รู้เองว่าอย่างน้อย 4 ตัวในนั้นพิมพ์รายการจริง** ⇒ ตะแกรงผูกกับ *รูปการเขียน* ไม่ใช่ *พฤติกรรม*
 *    ⇒ พฤติกรรมพิสูจน์ได้ทางเดียว: **ปลูกของเสียเข้าไป แล้วดูว่ามันแดงและเอ่ยถึงของที่ปลูกไหม**
 *
 * 🔑 และข้อที่ฝั่งจอเติมให้ใบบทเรียนวันนี้ ใช้กับไฟล์นี้ตรง ๆ:
 *    **"ถ้าไม่มีเคสที่เรารู้คำตอบเลย เราจะไม่มีวันรู้ว่าตัววัดผิด"**
 *    ⇒ ทะเบียนนี้ **คือคลังเคสที่เรารู้คำตอบ** ⇒ มันไม่ใช่แค่ขั้นตอนตรวจ แต่เป็นเงื่อนไขที่ทำให้วัดได้
 *
 * ⚠️ ด่านที่ยังไม่อยู่ในทะเบียน **ไม่ได้แปลว่าพัง** — แปลว่า "เขียวของมันยังไม่มีน้ำหนัก"
 *    🚫 ไฟล์นี้จึง **ไม่ทำให้ build ตก** (ไม่อยู่ใน prebuild) — เป็นตัววัดความครอบคลุม ไม่ใช่ด่าน
 *
 * ใช้: node scripts/ด่านแยกแยะได้ไหม.mjs [ชื่อบางส่วน]
 */
import { readdirSync } from "node:fs";
import { ปลูกแล้ววัด } from "./lib/ปลูกแล้ววัด.mjs";

/** ทะเบียน — เพิ่มด่านใหม่ที่นี่ พร้อม **วิธีปลูกที่เหมือนของจริง**
 *  🔑 `ยืนยันปลูกลง` คือคำสั่งที่ **ต้องล้ม** หลังปลูก ⇒ กัน "ปลูกไม่ลงแล้วอ่านเขียวว่าด่านพัง" */
const ทะเบียน = [
  {
    ด่าน: "check-syntax",
    ไฟล์: "scripts/gen-warn-keys.mjs",
    แก้: (s) => s + "\nconst ZZNOPEZZ = ;\n",
    ยืนยันปลูกลง: ["node", "--check", "scripts/gen-warn-keys.mjs"],
    คำสั่ง: ["node", "scripts/check-syntax.mjs"],
    ต้องเอ่ยถึง: "gen-warn-keys.mjs",
    ที่มา: "19 ก.ย. 2569 — `*/` ในคอมเมนต์ปิดคอมเมนต์กลางทาง ⇒ ไฟล์พังทั้งไฟล์",
  },
  {
    ด่าน: "check-shell-varnames",
    ไฟล์: "public/r2-setup.sh",
    แก้: (s) => s + "\n  ผลลัพธ์=$(echo hi)\n",
    คำสั่ง: ["node", "scripts/check-shell-varnames.mjs"],
    ต้องเอ่ยถึง: "ผลลัพธ์=",
    ที่มา: "19 ก.ย. 2569 — ชื่อตัวแปรไทยใน bash ล้มเงียบ ⇒ ตัวเช็คไม่เคยทำงาน",
  },
  {
    ด่าน: "check-twin-tables (เลขไม่ตรง)",
    ไฟล์: "netlify/lib/shipping.mjs",
    แก้: (s) => s.replace("upTo: 500", "upTo: 501"),
    คำสั่ง: ["node", "scripts/check-twin-tables.mjs"],
    ต้องเอ่ยถึง: "ไม่ตรงกัน",
    ที่มา: "19 ก.ย. 2569 — กติกา 'ต้องตรงกัน' ใน CLAUDE.md ไม่มีกลไกบังคับ",
  },
  {
    ด่าน: "check-twin-tables (สกัดไม่ออก)",
    ไฟล์: "scripts/img-to-r2.mjs",
    แก้: (s) => s.replace("const LADDER =", "const ขั้นบันได ="),
    คำสั่ง: ["node", "scripts/check-twin-tables.mjs"],
    ต้องเอ่ยถึง: "สกัดไม่ได้ ⇒ ถือว่าตก",
    ที่มา: "ทิศที่อันตรายกว่า: สกัดไม่ออกต้องแดง ไม่ใช่เขียว",
  },
  {
    ด่าน: "check-floating (promise ลอย)",
    ไฟล์: "netlify/lib/deploy-state.mjs",
    แก้: (s) => s.replace("const API =", "fetch('https://example.com');\nconst API ="),
    คำสั่ง: ["node", "scripts/check-floating.mjs"],
    ต้องเอ่ยถึง: "deploy-state.mjs",
    ที่มา: "กติกาเหล็กของโปรเจกต์ — Netlify แช่แข็งฟังก์ชันหลังตอบ งานที่ไม่ await ตายเงียบ",
  },
  {
    ด่าน: "check-claude-md-numbers",
    ไฟล์: "CLAUDE.md",
    /* 🔴 **รันแรกของทะเบียนนี้ให้ผล "ด่านจับไม่ได้" — และนั่นเป็นการโทษด่านผิด**
       ข้อความ `วัดจริง 6 ก.ย. 2569: 2,483 รายการ` อยู่ใน CLAUDE.md **สองที่** (บรรทัด 171 และ 198)
       `replace` โดนตัวแรก (บรรทัด 171 · ในบล็อกโครงสร้าง) แต่ด่านอ่านด้วย regex ที่ต้องมีคำว่า
       `products.json` อยู่ในบรรทัดเดียวกัน ⇒ มันอ่าน **บรรทัด 198 เท่านั้น** ⇒ เขียวถูกต้อง
       🔑 ⇒ **ปลูกลงจริง · พฤติกรรมเปลี่ยนจริง · แต่ลงนอกขอบเขตที่ด่านมอง**
          = ผลลัพธ์ที่ห้าที่เครื่องมือร่วมยังไม่มี (ตอนนี้มันถูกรายงานเป็น "ด่านจับไม่ได้")
       ⇒ แก้การปลูกให้ตรงบรรทัดที่ด่านอ่าน โดยผูกกับ `products.json` เหมือน regex ของด่าน */
    /* ⚠️ และรอบที่สองก็ยังแดง เพราะผมเขียนตัวแทนเป็นสตริง `"$1" + "99,999"` ติดกัน
       ⇒ JS อ่าน **`$199` เป็นกลุ่มที่ 199** ⇒ ข้อความที่ปลูกเพี้ยน ⇒ ด่านยังเขียว
       🔑 กับดักคลาสสิกของ `String.replace` — ใช้ **ฟังก์ชัน** แทนสตริงจึงไม่มีทางกำกวม */
    แก้: (s) => s.replace(
      /(products\.json[^\n]*?วัดจริง[^\n]*?:\s*)[\d,]+(\s*รายการ)/,
      (_, ก่อน, หลัง) => ก่อน + "99,999" + หลัง
    ),
    คำสั่ง: ["node", "scripts/check-claude-md-numbers.mjs"],
    /* 🔑 **`ต้องเอ่ยถึง` ต้องเป็นรูปที่ "ด่านพิมพ์" ไม่ใช่รูปที่ "เราปลูก"**
       ผมปลูก `99,999` (มีคอมมา ตามรูปในคู่มือ) แต่ด่าน normalize เป็นตัวเลขก่อนพิมพ์
       ⇒ มันพิมพ์ `คู่มือบอก 99999` ⇒ การเทียบด้วย "99,999" จึงไม่เจอ
       ⇒ แล้วเครื่องมือตอบว่า "ตะแกรงของด่านมีปัญหาจริง" = **โทษด่านผิดอีกชั้น**
       ⇒ ⇒ ชั้นนี้เพิ่งเสียเวลาไปสองรอบ (สตริง `$199` · คอมมา) ⇒ จดไว้เพื่อไม่ให้เสียอีก */
    ต้องเอ่ยถึง: "99999",
    ที่มา: "เลขในคู่มือค้างได้เงียบ ๆ — ตัวเลขที่โตเองต้องมีตัวเฝ้า",
  },
];

const กรอง = process.argv[2];
const รายการ = กรอง ? ทะเบียน.filter((t) => t.ด่าน.includes(กรอง)) : ทะเบียน;

const ผล = [];
for (const t of รายการ) {
  const r = ปลูกแล้ววัด({
    ไฟล์: t.ไฟล์,
    แก้: t.แก้,
    ยืนยันปลูกลง: t.ยืนยันปลูกลง,
    ด่าน: t.คำสั่ง,
    ต้องเอ่ยถึง: t.ต้องเอ่ยถึง,
  });
  ผล.push({ ...t, ...r });
  const ไอคอน = r.ผล === "ด่านจับได้" ? "✅" : r.ผล === "ด่านเห็นแต่ไม่ทำให้ตก" ? "🔵"
    : r.ผล === "ปลูกไม่ลง" ? "🟠" : "🔴";
  console.log(`${ไอคอน} ${t.ด่าน} — ${r.ผล}`);
  if (r.ผล !== "ด่านจับได้") console.log(`   ${r.รายละเอียด}`);
}

/* ความครอบคลุม — เทียบกับจำนวนด่านที่มีจริงในโฟลเดอร์ ไม่ใช่จำนวนในทะเบียน
   🔑 ถ้าเทียบกับทะเบียนเอง จะได้ 100% ตลอดกาล (สองข้างของเครื่องหมายเท่ากับมาจากที่เดียวกัน) */
const ด่านทั้งหมด = readdirSync("scripts").filter((f) => f.startsWith("check-") && f.endsWith(".mjs"));
const ชื่อในทะเบียน = new Set(ทะเบียน.map((t) => t.ด่าน.split(" ")[0] + ".mjs"));
const ยังไม่พิสูจน์ = ด่านทั้งหมด.filter((f) => !ชื่อในทะเบียน.has(f));
/* 🔵 "ด่านเห็นแต่ไม่ทำให้ตก" **นับเป็นตรวจจับได้** — ความรุนแรงเป็นการตัดสินใจแยกเรื่อง */
const จับได้ = ผล.filter((r) => r.ผล === "ด่านจับได้" || r.ผล === "ด่านเห็นแต่ไม่ทำให้ตก").length;

console.log(
  `\n📏 ทะเบียนนี้พิสูจน์ ${ผล.length} เคส · **ด่านจับได้ ${จับได้}** · ไม่ผ่าน ${ผล.length - จับได้}` +
  `\n   ด่านในโฟลเดอร์ทั้งหมด ${ด่านทั้งหมด.length} ตัว · อยู่ในทะเบียน ${ชื่อในทะเบียน.size} ตัว` +
  `\n   🔴 **ยังไม่เคยถูกพิสูจน์ ${ยังไม่พิสูจน์.length} ตัว** ⇒ เขียวของมันแปลว่า "ยังไม่เจอ" ไม่ใช่ "ไม่มี":` +
  `\n      ${ยังไม่พิสูจน์.join(" · ")}` +
  "\n   ⚠️ ไม่อยู่ในทะเบียน ≠ พัง · ไฟล์นี้ไม่ทำให้ build ตก (เป็นตัววัดความครอบคลุม ไม่ใช่ด่าน)"
);

if (ผล.some((r) => r.ผล === "ด่านจับไม่ได้")) process.exitCode = 1;
