#!/usr/bin/env node
/* ⚖️ สัดส่วน **"เครื่องตรวจได้เอง" เทียบ "อาศัยคำรับรอง"** ในด่านฝั่งท่อ
 *
 * 🔴 ที่มา 19 ก.ย. 2569 — ฝั่งจอวัดฝั่งตัวเองแล้วพบว่า 5 จาก 10 จอ
 *    ที่ "ผ่านด่าน applied" ผ่านด้วย **คำรับรองของคนเขียน** ไม่ใช่การตรวจ
 *    แกนของเรื่อง (ประโยคของเขา): *"คำรับรองถูกและมีเสมอ · การพิสูจน์แพงและบางทีทำไม่ได้
 *    ถ้าตัววัดนับสองอย่างนี้เท่ากัน ของถูกจะชนะทุกครั้งที่มีแรงกดดัน"*
 *    ⇒ ไม่มีใครตัดสินใจให้ด่านกลายเป็นพิธีกรรม มันแค่เป็นทางที่ต้านน้อยที่สุด ทีละข้อยกเว้นที่ฟังขึ้น
 *    ⇒ ฝั่งท่อจึงต้องวัดตัวเองด้วย **ก่อนที่จะเป็นแบบเดียวกันโดยไม่มีใครรู้**
 *
 * 🔑 และข้อที่สำคัญที่สุด: ตัวเลขต้องมาพร้อม **วิธีตอบสนองที่ถูก**
 *    ⇒ *"ต้องแก้ตัวจับ ไม่ใช่เพิ่มคำรับรอง"* — เพราะวิธีที่ง่ายที่สุดในการทำให้ด่านแดงเป็นเขียว
 *      คือเขียนคำรับรองเพิ่มอีกใบ ซึ่งลดเลขได้โดยไม่ได้เพิ่มความจริงเลยแม้แต่นิด
 *
 * 🚫 **รายงาน ไม่ใช่ด่าน** — ไม่ทำให้ build ตก · บางเรื่องพิสูจน์ด้วยเครื่องไม่ได้จริง ๆ
 *    (ยิงด้วยรหัสผิดแล้วต้องได้ 401 ⇒ ทำอัตโนมัติไม่ได้เพราะ adminGate ล็อก IP 15 นาที)
 * 🚫 **ไม่ใช่ parser** · ตะแกรงเป็นรูปของ assert ⇒ อ่านว่า "เจอเท่านี้ในตะแกรงนี้"
 *
 * ใช้: node scripts/check-attestation-ratio.mjs [--รายไฟล์]
 */
import { readdirSync, readFileSync } from "node:fs";
import { เตือนถ้ามีไฟล์ซ้อนชั้น } from "./lib/ไฟล์ซ้อนชั้นที่ด่านมองไม่เห็น.mjs";

const ไดเรกทอรี = "scripts/tests";
/* คำรับรอง = assert ที่ตรวจ **เนื้อไฟล์/ข้อความที่คนเขียนไว้** ⇒ เขียนข้อความให้ตรงก็ผ่าน
   กลไก    = assert ที่ตรวจ **ค่าที่ได้จากการเรียกใช้จริง** ⇒ ต้องทำงานถูกจึงผ่าน */
/* 🔴 **20 ก.ย. 2569 — `\b` ไม่มีขอบคำในอักษรไทย** (ตัวควบคุมข้างล่างจับได้ทันที)
   คำไทย `เนื้อ` · `ซอร์ส` · `โค้ด` **ไม่เคยถูกจับเลย** ⇒ ตะแกรงเห็นแต่ชื่อตัวแปรอังกฤษ
   ⇒ คลาสเดียวกับ `check-nested-nets` ที่เจอเมื่อครู่ (ไล่ทั้งคลาสแล้ว — ดูสรุปในคอมมิต) */
const ดูเหมือนเนื้อไฟล์อังกฤษ = /\b(src|SRC|code|text|raw|contents?)\b|readFileSync/;
const ดูเหมือนเนื้อไฟล์ไทย = /(เนื้อ|ซอร์ส|โค้ด|ซอร์สไม่เอาคอมเมนต์)/;
const ดูเหมือนเนื้อไฟล์ = { test: (x) => ดูเหมือนเนื้อไฟล์อังกฤษ.test(x) || ดูเหมือนเนื้อไฟล์ไทย.test(x) };

/* 🧪 **ตัวควบคุมฝังใน** (ท่าของฝั่งจอ · 20 ก.ย. 2569 เย็น)
   🔴 และรูปนี้เป็นรูปเดียวกับที่เพิ่งจับรูใน `check-nested-nets` ได้เมื่อครู่:
      `\b` **ไม่ทำงานกับอักษรไทย** ⇒ คำไทยในรายการ (`เนื้อ` · `ซอร์ส` · `โค้ด`) ไม่เคยถูกจับ
      ⇒ ⇒ จึงต้องมีเคส **ต้องจับที่เป็นภาษาไทย** เป็นตัวควบคุม ไม่ใช่เชื่อว่า regex ครอบ
   ⇒ ควบคุมไม่ผ่าน = **ผลของด่านนี้อ่านไม่ได้** ไม่ใช่ "สัดส่วนคำกล่าวอ้างปกติ" */
{
  const ต้องจับ = [
    "const s = readFileSync(p, 'utf8');",
    "ตรวจจาก src ของไฟล์",
    "อ่านเนื้อไฟล์มาเทียบ",        // ภาษาไทย — รูปที่ `\b` ทำให้หลุด
    "เทียบกับซอร์สที่ commit ไว้",
  ];
  const ต้องไม่จับ = [
    "const n = await coreQuery(sql);",
    "ยิงของจริงแล้ววัดผล",
    "",
  ];
  const พลาด = [
    ...ต้องจับ.filter((x) => !ดูเหมือนเนื้อไฟล์.test(x)).map((x) => `ต้องจับแต่ไม่จับ: ${x}`),
    ...ต้องไม่จับ.filter((x) => ดูเหมือนเนื้อไฟล์.test(x)).map((x) => `ต้องไม่จับแต่จับ: ${x}`),
  ];
  if (พลาด.length) {
    console.error(
      "🔴 **ตัวควบคุมของด่านนี้ไม่ผ่าน ⇒ ผลของด่านนี้อ่านไม่ได้**\n" +
      พลาด.map((x) => `   · ${x}`).join("\n") +
      "\n   🔑 ถ้าเป็นเคสภาษาไทย ให้สงสัย `\\b` ก่อน — มันไม่มีขอบคำในอักษรไทย"
    );
    process.exit(1);
  }
}

let ไฟล์ = 0;
let ไฟล์ที่อ่านซอร์ส = 0;
let กลไกแน่ = 0;      // assert ในไฟล์ที่ **ไม่เคยอ่านซอร์สเลย** ⇒ ตรวจค่าตอนรันแน่นอน
let คำรับรองแน่ = 0;  // assert ที่สิ่งถูกตรวจดูเหมือนเนื้อไฟล์ตรง ๆ
let แยกไม่ออก = 0;    // assert ในไฟล์ที่อ่านซอร์ส แต่ตัวที่ถูกตรวจชื่ออะไรก็ไม่รู้
const รายไฟล์ = [];

/* 🔑 ด่านนี้กวาด `scripts/tests` **ชั้นเดียว** ⇒ ต้องรู้ตัวถ้ามีเทสย้ายลงโฟลเดอร์ย่อย
   (วัดแล้ว 20 ก.ย. 2569: ยังแบนจริง 0 ไฟล์ ⇒ ด่านถูกวันนี้ แต่ถูกเพราะข้อเท็จจริงนอกตัวมันเอง) */
if (เตือนถ้ามีไฟล์ซ้อนชั้น([ไดเรกทอรี], (n) => n.endsWith(".test.mjs"), "check-attestation-ratio")) {
  process.exit(1);
}
for (const f of readdirSync(ไดเรกทอรี).filter((x) => x.endsWith(".test.mjs"))) {
  ไฟล์ += 1;
  const s = readFileSync(`${ไดเรกทอรี}/${f}`, "utf8");
  const อ่านซอร์ส = /readFileSync|ซอร์สไม่เอาคอมเมนต์|เทียบคอมเมนต์/.test(s);
  /* 🔑 **จัดประเภทราย assert ไม่ใช่รายไฟล์** (แก้ 20 ก.ย. 2569)
     รุ่นก่อนถือว่า "ไฟล์นี้อ่านซอร์ส ⇒ assert ทุกตัวในไฟล์กำกวม" ⇒ กอง ❓ ใหญ่ถึง 828
     ⇒ ตัววัดนี้จึง **ไม่เคยให้สัดส่วนได้เลย** (มันประกาศเองว่า "ห้ามรายงาน")
     ⇒ เก็บ **ชื่อตัวแปรที่ถือเนื้อซอร์สจริงในไฟล์นั้น** แล้วดูว่า assert ตรวจตัวไหน
     🚫 ยังคงเป็นตะแกรง ไม่ใช่ parser ⇒ ของที่แยกไม่ออกต้อง **คงอยู่ในกอง ❓** ห้ามเดาเข้าข้างตัวเอง
     (ฝั่งจอทำข้อเดียวกันกับด่านของเขาแล้วได้อำนาจแยกแยะ 38% — เลขที่ใช้ได้ ดีกว่า "ห้ามรายงาน") */
  const ชื่อถือซอร์ส = new Set();
  for (const m of s.matchAll(/(?:const|let|var)\s+([\wก-๙$]+)\s*=\s*(?:await\s+)?[^;\n]*?(?:readFileSync|ซอร์สไม่เอาคอมเมนต์)/g)) ชื่อถือซอร์ส.add(m[1]);
  /* รูปรื้อโครง: `const { โค้ด, ดิบ } = เทียบคอมเมนต์(…)` */
  for (const m of s.matchAll(/(?:const|let)\s*\{([^}]*)\}\s*=\s*เทียบคอมเมนต์/g)) {
    for (const ชื่อ of m[1].split(",")) { const t = ชื่อ.trim(); if (t) ชื่อถือซอร์ส.add(t); }
  }
  /* 🔑 เซตสมมาตร: ชื่อที่ถือ **ค่าตอนรัน** (มาจากการเรียกฟังก์ชัน/await ที่ไม่ใช่ตัวอ่านซอร์ส)
     ⇒ assert ที่ตรวจชื่อพวกนี้คือ "กลไก" แม้จะอยู่ในไฟล์ที่อ่านซอร์สด้วย
     🚫 กันเข้าข้างตัวเอง: ชื่อที่ **ฟังเหมือนเนื้อไฟล์** (src · ซอร์ส · โค้ด · ดิบ · เนื้อ · text)
        ไม่ถูกนับเป็นค่าตอนรัน แม้จะมาจากการเรียกฟังก์ชัน (อาจเป็นฟังก์ชันอ่านไฟล์ที่เราไม่รู้จัก) */
  const ฟังเหมือนซอร์ส = /src|ซอร์ส|โค้ด|ดิบ|เนื้อ|text|body/i;
  const ชื่อถือค่ารัน = new Set();
  for (const m of s.matchAll(/(?:const|let|var)\s+([\wก-๙$]+)\s*=\s*(?:await\s+)?[\wก-๙$.]+\(/g)) {
    const ชื่อ = m[1];
    if (!ชื่อถือซอร์ส.has(ชื่อ) && !ฟังเหมือนซอร์ส.test(ชื่อ)) ชื่อถือค่ารัน.add(ชื่อ);
  }
  if (อ่านซอร์ส) ไฟล์ที่อ่านซอร์ส += 1;
  let ก = 0;
  let ค = 0;
  let ม = 0;
  for (const m of s.matchAll(/\bassert(?:\.\w+)?\(\s*([^,)\n]{1,120})/g)) {
    const ตัวถูกตรวจ = m[1];
    const หัวตัวแปร = (ตัวถูกตรวจ.match(/^[\wก-๙$]+/) ?? [""])[0];
    if (ดูเหมือนเนื้อไฟล์.test(ตัวถูกตรวจ) || ชื่อถือซอร์ส.has(หัวตัวแปร)) ค += 1;
    /* มีวงเล็บเรียกฟังก์ชัน = ค่าที่คิดตอนรัน ⇒ กลไก (แม้อยู่ในไฟล์ที่อ่านซอร์สด้วย) */
    else if (/\(/.test(ตัวถูกตรวจ) || ชื่อถือค่ารัน.has(หัวตัวแปร)) ก += 1;
    else if (อ่านซอร์ส) ม += 1;   // 🚫 ห้ามนับเป็นกลไก — ดูเหตุผลหัวไฟล์
    else ก += 1;
  }
  กลไกแน่ += ก;
  คำรับรองแน่ += ค;
  แยกไม่ออก += ม;
  if (ค + ม > 0) รายไฟล์.push({ f, ก, ค, ม });
}

/* 📏 0 ไฟล์ = ตก ไม่ใช่เขียว (ย้ายโฟลเดอร์/เปลี่ยนนามสกุลแล้วรายงานจะดูสวยที่สุด) */
if (ไฟล์ === 0) {
  console.error("✗ check-attestation-ratio: สแกนได้ 0 ไฟล์เทส ⇒ **ตะแกรงพัง ไม่ใช่ไม่มีปัญหา**");
  process.exit(2);
}

const รวม = กลไกแน่ + คำรับรองแน่ + แยกไม่ออก;

if (process.argv.includes("--รายไฟล์")) {
  for (const r of รายไฟล์.sort((a, b) => b.ค + b.ม - (a.ค + a.ม))) {
    console.log(`   คำรับรองแน่ ${r.ค} · แยกไม่ออก ${r.ม} · กลไก ${r.ก} — ${r.f}`);
  }
  console.log("");
}

console.log(
  "⚖️  ด่านฝั่งท่อ — **สามกอง ไม่ใช่สองกอง**\n" +
  /* 🔴 **ป้ายเดิมผิดหลังแก้ตะแกรง (20 ก.ย. 2569)**: เขียนว่า "อยู่ในไฟล์ที่ไม่เคยอ่านซอร์สเลย"
     ⇒ ตอนนี้กองนี้รวม assert ที่ตรวจ **ค่าตอนรัน** ในไฟล์ที่อ่านซอร์สด้วย
     🔑 ป้ายที่ไม่ตรงกับสิ่งที่นับ อันตรายกว่าตัวเลขที่ผิด เพราะคนอ่านเอาไปอ้างต่อได้ทันที */
  `   ✅ กลไก ${กลไกแน่} (ตรวจค่าตอนรัน: อยู่ในไฟล์ที่ไม่อ่านซอร์ส · หรือตรวจผลการเรียกฟังก์ชัน/ตัวแปรที่ถือค่ารัน)\n` +
  `   📝 คำรับรองแน่ ๆ ${คำรับรองแน่} (สิ่งที่ถูกตรวจคือเนื้อไฟล์ ⇒ เขียนข้อความให้ตรงก็ผ่าน)\n` +
  `   ❓ **แยกไม่ออกด้วยตะแกรงนี้ ${แยกไม่ออก}** (อยู่ในไฟล์ที่อ่านซอร์ส · ตัวถูกตรวจเป็นชื่อที่ตะแกรงไม่รู้จักทั้งสองเซต)\n` +
  `   📏 ตะแกรง: assert ทุกตัวใน ${ไดเรกทอรี}/*.test.mjs (${ไฟล์} ไฟล์ · อ่านซอร์ส ${ไฟล์ที่อ่านซอร์ส} ไฟล์) · รวม ${รวม} จุด\n` +
  "      🔴 **รุ่นแรกของรายงานนี้ยัดกอง ❓ ไปรวมกับ ✅ แล้วได้ 'คำรับรองแค่ 2.1%'**\n" +
  "         เอาตาดูตัวอย่างจริงแล้วพบว่าผิด: `admin-routes-use-gate` เทียบตัวแปรชื่อ `ค้าง`/`ตายแล้ว`\n" +
  "         ซึ่งเป็นรายการที่ได้จาก **การกวาดซอร์ส** ⇒ เป็นคำรับรอง แต่ถูกนับเป็นกลไก\n" +
  "         🔑 ตัวเลขที่เข้าข้างตัวเองต้องถูกสงสัยก่อนถูกรายงาน [[three-states-not-two]]\n" +
  "      ⚠️ อ่านว่า \"เจอเท่านี้ในตะแกรงนี้\" ไม่ใช่ \"มีเท่านี้\"\n" +
  (แยกไม่ออก > กลไกแน่ * 0.3
    ? "   🔴 กอง ❓ ใหญ่เกินกว่าจะสรุปอะไรได้ ⇒ **ต้องแก้ตะแกรงก่อน ห้ามรายงานสัดส่วน**\n"
    : "   ✅ กอง ❓ เล็กพอที่สัดส่วนเริ่มมีความหมาย — แต่ยังต้องดูแนวโน้ม ไม่ใช่ครั้งเดียว\n") +
  "   🔑 วิธีที่ง่ายที่สุดในการทำให้ด่านแดงกลายเป็นเขียว **คือเขียนคำรับรองเพิ่มอีกใบ**\n" +
  "      ⇒ *ต้องแก้ตัวจับ ไม่ใช่เพิ่มคำรับรอง* (ประโยคของฝั่งจอ 19 ก.ย. 2569)"
);
