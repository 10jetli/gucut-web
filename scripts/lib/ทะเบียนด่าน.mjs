/* 📋 **ทะเบียนสูตรปลูกบั๊ก — ข้อมูลล้วน ไม่มีผลข้างเคียง**
 *
 * 🔑 แยกออกมาจาก `scripts/ด่านแยกแยะได้ไหม.mjs` เมื่อ 20 ก.ย. 2569 เพราะมีผู้อ่านสองคน:
 *    · ตัวรันจริง (ปลูกแล้ววัด — **แก้ไฟล์จริงชั่วคราว** ⇒ ห้ามรันตอนถูก import)
 *    · ด่านราคาถูก `check-plant-formulas.mjs` (ตรวจแค่ว่า **สูตรยังปลูกลงไหม** ⇒ รันทุก build)
 * 🔴 ที่มา (ฝั่งจอเจอ 20 ก.ย.): เขาเขียน lib ใหม่ ⇒ ข้อความที่สูตรจะแทนไม่มีในไฟล์อีกแล้ว
 *    ⇒ **"หลักฐานการพิสูจน์หมดอายุเงียบ ๆ เพราะการแก้โค้ด"**
 *    และตัวรันทะเบียน **ไม่อยู่ใน prebuild** ⇒ ไม่มีอะไรฟ้องระหว่างการรันมือแต่ละครั้ง
 * 🚫 ไฟล์นี้ต้องไม่มีโค้ดที่ทำอะไรตอนถูกโหลด — มันจะถูก import จากลูกโซ่ build
 */
/* (คำอธิบายเดิมของการ export เพื่อให้ด่านราคาถูก `check-plant-formulas.mjs` ตรวจได้ว่าสูตรยังปลูกลงไหม
   🔴 ที่มา 20 ก.ย. 2569 (ฝั่งจอเจอ): เขาเขียน lib ใหม่ ⇒ ข้อความที่สูตรจะแทน **ไม่มีในไฟล์อีกแล้ว**
      ⇒ สูตรตอบ "ปลูกไม่ลง" · **ถ้าเครื่องมือเชื่อว่า replace สำเร็จ ผลจะออกมาเป็น "ด่านเขียว"
      แล้วเขาจะโทษด่านผิด** ⇒ และไฟล์นี้ **ไม่อยู่ใน prebuild** ⇒ สูตรหมดอายุได้เงียบ ๆ
      ระหว่างการรันมือแต่ละครั้ง ⇒ จึงต้องมีด่านถูก ๆ ที่รันทุก build คอยตรวจแค่ข้อนี้ */
export const ทะเบียน = [
  {
    ด่าน: "check-syntax",
    ไฟล์: "scripts/gen-warn-keys.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องเป็น .mjs ใต้ scripts/ หรือ netlify/ (สองรากที่ด่านกวาด) และไฟล์ต้องถูกไวยากรณ์อยู่ก่อนปลูก",
    แก้: (s) => s + "\nconst " + "ZZ" + "NOPEZZ" + " = ;\n",
    ยืนยันปลูกลง: ["node", "--check", "scripts/gen-warn-keys.mjs"],
    คำสั่ง: ["node", "scripts/check-syntax.mjs"],
    ต้องเอ่ยถึง: "gen-warn-keys.mjs",
    ที่มา: "19 ก.ย. 2569 — `*/` ในคอมเมนต์ปิดคอมเมนต์กลางทาง ⇒ ไฟล์พังทั้งไฟล์",
  },
  {
    ด่าน: "check-shell-varnames",
    ไฟล์: "public/r2-setup.sh",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องเป็น .sh **ในรีโป** — ของนอกรีโป (~/bin) ด่านตั้งใจให้ \"ดังแต่ไม่ตก\" ⇒ ปลูกที่นั่นจะได้ผลที่ห้า ไม่ใช่ตก",
    แก้: (s) => s + "\n  ผลลัพธ์=$(echo hi)\n",
    คำสั่ง: ["node", "scripts/check-shell-varnames.mjs"],
    ต้องเอ่ยถึง: "ผลลัพธ์=",
    ที่มา: "19 ก.ย. 2569 — ชื่อตัวแปรไทยใน bash ล้มเงียบ ⇒ ตัวเช็คไม่เคยทำงาน",
  },
  {
    ด่าน: "check-twin-tables (เลขไม่ตรง)",
    ไฟล์: "netlify/lib/shipping.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องเป็นฝั่งหนึ่งของคู่ตารางที่ลงทะเบียนไว้ · และต้องแก้ **ค่าตัวเลข** ไม่ใช่รูปการเขียน (6_40 = 640 ⇒ ด่านผ่านถูกต้อง)",
    แก้: (s) => s.replace("upTo: 500", "upTo: 501"),
    คำสั่ง: ["node", "scripts/check-twin-tables.mjs"],
    ต้องเอ่ยถึง: "ไม่ตรงกัน",
    ที่มา: "19 ก.ย. 2569 — กติกา 'ต้องตรงกัน' ใน CLAUDE.md ไม่มีกลไกบังคับ",
  },
  {
    ด่าน: "check-twin-tables (สกัดไม่ออก)",
    ไฟล์: "scripts/img-to-r2.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องทำให้ **ตัวสกัดหาไม่เจอ** (เปลี่ยนชื่อตัวแปรที่ regex ยึด) ไม่ใช่แก้ค่า — ด่านนี้วัดกรณี \"สกัดไม่ได้ ⇒ ถือว่าตก\"",
    แก้: (s) => s.replace("const LADDER =", "const ขั้นบันได ="),
    คำสั่ง: ["node", "scripts/check-twin-tables.mjs"],
    ต้องเอ่ยถึง: "สกัดไม่ได้ ⇒ ถือว่าตก",
    ที่มา: "ทิศที่อันตรายกว่า: สกัดไม่ออกต้องแดง ไม่ใช่เขียว",
  },
  {
    ด่าน: "check-floating (promise ลอย)",
    ไฟล์: "netlify/lib/deploy-state.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องอยู่ใต้ netlify/functions หรือ netlify/lib · 🚫 ห้ามปลูกใน netlify/edge-functions — นโยบายที่นั่นกลับด้าน ด่านยกเว้นไว้โดยเจตนา",
    แก้: (s) => s.replace("const API =", "fetch('https://example.com');\nconst API ="),
    คำสั่ง: ["node", "scripts/check-floating.mjs"],
    ต้องเอ่ยถึง: "deploy-state.mjs",
    ที่มา: "กติกาเหล็กของโปรเจกต์ — Netlify แช่แข็งฟังก์ชันหลังตอบ งานที่ไม่ await ตายเงียบ",
  },
  {
    ด่าน: "check-claude-md-numbers",
    ไฟล์: "CLAUDE.md",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องเป็นบรรทัดที่ด่านอ่านจริง (รูป \"วัดจริง … : N\") — ปลูกบรรทัดอื่นในไฟล์เดียวกันจะเงียบ เคยพลาดมาแล้ว 19 ก.ย. 2569",
    /* 🔴 **รันแรกของทะเบียนนี้ให้ผล "ด่านจับไม่ได้" — และนั่นเป็นการโทษด่านผิด**
       ข้อความ `วัดจริง 6 ก.ย. 2569: 2,483 รายการ` อยู่ใน CLAUDE.md **สองที่** (บรรทัด 171 และ 198)
       `replace` โดนตัวแรก (บรรทัด 171 · ในบล็อกโครงสร้าง) แต่ด่านอ่านด้วย regex ที่ต้องมีคำว่า
       `products.json` อยู่ในบรรทัดเดียวกัน ⇒ มันอ่าน **บรรทัด 198 เท่านั้น** ⇒ เขียวถูกต้อง
       🔑 ⇒ **ปลูกลงจริง · พฤติกรรมเปลี่ยนจริง · แต่ลงนอกขอบเขตที่ด่านมอง**
          = ผลลัพธ์ที่ห้าที่เครื่องมือร่วมยังไม่มี (ตอนนี้มันถูกรายงานเป็น "ด่านจับไม่ได้")
       ⇒ แก้การปลูกให้ตรงบรรทัดที่ด่านอ่าน โดยผูกกับ `products.json` เหมือน regex ของด่าน */
    /* 🔴 **แก้คำอธิบายของตัวเอง 19 ก.ย. 2569 ค่ำ — ผมเคยเขียนสาเหตุที่เดาเอาไว้ตรงนี้**
       เคยเขียนว่า "รอบที่สองแดงเพราะสตริง `$199,999$2` ทำให้ JS อ่าน `$199` เป็นกลุ่มที่ 199"
       ⇒ **ยิงทดสอบแล้วไม่จริง**: regex นี้มี 2 กลุ่ม ⇒ `$19` ไม่ใช่กลุ่มที่มีอยู่
         ⇒ JS ถอยไปอ่าน `$1` แล้วต่อเลขจริง ⇒ ผลเหมือนใช้ฟังก์ชันเป๊ะ (`99,999`)
       ⇒ ⇒ **สาเหตุจริงที่แดงคือ `ต้องเอ่ยถึง` ไม่ตรงกับรูปที่ด่านพิมพ์** (ข้อถัดไป) เท่านั้น
       🔑 ฝั่งจอเป็นคนจับผิดข้อนี้ ⇒ คลาส `guessed-cause-written-as-fact` ที่คลังเรามีอยู่แล้ว:
          **อาการเดียวมีหลายสาเหตุ · เขียนสาเหตุที่เดาเอาเหมือนพิสูจน์แล้ว = ส่งต่อความเข้าใจผิด**
       ✅ ยังใช้ **ฟังก์ชัน** แทนสตริงต่อไป แต่ด้วยเหตุผลที่ถูก:
          **อย่าให้ผลลัพธ์ขึ้นกับว่า regex มีกี่กลุ่ม** (`$1`+`2` จะกัดถ้าวันหนึ่งมี 12 กลุ่ม)
          ⇒ เหตุผลนี้ทดสอบได้ด้วย regex กี่กลุ่มก็ตาม · เหตุผลเดิมทดสอบด้วย 1 กลุ่มแล้วจะสรุปว่าไม่มีปัญหา */
    แก้: (s) => s.replace(
      /(products\.json[^\n]*?วัดจริง[^\n]*?:\s*)[\d,]+(\s*รายการ)/,
      (_, ก่อน, หลัง) => ก่อน + "99,999" + หลัง
    ),
    คำสั่ง: ["node", "scripts/check-claude-md-numbers.mjs"],
    /* 🔑 **`ต้องเอ่ยถึง` ต้องเป็นรูปที่ "ด่านพิมพ์" ไม่ใช่รูปที่ "เราปลูก"**
       ผมปลูก `99,999` (มีคอมมา ตามรูปในคู่มือ) แต่ด่าน normalize เป็นตัวเลขก่อนพิมพ์
       ⇒ มันพิมพ์ `คู่มือบอก 99999` ⇒ การเทียบด้วย "99,999" จึงไม่เจอ
       ⇒ แล้วเครื่องมือตอบว่า "ตะแกรงของด่านมีปัญหาจริง" = **โทษด่านผิดอีกชั้น**
       ⇒ ⇒ ชั้นนี้เพิ่งเสียเวลาไปสองรอบ (สตริง `$199` · คอมมา) ⇒ จดไว้เพื่อไม่ให้เสียอีก */
    ต้องเอ่ยถึง: "99999",
    ที่มา: "เลขในคู่มือค้างได้เงียบ ๆ — ตัวเลขที่โตเองต้องมีตัวเฝ้า",
  },
  {
    /* ───── ทางที่สองของด่านคู่มือ: **หาประโยคไม่เจอ** (20 ก.ย. 2569) ─────
       ฝั่งจอเดินทางนี้ในด่านของเขาแล้วพบว่า **ตัวอ่านที่เดาเลขเมื่ออ่านไม่ออก**
       ทำให้คู่มือ "ตรง" ทั้งที่ไม่มีใครอ่านอะไรได้ ⇒ เขาบังคับให้คืน null
       🔑 ฝั่งผมไม่เดา (ใช้ `m[1]` จาก regex) **แต่ทางนี้ไม่เคยถูกเดินผ่าน**
          สูตรเดิมทดสอบแค่ทาง "เลขไม่ตรง" ⇒ ทาง "หาไม่เจอ ⇒ ตก" ยังเป็นคำกล่าวอ้าง */
    ด่าน: "check-claude-md-numbers (หาประโยคไม่เจอ)",
    ไฟล์: "CLAUDE.md",
    เงื่อนไขเป้า: "ต้องแก้ **ถ้อยคำ** ของบรรทัดที่ regex ยึด (ไม่ใช่ตัวเลข) ⇒ ตัวอ่านหาไม่เจอ · แก้ตัวเลขจะไปเข้าทาง 'เลขไม่ตรง' ซึ่งเป็นสูตรอีกใบ",
    แก้: (s) => s.replace("ls public/rv-img | wc -l", "ls public/rv-img ตัวนับใหม่"),
    คำสั่ง: ["node", "scripts/check-claude-md-numbers.mjs"],
    ต้องเอ่ยถึง: "หาประโยคที่ลงทะเบียนไม่เจอ",
    ที่มา: "ด่านที่อ่านคู่มือด้วย regex จะเงียบทันทีที่ถ้อยคำเปลี่ยน ⇒ ต้องพิสูจน์ว่ามันร้อง",
  },
  {
    /* 🔴 **ปิดหนี้ที่ผมประกาศเองเมื่อจดหมายที่แล้ว** (20 ก.ย. 2569 · ฝั่งจอปิดของเขาก่อน)
       ผมเขียนว่า "4 ตัวนอก prebuild พิสูจน์ได้ตั้งแต่ตอนนี้ · ยังไม่ทำ" ⇒ นี่คือตัวแรก
       🔑 ปลูกตรงรูปที่ด่านนี้ประกาศไว้เอง: **รายชื่อรุ่นสองฝั่งไม่ตรงกัน**
          (ต้นฉบับ `src/lib/permit.ts` · สำเนา `netlify/lib/permit-models.mjs`)
       ⚠️ ด่านนี้ **ทำให้ build ตกโดยเจตนา** เพราะเป็นข้อมูลกฎหมาย —
          ร้านเพิ่มรุ่นใหม่แล้วลืมแก้ฝั่งเซิร์ฟเวอร์ = เครื่องคิดเงินไม่เตือนเรื่องทะเบียน
          ⇒ ลูกค้าถือเลื่อยที่ต้องมีใบอนุญาตออกจากร้านโดยไม่มีใครรู้ */
    ด่าน: "check-permit-models",
    ไฟล์: "netlify/lib/permit-models.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องเปลี่ยน **ตัวรุ่น** ในสำเนาฝั่ง .mjs (ไม่ใช่ช่องว่าง/คอมเมนต์) — ด่านเทียบรายชื่อกับ src/lib/permit.ts",
    แก้: (s) => s.replace('"F250",', '"F250X",'),
    คำสั่ง: ["node", "scripts/check-permit-models.mjs"],
    ต้องเอ่ยถึง: "ไม่ตรงกัน",
    ที่มา: "สำเนารายชื่อรุ่นสองฝั่งต้องตรงกัน — .mjs import .ts ตรง ๆ ไม่ได้ จึงต้องมีสำเนา",
  },
  /* ───── ด่านที่ "ต้องมีเว็บจริง" — ปลูกใน **คำตอบ** ผ่านท่อปลอม (20 ก.ย. 2569)
     🔑 ทั้งสามเคยหลุดจากทั้งตัวเศษและตัวส่วน เพราะรันในลูกโซ่ build ไม่ได้
        (บทเรียนฝั่งจอ `the-checkers-closest-to-reality-are-the-least-proven`)
     🚫 ขอบเขต: พิสูจน์ว่า "แยกคำตอบดีจากเสียได้" **ไม่** พิสูจน์ว่าเล็งโดเมน/คีย์จริงถูก ───── */
  /* ───── 🧩 `check-lib-loads` — ตัวช่วยใน `scripts/lib/` ต้องโหลดขึ้นได้จริง
     🔴 ที่มา 20 ก.ย. 2569: ผมลืม `import` ใน `gen-zort-arch.mjs`
        ⇒ **เทส 709 ผ่าน · `node --check` ผ่าน · ตัวสร้างพังด้วย `ReferenceError`**
     🚫 ขอบเขต: ครอบแค่ **ระดับบนสุดของโมดูล** — `ReferenceError` ในฟังก์ชันที่ไม่มีใครเรียกยังหลุด ───── */
  /* ───── 🖼️ `check-vectors-vs-index` — ลายนิ้วมือรูปต้องตรงกับดัชนีค้นหา
     🔴 ที่มา 20 ก.ย. 2569: คู่มือมีกติกา "ต้องตรงกันเป๊ะ" **3 คู่** · มีด่านคุม 2 คู่
        คู่ที่สาม (`img-vectors.bin` ↔ `search-index.json`) **ไม่มีใครคุม**
        และคู่มือเขียนผลไว้ว่า **"ผลค้นหาจะสลับตัวกันมั่ว"** ⇒ ลูกค้าได้สินค้าผิดตัวแบบดูน่าเชื่อ
     🚫 ขอบเขต: จับได้ทิศเดียว — จำนวนไม่เท่า ⇒ ผิดแน่ · เท่ากัน ⇒ **ยังไม่รู้ว่าเรียงตรง** ───── */
  {
    ด่าน: "check-vectors-vs-index (ลายนิ้วมือรูปคนละชุดกับดัชนี)",
    ไฟล์: "public/img-vectors.bin",
    เงื่อนไขเป้า: "ต้องแก้ **หัวไฟล์ไบนารี** (จำนวนเวกเตอร์ uint32 ที่ byte 0-3) หรือ **ตัดท้ายไฟล์** ⇒ ปลูกที่ข้อความในซอร์สไม่ได้เพราะเป้าเป็นไฟล์ไบนารี",
    แก้: (s) => s,   /* ปลูกไบนารีทำผ่าน `แก้` ที่รับสตริงไม่ได้ ⇒ ใช้ `ปลูกไบนารี` ข้างล่าง */
    ปลูกไบนารี: (buf) => { const d = Buffer.from(buf); d.writeUInt32LE(d.readUInt32LE(0) - 1, 0); return d; },
    คำสั่ง: ["node", "scripts/check-vectors-vs-index.mjs"],
    ต้องเอ่ยถึง: "คนละชุด",
    ที่มา: "กติกา 'ต้องตรงกันเป๊ะ' คู่ที่ 3 ในคู่มือ ที่ยังไม่มีด่านคุม (20 ก.ย. 2569)",
    /* ⚠️ **หมายเหตุสำคัญ**: ตัวปลูกกลาง (`ปลูกแล้ววัด`) อ่าน/เขียนไฟล์เป็น utf8
       ⇒ **ใช้กับไฟล์ไบนารีไม่ได้** (จะทำไฟล์เสียถาวร) ⇒ สูตรนี้จึงพิสูจน์ด้วยมือแล้ว
       📏 พิสูจน์ 20 ก.ย. 2569: แก้จำนวนในหัว ⇒ exit 1 เอ่ย "คนละชุด" ·
          ตัดท้ายไฟล์ 512 ไบต์ ⇒ exit 1 เอ่ย "ตัดกลางทาง" · คืนไฟล์ ⇒ exit 0
       🔑 จดไว้เป็นหนี้: ตัวปลูกกลางยังไม่รองรับไบนารี ⇒ สูตรนี้ **ไม่อยู่ในลูปปลูกอัตโนมัติ** */
    ข้ามลูปปลูก: "ตัวปลูกกลางอ่านไฟล์เป็น utf8 ⇒ ใช้กับไบนารีไม่ได้ · พิสูจน์ด้วยมือแล้ว 20 ก.ย. 2569",
  },
  {
    ด่าน: "check-lib-loads (ลืม import ⇒ ReferenceError ตอนโหลด)",
    ไฟล์: "scripts/lib/รูปที่เคยพลาด.mjs",
    เงื่อนไขเป้า: "ต้องปลูกให้เกิด **ReferenceError ที่ระดับบนสุดของโมดูล** (ไม่ใช่ SyntaxError ซึ่ง node --check จับได้อยู่แล้ว · ไม่ใช่ในฟังก์ชันที่ไม่มีใครเรียก ซึ่งด่านนี้ไม่ครอบ)",
    แก้: (s) => `export const __ปลูกทดสอบ = ตัวที่ไม่มีอยู่จริง();\n` + s,
    คำสั่ง: ["node", "scripts/check-lib-loads.mjs"],
    ต้องเอ่ยถึง: "ReferenceError",
    ที่มา: "คลาสที่ผมพลาดเอง 20 ก.ย. 2569 · ฝั่งจอทำด่านเดียวกันในรีโปเขา (check-lib-loads)",
  },
  {
    ด่าน: "verify-contract-keys (คีย์ตามสัญญาหายไปจากคำตอบ)",
    ไฟล์: "scripts/lib/ท่อปลอมสำหรับวัดด่าน.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องถอดคีย์ที่อยู่ใน **รายการสัญญา** ของด่านนี้เท่านั้น (คีย์อื่นด่านไม่ตรวจ) · และท่อปลอมต้องขึ้นได้จริงก่อนวัด",
    แก้: (s) => s.replace("burnPerDay: 1, daysLeft: 9,", "burnPerDay: 1,"),
    ยืนยันปลูกลง: ["node", "scripts/ยืนยันคำตอบดีของท่อปลอม.mjs"],
    คำสั่ง: ["node", "scripts/วัดด่านที่ต้องมีเว็บ.mjs", "verify-contract-keys"],
    ต้องเอ่ยถึง: "ขาดคีย์: daysLeft",
    ที่มา: "สัญญากับฝั่งจอ 19 ก.ย. 2569 — ป้ายเครดิตเปลี่ยนสีตาม daysLeft",
  },
  {
    ด่าน: "audit-core-contract (ขาเขียนที่ควรถูกปฏิเสธ กลับตอบ 200)",
    ไฟล์: "scripts/lib/ท่อปลอมสำหรับวัดด่าน.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องแก้ **ตารางตัดสินสถานะ** ให้ปล่อยขาเขียนผ่าน (405→200) · แก้เนื้อคำตอบเฉย ๆ ด่านไม่สนใจ (มันดู status code)",
    /* ปลูกให้ POST ทั่วไปตอบ 200 = ท่อเปิดขาเขียนให้ทุกคน (ความผิดที่ร้ายแรงที่สุดของเส้นนี้) */
    แก้: (s) => s.replace('if (มี("move=1") || มี("addcontact=1")) return 400;   // ต้องตีกลับก่อนเขียน\n    return 405;',
                          'if (มี("move=1") || มี("addcontact=1")) return 400;\n    return 200;'),
    ยืนยันปลูกลง: ["node", "scripts/ยืนยันคำตอบดีของท่อปลอม.mjs"],
    คำสั่ง: ["node", "scripts/วัดด่านที่ต้องมีเว็บ.mjs", "audit-core-contract"],
    ต้องเอ่ยถึง: "FAIL",
    ที่มา: "เส้น /api/core ต้องตอบ 405 กับ POST/DELETE ที่ไม่ใช่เส้นเขียน — กันเขียนข้อมูลโดยไม่ตั้งใจ",
  },
  {
    ด่าน: "probe-applied-keys (เส้นไม่ส่ง applied)",
    ไฟล์: "scripts/lib/ท่อปลอมสำหรับวัดด่าน.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องถอด applied ออกจากคำตอบของเส้น list= · 🔑 คาดผลที่ห้าไว้แล้ว (ตัวนี้เป็นตัววัด exit 0 เสมอโดยเจตนา)",
    แก้: (s) => s.replace("  applied: { q: null, from: null },\n", ""),
    ยืนยันปลูกลง: ["node", "scripts/ยืนยันคำตอบดีของท่อปลอม.mjs"],
    คำสั่ง: ["node", "scripts/วัดด่านที่ต้องมีเว็บ.mjs", "probe-applied-keys"],
    ต้องเอ่ยถึง: "ไม่ส่ง applied",
    /* 🔑 คาดไว้ว่าจะได้ **ผลลัพธ์ที่ห้า** — ตัวนี้เป็น "ตัววัด" ไม่ใช่ "ด่าน" ⇒ exit 0 เสมอโดยเจตนา
       (ท่าเดียวกับ `check-gate-source` ของฝั่งจอ) ⇒ ความรุนแรงเป็นการตัดสินใจ แยกจากการตรวจจับ */
    ที่มา: "ชุดคีย์ของ applied = คำประกาศที่เส้นนั้นพูดเอง — ฝั่งจอใช้แทนการเดาจากอาการ",
  },
  /* ───── ด่านที่ตรวจ "สูตรปลูกยังใช้ได้ไหม" — ต้องถูกพิสูจน์ด้วยสูตรเหมือนกัน (20 ก.ย. 2569)
     🔑 ฝั่งจอลงทะเบียนตัวนี้ก่อน แล้วบอกมา ⇒ ผมทำตาม
     ⚠️ ปลูกที่ **ไฟล์ทะเบียนนี้เอง** ⇒ ทำให้สูตรของรายการแรกในทะเบียนหมดอายุ
        แล้ว `check-plant-formulas` ต้องร้องว่า "ปลูกไม่ลง" ⇒ เป็นการวัดตัวมันเองด้วยของจริง ───── */
  {
    ด่าน: "check-plant-formulas (สูตรในทะเบียนหมดอายุ)",
    ไฟล์: "scripts/lib/ทะเบียนด่าน.mjs",
    /* 🔑 **เงื่อนไขของเป้า** — ฝั่งจอสรุป 20 ก.ย. 2569: ตัวสูตรอ่านออกจากโค้ดได้
       แต่ "ทำไมต้องเป็นเป้านี้" อ่านไม่ออกถ้าไม่เขียน · เป็นส่วนที่แพงที่สุดและหายง่ายที่สุด
       (เขาปลูกนอกขอบเขต 6 ครั้งในวันเดียว ทุกครั้งเสียเวลาไล่ว่า "ด่านพังหรือเราผิด") */
    เงื่อนไขเป้า: "ต้องทำให้ฟิลด์ `ไฟล์` ของรายการอื่นชี้ไปไฟล์ที่ไม่มีอยู่ · และไฟล์นี้ต้องสะอาดใน git ก่อนปลูก (ตาข่ายของเครื่องมือจะปฏิเสธ)",
    /* ทำให้สูตรของ check-syntax ชี้ข้อความที่ไม่มีในไฟล์เป้าหมายอีกแล้ว */
    แก้: (s) => s.replace('ไฟล์: "netlify/lib/deploy-state.mjs"', 'ไฟล์: "netlify/lib/ไฟล์ที่ไม่มีอยู่จริง.mjs"'),
    /* 🔴 **สัญญาของ `ยืนยันปลูกลง` คือ "คำสั่งที่ต้องล้ม *หลัง* ปลูก"** (สุขภาพดีก่อน · พังหลัง)
       รอบแรกผมเขียนกลับทิศ (ล้มเมื่อ *ยัง* ไม่ปลูก) ⇒ เครื่องมือตอบ "ปลูกไม่ลง"
       ⇒ ซึ่ง **อ่านเหมือนสูตรผิด** ทั้งที่สูตรถูก · ตัวยืนยันผิด
       ⇒ ท่าที่ถูก: เช็คคุณสมบัติที่ **จริงอยู่ตอนนี้** แล้วปลูกทำให้มันเท็จ
          (ทุก `ไฟล์` ในทะเบียนต้องมีอยู่จริง ⇒ ปลูกชี้ไปไฟล์ที่ไม่มี ⇒ ล้ม) */
    ยืนยันปลูกลง: ["node", "-e", "Promise.all([import('node:fs'),import('./scripts/lib/ทะเบียนด่าน.mjs')]).then(([fs,m])=>{if(m.ทะเบียน.some(t=>t.ไฟล์&&!fs.existsSync(t.ไฟล์)))process.exit(1)})"],
    คำสั่ง: ["node", "scripts/check-plant-formulas.mjs"],
    ต้องเอ่ยถึง: "ไฟล์เป้าหมายไม่มีอยู่แล้ว",
    ที่มา: "ด่านที่เฝ้าสูตรปลูก ถ้าตัวมันเองไม่ถูกพิสูจน์ ทะเบียนทั้งชุดจะเชื่อไม่ได้",
  },
  {
    ด่าน: "probe-list-filters (ประกาศว่ากรองแต่เมินจริง)",
    ไฟล์: "scripts/lib/ท่อปลอมสำหรับวัดด่าน.mjs",
    เงื่อนไขเป้า: "ต้องทำให้ท่อปลอมเข้าโหมด **คืนแถวจริง + ประกาศ supportedFilters + เมินทุกตัวกรอง** · ท่อที่คืน 0 แถว ด่านจะตอบ 'วัดไม่ได้' ซึ่งซื่อสัตย์แต่ไม่แยกแยะ",
    แก้: (s) => s.replace("ถอดapplied = false, เมินตัวกรอง = false", "ถอดapplied = false, เมินตัวกรอง = true"),
    /* 🔴 **ครั้งที่สองของวันที่ผมเขียนตัวยืนยันกลับทิศ** — สัญญาคือ "คำสั่งที่ต้องล้ม *หลัง* ปลูก"
       ⇒ เช็คคุณสมบัติที่ **จริงตอนยังไม่ปลูก** (ท่อปลอมไม่ประกาศ supportedFilters)
         แล้วปลูกทำให้มันเท็จ ⇒ ล้ม
       🔑 ผมพลาดรูปนี้ซ้ำทั้งที่เพิ่งแก้เมื่อไม่กี่ชั่วโมงก่อน ⇒ เขียนกำกับไว้ให้คนถัดไป(และตัวเอง) */
    ยืนยันปลูกลง: ["node", "-e", "import('./scripts/lib/ท่อปลอมสำหรับวัดด่าน.mjs').then(async(m)=>{const t=await m.เปิดท่อปลอม();const r=await fetch(t.ที่อยู่+'/api/core?list=x');const d=await r.json();await t.ปิด();if(d.supportedFilters)process.exit(1)})"],
    คำสั่ง: ["node", "scripts/วัดด่านที่ต้องมีเว็บ.mjs", "probe-list-filters"],
    ต้องเอ่ยถึง: "supportedFilters ไม่ตรงกับที่วัดได้",
    ที่มา: "ปุ่มกรองหลอก — ทีมเจอของจริงหลายรอบ และทุกรอบหน้าตาเหมือนสำเร็จ (200 + ตัวเลขสมเหตุสมผล)",
  },
  /* ── 🌱 เพิ่ม 20 ก.ย. 2569 · ตอบเกณฑ์ที่ฝั่งจอตั้ง: "ด่านตัวไหนไม่มีสูตรปลูกเลยสักสูตร"
     📏 วัดฝั่งท่อ: ด่านในสาย prebuild **17 ตัว** · ไม่มีสูตรเลย **9 ตัว** ⇒ เพิ่มสามตัวนี้ ⇒ เหลือ 6
     ⚠️ ทั้งสามสูตร **ปลูกนอกขอบเขตในรอบแรก** และเครื่องมือแยกให้ได้ว่าเป็น "ด่านถูก เราผิด"
        (ปลูกในไฟล์ที่ด่านยกเว้น · ปลูกชื่อที่ไม่ตรงรูปที่ด่านนับ) ⇒ `เงื่อนไขเป้า` จึงเขียนละเอียด */
  {
    ด่าน: "check-num-helpers (ตัวช่วยกลืน \"อ่านไม่ได้\" เป็น 0)",
    ไฟล์: "netlify/lib/coredb.mjs",
    เงื่อนไขเป้า: "ต้องเป็นไฟล์ .mjs/.ts ที่ด่านกวาด (ทุกรากยกเว้น node_modules/.git/out/.next/public) และ **ยังไม่มีตัวช่วยรูปนี้อยู่ก่อน** ⇒ ชื่อไฟล์จึงใช้เป็น `ต้องเอ่ยถึง` ที่แยกแยะได้ · ชื่อตัวแปรต้องอยู่ในรายการที่ด่านนับ (num·money·toNum·…) และมีช่องว่างนำหน้าตามรูป `(?:const|let|function)\\s+`",
    /* 🔴 **ประกอบชื่อเป็นสองชิ้นโดยตั้งใจ** — ไม่ใช่ความสวยงาม
       `check-num-helpers` กวาด **ทุกไฟล์ .mjs รวมไฟล์นี้** ⇒ เขียนเป็น literal เดียว
       = ด่านนับ **ข้อมูลทดสอบของเรา** เป็นจุดที่ 25 ⇒ **build ตกทันทีที่เพิ่มสูตร**
       (เจอของจริงตอน build 20 ก.ย. 2569 · ด่านจับสูตรที่เขียนมาวัดตัวมันเอง)
       ⇒ ทางเลือกอีกทางคือใส่ไฟล์นี้ในรายการยกเว้นของด่าน — **ไม่เอา**
          รายการยกเว้นค้างเสมอ และจะทำให้ด่านมองไฟล์นี้ไม่เห็นตลอดกาล */
    แก้: (s) => s + `\nconst to${"Num"} = (v) => (Number.isFinite(Number(v)) ? Number(v) : 0);\n`,
    ต้องมีในไฟล์: "const to" + "Num",
    คำสั่ง: ["node", "scripts/check-num-helpers.mjs"],
    ต้องเอ่ยถึง: "coredb.mjs",
    ที่มา: "เกณฑ์ของฝั่งจอ 20 ก.ย. 2569 (ใบ S2 รอบสี่) · รอบแรกผมปลูกชื่อ `__ปลูกnum` ซึ่ง **ไม่ตรงรูป** ⇒ ด่านเขียว ⇒ เกือบสรุปว่าด่านพัง",
  },
  {
    ด่าน: "check-thai-offset-copies (บวก 7 ชม. ด้วยมือจุดใหม่)",
    ไฟล์: "netlify/lib/coredb.mjs",
    เงื่อนไขเป้า: "**ห้ามปลูกใน `netlify/lib/thaiday.mjs`** — ด่านยกเว้นแหล่งกลางกับตัวเองไว้ (ปลูกที่นั่น = นอกขอบเขต เจอมาแล้ว) · ต้องเป็นรูปที่ด่านนับ (เขียนแยกชิ้นในไฟล์นี้เพื่อไม่ให้ด่านนับข้อมูลทดสอบ): บวก ๗ คูณ `3600e3` หรือ `3600 * 1000` หรือ `60 * 60 * 1000` · และเป้าต้องยังไม่มีจุดอยู่ก่อน เพื่อให้ชื่อไฟล์แยกแยะได้",
    /* 🔴 ประกอบเลขเป็นสองชิ้น **เหตุผลเดียวกับสูตร check-num-helpers ข้างบน**
       ด่าน `check-thai-offset-copies` กวาดราก `scripts` ด้วย ⇒ literal เดียว = ด่านนับ 4 จุด
       จากไฟล์นี้ ⇒ **build ตก (49 > 45)** · เจอของจริงตอน build 20 ก.ย. 2569 ครั้งที่สอง
       🔑 ครั้งแรกกับ `check-num-helpers` ผมแก้เฉพาะสูตรนั้น **ไม่ได้ไล่ทั้งคลาส**
          ⇒ เหยียบซ้ำใน build ถัดไปทันที ⇒ กฎ [[fix-whole-bug-class]] ใช้กับเรื่องนี้ด้วย */
    แก้: (s) => s + `\nexport const __ปลูกบวก7 = (t) => new Date(t + 7 * 36${"00"} * 1000);\n`,
    ต้องมีในไฟล์: "__ปลูกบวก7",
    คำสั่ง: ["node", "scripts/check-thai-offset-copies.mjs"],
    ต้องเอ่ยถึง: "coredb.mjs",
    ที่มา: "การปลูกรอบนี้เผยสองข้อ: ① เพดานตั้งไว้ 46 ทั้งที่ของจริง 45 ⇒ **มีช่องให้เพิ่ม 1 จุดเงียบ ๆ** (แก้: ยกกลไก \"ต่ำกว่าเพดานต้องฟ้อง\" จาก check-num-helpers มา) ② ด่านพิมพ์แค่ 6 ไฟล์เรียงตามขนาด ⇒ จุดใหม่ (1 จุด) ไม่เคยโผล่ ⇒ แดงถูกแต่ชี้ผิดที่",
  },
  {
    ด่าน: "check-test-imports-guard (ด่านไม่มี guard ⇒ เทสข้อที่เหลือไม่ได้รัน)",
    ไฟล์: "scripts/check-leaks.mjs",
    เงื่อนไขเป้า: "ต้องเป็นด่านที่ **มีไฟล์เทส import มันอยู่จริง** และปัจจุบันมี guard อยู่ ⇒ ถอด guard ออก (ทำให้ `เป็นตัวหลัก` เป็นจริงตลอด) · ถอดที่ไฟล์เทสไม่ได้ เพราะด่านนี้ตรวจฝั่งด่าน ไม่ใช่ฝั่งเทส",
    แก้: (s) => s.replace(/const เป็นตัวหลัก = [^\n]+/, "const เป็นตัวหลัก = true;"),
    ต้องมีในไฟล์: "เป็นตัวหลัก = true",
    คำสั่ง: ["node", "scripts/check-test-imports-guard.mjs"],
    ต้องเอ่ยถึง: "check-leaks",
    ที่มา: "ด่านนี้เกิดจากบั๊กของผมเอง (เทส 6 ข้อรายงาน pass 1 fail 0) · และตัวมันเองไม่มีสูตรปลูกจนถึงวันนี้ ⇒ ตรงกับกฎ *ไฟล์ที่สร้างเพื่อกันความพลาด คือที่ที่เราระวังน้อยที่สุด*",
  },
  {
    ด่าน: "check-layer-direction (ชั้นล่างพึ่งชั้นบน)",
    ไฟล์: "netlify/lib/thaiday.mjs",
    เงื่อนไขเป้า: "ต้องเป็นไฟล์ใต้ `netlify/lib/` หรือ `src/lib/` (ชั้นล่าง) และของที่ปลูกต้องเป็น import ที่ชี้ไป `functions/` · `edge-functions/` · `components/` · `app/` · ห้ามปลูกในไฟล์ชั้นบน (นั่นคือทิศที่ถูกต้อง ด่านจะไม่ร้องและเราจะอ่านผิดว่าด่านพัง)",
    แก้: (s) => s + `\nimport { STATUSES as __ปลูกทิศ } from "../functions/orders.mjs";\nexport const __ใช้ปลูกทิศ = () => __ปลูกทิศ;\n`,
    ต้องมีในไฟล์: "__ปลูกทิศ",
    คำสั่ง: ["node", "scripts/check-layer-direction.mjs"],
    ต้องเอ่ยถึง: "thaiday.mjs",
    ที่มา: "ฝั่งจอเจอรูปนี้ในรีโปเขาก่อน (`lib/product-menu.ts` → `components/zort`) · ฝั่งท่อวัดตามแล้วเจอ 1 จุดบนเส้นเงิน (`beam-sweep` → `functions/orders`) ซึ่งใช้ `await import()` กันวงกลมไว้ ⇒ **dynamic import ที่ใส่เพื่อกันวงกลมคือหลักฐานว่าทิศผิด ไม่ใช่การแก้**",
  },
  {
    ด่าน: "check-gate-selfcontrol (ด่านที่ไม่มีตัวควบคุมฝังใน)",
    ไฟล์: "scripts/check-layer-direction.mjs",
    เงื่อนไขเป้า: "ต้องเป็นด่าน **ที่อยู่ในสาย prebuild** และ **มีตัวควบคุมฝังในอยู่แล้ว** ⇒ ปลูกโดยถอดการประกาศฝั่ง `ต้องไม่จับ` ออก · ปลูกในด่านที่ไม่มีตัวควบคุมอยู่แล้วจะไม่เปลี่ยนตัวเลข (ด่านเขียว) แล้วเราจะอ่านผิดว่าด่านพัง",
    แก้: (s) => s.replace("  const ต้องไม่จับ = [", "  const __ถอดออกแล้ว = ["),
    ต้องมีในไฟล์: "__ถอดออกแล้ว",
    คำสั่ง: ["node", "scripts/check-gate-selfcontrol.mjs"],
    ต้องเอ่ยถึง: "หนี้โตขึ้น",
    ที่มา: "ฝั่งจอเสนอเป็นหนี้ร่วม (เขา 44/47 · ท่อ 17/19) · และตอนพิสูจน์ ตะแกรงรอบแรกของผม **นับเกิน** เพราะจับ *คำ* `ต้องไม่จับ` ซึ่งปรากฏในข้อความ error ของด่านที่มีตัวควบคุมอยู่แล้ว ⇒ แก้ให้จับ **การประกาศตัวแปร** และเพิ่มเคสควบคุมที่จับความพลาดนั้นไว้ในตัวด่านเอง",
  },
  /* 🇹🇭 **สูตรคู่ภาษาไทย** — เกณฑ์ที่ฝั่งจอตั้ง 20 ก.ย. 2569 และผมรับ:
     **ด่านที่ตะแกรงเป็น regex ต้องมีคู่ภาษาไทยในทะเบียนเสมอ · ไม่มี = พิสูจน์แค่ครึ่งเดียว**
     🔑 เหตุ: **บ้านเราตั้งชื่อฟังก์ชัน/ตัวแปรเป็นไทย** ⇒ นี่คือ **เคสหลัก ไม่ใช่เคสขอบ**
     📏 ของจริงที่พิสูจน์วันนี้: ปลูก `เลข()` ⇒ ด่านเงียบ · ปลูก `toNum()` ⇒ ด่านจับ
        ⇒ ⇒ ตัวช่วยอ่านเลขที่ตั้งชื่อไทย **ไม่เคยถูกนับเลย** (แก้แล้วในคอมมิตเดียวกัน) */
  {
    ด่าน: "check-num-helpers (ชื่อไทย — คู่ภาษาของสูตรข้างบน)",
    ไฟล์: "netlify/lib/coredb.mjs",
    เงื่อนไขเป้า: "ต้องปลูกตัวช่วยที่ **ตั้งชื่อเป็นอักษรไทย** และกลืน 'อ่านไม่ได้' เป็น 0 · ชื่อต้องอยู่ในรายการไทยที่ด่านจับ (เลข·จำนวน·ยอด·ตัวเลข·เป็นเลข·อ่านเลข·เงิน) และ **ห้ามมีอักษรไทยต่อท้าย** (ด่านปิดด้วย (?![ก-๙]) เพื่อไม่ให้ `เลขรวม` ถูกจับเป็น `เลข`)",
    แก้: (s) => s + `\nexport const ${"เลข"} = (v) => (Number.isFinite(Number(v)) ? Number(v) : 0);\n`,
    ต้องมีในไฟล์: "Number.isFinite(Number(v))",
    คำสั่ง: ["node", "scripts/check-num-helpers.mjs"],
    ต้องเอ่ยถึง: "coredb.mjs",
    ที่มา: "ฝั่งจอเจอรูปเดียวกันในรีโปเขา (`check-tab-counts` จับสถานะด้วย `const [(\\w+)...]` ⇒ ชื่อไทยไม่เคยถูกจับ **และคอมเมนต์เหนือบรรทัดนั้นเขียนไว้เองว่ารับชื่อไทย**) ⇒ ผมวัดฝั่งนี้ตามแล้วเจอกับ `check-num-helpers`",
  },
];



/* 📋 **ตัวตรวจที่อยู่นอกลูกโซ่ prebuild** — ต้องมีในตัวหารด้วย ไม่งั้นความครอบคลุมดูดีเกินจริง
 * 🔴 รายการนี้ **พิมพ์ด้วยมือ** ⇒ วันที่มีใครเพิ่ม/ลบตัวตรวจ มันจะค้างเงียบ ๆ
 *    ⇒ ฝั่งจอทำรายการของเขาให้ **ตรวจตัวเอง** แล้วบอกมา 20 ก.ย. 2569 ⇒ ผมทำตาม
 *    ⇒ `check-plant-formulas.mjs` (อยู่ใน prebuild) ตรวจสองข้อทุก build:
 *       ① ทุกชื่อในรายการต้องมีไฟล์จริง  ② ไฟล์ `check-*` ที่ไม่อยู่ในลูกโซ่ ต้องอยู่ในรายการ
 * 🔑 กติกาที่ได้: **รายการยกเว้นทุกชนิดคือคำกล่าวอ้างที่จะค้างทันทีที่มีคนแก้ของจริง** */
export const ตัวตรวจนอกลูกโซ่ = [
  "audit-core-contract.mjs", "check-filters-work.mjs", "check-ordercheck-shipping.mjs",
  "check-paging-complete.mjs", "check-permit-models.mjs", "probe-applied-keys.mjs",
  "probe-fallthrough-markers.mjs", "probe-skip-endpoints.mjs", "probe-list-filters.mjs", "verify-claims.mjs", "verify-contract-keys.mjs",
];

