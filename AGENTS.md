# กฎสำหรับ agent ทุกตัวที่ทำงานใน repo นี้ (Codex อ่านไฟล์นี้อัตโนมัติ)

> ไฟล์นี้คือ "ฉบับย่อ" — กติกาเต็มอยู่ใน `CLAUDE.md` (ภาษาไทย ยาวมาก แต่ทุกข้อ
> แลกมาด้วยของพังจริง) · ขัดกันเมื่อไหร่ **CLAUDE.md ชนะ**
> This file is the short version; `CLAUDE.md` (Thai) is authoritative.

## 🔴 ห้ามเด็ดขาด — เคยพังจริงมาแล้วทุกข้อ / Hard rules

1. **ห้ามแตะ `src/data/reviews.json` ให้ยอดลด** — 11,304 รีวิวมาจากร้านที่ปิดถาวร
   ดึงซ้ำไม่ได้อีกตลอดกาล · `count` ≠ `items.length` (count คือยอดจริงจากมาร์เก็ตเพลส
   items เป็นแค่ตัวอย่าง) — **ห้ามคำนวณ count/avg ใหม่จาก items เด็ดขาด**
2. **ห้ามใส่ที่อยู่ร้านลงโค้ดฝั่งลูกค้า** โดยเฉพาะ `src/lib/shop.ts` — ค่าในไฟล์นั้น
   ติดไปกับ bundle ที่ส่งให้ทุกเบราว์เซอร์ · ตรวจ: `grep -rl "81 หมู่ 11" out/` ต้องว่าง
3. **ห้ามยิง POST ไป ZORT API** — ZORT ตอบ 200 แทบทุกอย่าง แยกไม่ออกว่า
   "ถูกปฏิเสธ" หรือ "สร้างเอกสารจริงไปแล้ว" · ตรวจสอบ = GET เท่านั้น
4. **ห้ามแตะการตั้งค่า LINE ของ @gucut1** (webhook · token) — token ใช้ร่วมกับ
   ระบบแชท ZORT ที่ร้านหากินอยู่ทุกวัน · Reissue = แชทร้านพังทั้งระบบ
5. **ห้าม push ไป `main` และห้าม deploy** — deploy กิน 87% ของเครดิต Netlify
   ทั้งเดือน · เจ้าของร้านสั่งรวบ push รอบเดียว 21:00 ทุกวัน
   → ทำงานใน branch `codex/<งาน>` เท่านั้น แล้วรอคนรีวิวรวมให้
   → 🔴 **ห้ามสลับ branch ในโฟลเดอร์นี้** — มี agent อื่นทำงานค้างอยู่ checkout
   จะกวาดงานเขาพัง · ใช้ worktree แยก: `git worktree add ~/codex-wt/<งาน> -b codex/<งาน>`
6. **ห้ามใช้คีย์หลังร้าน** (`CHAT_ADMIN_KEY` · คีย์ ZORT · `~/.gucut-admin-key`) —
   งานที่ต้องยิง API จริง ให้เขียนสคริปต์ไว้แล้วบอกในคำอธิบาย branch คนรีวิวจะยิงให้
   → **branch `codex/*` อายุไม่เกินวันเดียว** — ทำไม่จบในวัน ให้ปิด branch ทิ้งแล้ว
   เริ่มใหม่จาก main ล่าสุดวันถัดไป (main เดินเร็ว branch ค้างข้ามวัน conflict แน่นอน)
   → ผังทีมเต็มอยู่ที่ `~/claude-shared/สถาปัตยกรรม-ทีม.md`
7. **ห้ามลบ/ย้ายไฟล์ใน `public/img/` `public/rv/` `public/rv-img/` `public/model/`**
   — ไฟล์นับพันที่ไม่มีสำรองครบชุดที่อื่น
8. **ห้ามเขียนคำว่า "ส่งฟรี"** — ร้านไม่ได้ส่งฟรี (ยกเว้นข้อความในหน้า `/policy/*`
   ซึ่งเจ้าของร้านสั่งให้คงตามต้นฉบับเดิม — **ห้ามแก้หน้านโยบายทุกหน้า**)
9. **ห้ามเพิ่มลิงก์บทความ/ใบอนุญาตเข้าเมนู** — เจ้าของร้านสั่งไว้
10. **หน้าต้องห้าม: `/catalog/` ใน repo gucut-next** (admin.gucut.com/catalog/) —
    เจ้าของร้านสั่ง 7 ก.ย. 2569 **ห้ามแตะต้องทุกกรณี** (ไม่ใช่แค่ห้ามลบ — ห้ามแก้/ย้าย/
    ปรับปรุง/refactor) เว้นแต่เจ้าของร้านสั่งเป็นคำพูด · รหัส OEM/HH ในนั้นไม่มีที่อื่น

## ⚠️ กติกาเทคนิคฝั่งเซิร์ฟเวอร์ (`netlify/**/*.mjs`)

- **ห้ามปล่อย promise ลอย** — Netlify แช่แข็งฟังก์ชันทันทีที่ตอบเสร็จ งานที่ไม่
  `await` ตายเงียบ · `scripts/check-floating.mjs` รันใน prebuild เจอแล้ว build ตก
  จำเป็นจริงให้ใส่คอมเมนต์ `ปล่อยลอย-ตั้งใจ` กำกับบรรทัด
- **เวลา**: เซิร์ฟเวอร์รัน UTC ร้านอยู่ไทย (UTC+7) — ทุกที่ที่ "วัน" มีความหมาย
  ทางธุรกิจต้อง `date(col, '+7 hours')` ก่อนเทียบ
- ไฟล์ `.mjs` ไม่มี type checking — `npm run build` ผ่านไม่ได้แปลว่าไม่พัง
  **แก้ API แล้วต้องยิงของจริงทดสอบเสมอ** (ReferenceError โผล่ตอนรันเท่านั้น)
- API หลังร้านทุกตัวต้องผ่าน `netlify/lib/admin-gate.mjs` — ห้ามเทียบคีย์เอง
- ส่งเงินให้ ZORT ต้องครบสามชั้น: ราคาต่อหน่วย → รวมบรรทัด → รวมหัวใบ
  (ZORT ไม่คำนวณอะไรให้เลย ใบ ฿0 ก็ผ่านได้เลขที่ใบจริง)

## 📋 วิธีทำงานร่วมกับ agent ตัวอื่น

- เครื่องนี้มี Claude 2 บัญชีทำงานอยู่ (`gucut` ฝั่งท่อ/repo นี้ · `gucut2` ฝั่งจอ/repo
  `~/gucut-next`) — **ห้ามแก้ไฟล์ที่มี uncommitted changes ของคนอื่น** เช็คด้วย
  `git status` ก่อนเริ่มเสมอ
- สถานะงานปัจจุบันอยู่ที่ `~/claude-shared/handoff.md` — **อ่านได้ อ่านก่อนเริ่มเสมอ**
- 🔴 **Codex ห้ามเขียนไฟล์ใด ๆ ใน `~/claude-shared/`** — Claude สองตัวอ่านสมุดนั้น
  เป็นกึ่งคำสั่ง (เหตุผลเดียวกับที่ห้ามใช้ช่องคุยสด) · ผลงาน/ข้อค้นพบของ Codex
  สื่อสารทางเดียว: **branch + commit message + ไฟล์ .md ใน branch นั้น**
- ก่อนบอกว่า "ยังไม่มีใครทำ X" ให้ grep ก่อน — งานส่วนใหญ่มีคนแตะไปแล้ว

## ✅ ก่อนส่งงานทุกครั้ง

```bash
npm run build                      # ต้องผ่าน (รวม check-floating ใน prebuild)
node --check <ไฟล์ .mjs ที่แก้>     # ทุกไฟล์เซิร์ฟเวอร์ที่แตะ
```

commit message เขียนภาษาไทย บอก "ทำไม" ไม่ใช่แค่ "ทำอะไร" — ดูตัวอย่างจาก `git log`
