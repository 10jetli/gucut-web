# new78.com = เว็บซ้อมชั่วคราว ยังไม่ให้ Google เก็บ
# ห้ามให้ index เด็ดขาด ไม่งั้นจะกลายเป็นเนื้อหาซ้ำกับ gucut.com
# แล้ว Google อาจเลือกให้อันดับ new78.com แทน gucut.com ที่สะสมอันดับมานาน
#
# ═══════════════════════════════════════════════════════════════════════════
# วันที่ย้ายไปอยู่บน gucut.com จริงแล้ว ให้แทนที่ทั้งไฟล์ด้วยข้างล่างนี้
# (ทำ "หลัง" DNS ชี้มาแล้วเท่านั้น — เปิดก่อน = โดนเก็บซ้ำสองเว็บ)
#
#   User-agent: *
#   Allow: /
#   Disallow: /admin/            ← ห้ามลืม หลังร้านห้ามขึ้น Google
#   Disallow: /account/          ← หน้าส่วนตัวของลูกค้า
#   Disallow: /checkout/
#
#   # ── ผู้ช่วย AI: เปิดให้เข้าทั้งหมด (ตั้งใจ) ──
#   # ร้านอยากให้ ChatGPT / Claude / Perplexity / Gemini อ่านแล้วแนะนำร้านเรา
#   # ได้เวลามีคนถามว่า "ซื้อเลื่อยยนต์ที่ไหนดี" — นี่คือหัวใจของ GEO/LLMO
#   # เว็บส่วนใหญ่บล็อกพวกนี้ ซึ่งเสียโอกาสฟรี ๆ
#   User-agent: GPTBot
#   Allow: /
#   User-agent: OAI-SearchBot
#   Allow: /
#   User-agent: ChatGPT-User
#   Allow: /
#   User-agent: ClaudeBot
#   Allow: /
#   User-agent: Claude-Web
#   Allow: /
#   User-agent: PerplexityBot
#   Allow: /
#   User-agent: Google-Extended
#   Allow: /
#   User-agent: Applebot-Extended
#   Allow: /
#   User-agent: cohere-ai
#   Allow: /
#
#   Sitemap: https://gucut.com/sitemap.xml
#
#   # ไฟล์ข้อมูลแบบเครื่องอ่าน — บอกไว้ใน /llms.txt ด้วย
#   #   /products.json  สินค้าทุกตัวพร้อมราคาและสต็อก
#   #   /feed.json      คลิปหน้างานจริง
# ═══════════════════════════════════════════════════════════════════════════
User-agent: *
Disallow: /
